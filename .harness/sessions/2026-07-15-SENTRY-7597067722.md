# Session: SENTRY-7597067722 — Failed to fetch notifications:

- Started: 2026-07-15T00:00:00Z
- Branch: sentry-fix/SENTRY-7597067722
- Starting commit: f1b52415ab705d798fc471bf3aab4b6c35767217

## Plan
Triage Sentry issue MGR-J ("Failed to fetch notifications:" on /sales/customers/:id, error extra: `TypeError: Failed to fetch (phwjrfdtebftetctkhdr.supabase.co)`), classify root cause, and fix it if reachable from application code. This is a recurrence of an issue previously fixed on 2026-07-13 (see `.harness/sessions/2026-07-13-SENTRY-7597067722.md`) for a different symptom (lock-steal AbortError); this event has a different underlying cause.

## Decisions
- 2026-07-15T00:00:00Z — Ruled out (A) at the call site: `src/contexts/notifications.tsx:121-130` already passes the raw Supabase `error` object to `log.error` (not a destructured copy) per the fix note referencing SENTRY-7597067759. No app-code defect at the call site.
- 2026-07-15T00:00:00Z — Read `node_modules/@supabase/postgrest-js/src/PostgrestBuilder.ts` (confirmed via WebFetch against the upstream source since the installed version matches) — its fetch `.catch()` handler returns `{ error: { message, details, hint, code } }` as a **plain object literal**, not an `instanceof Error`, for network-level fetch failures (and this same object shape is used generally for the `error` field on any non-throwing Postgrest response).
- 2026-07-15T00:00:00Z — Classified **(D) observability gap**: `src/lib/client-logger.ts`'s `forward()` only calls `Sentry.captureException` when `arg instanceof Error`; this plain object fails that check and falls to `Sentry.captureMessage(message)`, discarding stack/details/code. This exactly matches the event: title equals the literal log message string, Stack Trace is "(unavailable)".
- 2026-07-15T00:00:00Z — The underlying reason the fetch itself failed (network/DNS/connectivity to `phwjrfdtebftetctkhdr.supabase.co` from the dev browser) remains classification (B)/(C) and is not diagnosable or fixable from application code — filed as a separate GitHub investigation issue per the (D) branch instructions.
- 2026-07-15T00:00:00Z — Fix: added `isPostgrestErrorLike()` structural check to `client-logger.ts`'s `forward()` — constructs a real `Error` from the plain object (message + stack sourced from `details`), forwards `code`/`hint`/`details` as Sentry extra, and routes to `captureException`. Centralized in the logger so every call site benefits, not just notifications.
- 2026-07-15T00:00:00Z — Pattern scan: grepped `instanceof Error` across `src/` — all other call sites (`src/lib/logger.ts` server-side pino logger, ~40 UI `error instanceof Error ? ... : "fallback"` ternaries) either don't handle Supabase `PostgrestError`-shaped values or aren't reachable from this Sentry issue's stack (server-side logger has its own separate code path and test coverage); left out of scope per "don't modify unrelated code."
- 2026-07-15T00:00:00Z — Updated the existing `SENTRY-7597067722` entry in `docs/feature_list.json` in place (rather than duplicating the id) since `verify-feature.sh` looks up by exact id; added a `notes` field documenting both occurrences.

## Verification log
- `bun run test src/lib/__tests__/client-logger.test.ts` ran pre-fix (via `git stash` of `client-logger.ts` only) — 2 of 13 tests failed as expected (Postgrest-error-shaped objects fell through to `captureMessage`).
- `bun run test src/lib/__tests__/client-logger.test.ts` ran post-fix — all 13 passed.
- `bun run typecheck` ran post-fix — clean.
- `bun run test` (full suite) ran post-fix — 186 files / 2475 tests passed.
- `bun lint` ran post-fix — clean.
- `/simplify` pass — removed a redundant `!(value instanceof Error)` guard inside `isPostgrestErrorLike` (unreachable, since the only call site already returns early on `instanceof Error`). Re-ran typecheck/targeted test/lint — clean.
- `make check` — see PR for final gate result.

## Outcome
- Ending commit: (see PR #513)
- Feature state: passing (the reporting-gap fix; the underlying network failure is NOT resolved — see issue #514)
- Followups: NOT "none" — the underlying cause of the `TypeError: Failed to fetch` network failure against Supabase is still unknown. This session only fixed the *reporting* gap so the next occurrence of this failure will carry a real stack trace, message, and (if applicable) a Postgrest error code in Sentry instead of degrading to a bare message. GitHub issue #514 tracks the unresolved network-level root cause (labels `sentry-fix`, `needs-human`).
