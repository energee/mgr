# Session: SENTRY-7611936148 — createRevisionHistoryDisplay() client-boundary crash

- Started: 2026-07-15T17:00:00Z
- Branch: sentry-fix/SENTRY-7611936148 (created at end of session)
- Starting commit: 6768bac6102c09ac4eb2fc4344136509032781da

## Plan
Triage Sentry issue MGR-S ("Attempted to call createRevisionHistoryDisplay() from
the server but createRevisionHistoryDisplay is on the client"), classify root
cause, and fix if it is genuine application code (classification A).

## Decisions
- 2026-07-15T17:05Z — Classified (A) application code, not (B)/(C)/(D) — traced
  the stack to `revision-history-display.tsx` carrying `"use client"` while its
  export `createRevisionHistoryDisplay()` is invoked eagerly at module scope
  inside every entity `presentation.tsx`. Ruled out (D): the stack trace and
  event context were complete and pointed straight at the defect, no logging
  gap involved.
- 2026-07-15T17:10Z — Confirmed the exact route in the Sentry event
  (`/production/batches`) is *not* currently reproducible on `main`: the
  sitewide-loading-pattern feature (commit c84bc14) already made
  `production/batches/page.tsx` import `batchCore` instead of the assembled
  `batchEntity`, specifically to avoid this class of bug (see its inline
  comment). Verified every other Server Component in `src/app` that touches
  `@/entities/*` does the same (`brandCore`, `batchCore` in
  `[id]/page.tsx`) — no reachable path today. Decided to fix the underlying
  defect anyway (the incorrect "use client" placement) rather than close as
  already-fixed, since the hazard is real and will refire the moment a future
  Server Component imports an assembled entity instead of its core.
- 2026-07-15T17:15Z — Root fix: remove the unneeded `"use client"` from
  `revision-history-display.tsx`. It has no hooks of its own; the actual
  client-only logic lives in the child `RevisionHistory` component, which
  already carries its own directive. Server Components can render Client
  Component children — the wrapper never needed the boundary.
- 2026-07-15T17:20Z — Pattern scan (`grep -n "^export function create[A-Z]"`)
  found the identical shape in `createQBOSyncDisplay`
  (`qbo-sync-section.tsx`), used by customer/supplier/order/purchase-order
  presentations. Unlike revision-history, `QBOSyncSection` in the same file
  genuinely needs `"use client"` (calls useState/useQuery/useMutation
  directly), so the fix there is to split the factory into a new
  `qbo-sync-display.tsx` (no directive) rather than remove the directive
  outright — mirroring the revision-history split.
- 2026-07-15T17:30Z — Regression test written as a source-text assertion
  (`entity-config-client-boundary.test.ts`), not a rendered-component test:
  Vitest has no equivalent of Next's flight-client-entry-loader, so it cannot
  reproduce the actual "call a client reference" runtime error. Followed the
  existing convention for this class of bug
  (`dashboard-lazy-hydration.test.ts`). Verified by temporarily `git stash`-ing
  the fix: 2 of 4 assertions failed against the pre-fix files, passed after
  restoring the fix.

## Verification log
- `bun run typecheck` — clean.
- `bun run test` (full suite) — 181 files / 2454 tests passed.
- `bun lint` — clean.
- `bun run test src/components/domain/shared/__tests__/entity-config-client-boundary.test.ts`
  — failed on pre-fix files (2/4), passed on fixed files (4/4).
- `/simplify` and `/code-review:code-review` skills returned no content in
  this environment; performed both passes manually (diff review for reuse/
  dedup/bugs) — diff is minimal, no further simplification found.
- `make check` — pending, run before PR.

## Outcome
- Ending commit: (pending PR branch creation)
- Feature state: passing
- Followups: none — the fix addresses the actual defect (incorrect "use
  client" placement), covers the one other live instance of the same pattern
  (QBO sync), and the existing `batchCore`-not-`batchEntity` convention on
  Server Component routes remains in place as defense in depth.
