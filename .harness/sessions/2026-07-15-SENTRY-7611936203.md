# Session: SENTRY-7611936203 — createRevisionHistoryDisplay server/client RSC boundary error

- Started: 2026-07-15T00:00:00Z
- Branch: sentry-fix/SENTRY-7611936203
- Starting commit: 6768bac6102c09ac4eb2fc4344136509032781da

## Plan

Triage Sentry issue MGR-T (7611936203): "Attempted to call createRevisionHistoryDisplay() from
the server but createRevisionHistoryDisplay is on the client." Trace the stack, classify root
cause, and fix if it's (A) application code.

## Decisions

- 2026-07-15T00:05Z — Classified (A) application code: an RSC client/server module boundary
  violation, not a database/infra/third-party issue. `createRevisionHistoryDisplay` is exported
  by `src/components/domain/shared/revision-history-display.tsx` ("use client"), and is called
  as a plain factory function at module-evaluation top level inside
  `src/entities/batch/presentation.tsx:254`. The stack trace shows this import chain reached
  through `src/entities/batch/index.ts` from `src/app/(app)/production/batches/loading.tsx` —
  a Server Component at the time.
- 2026-07-15T00:10Z — `git log -p` on `loading.tsx` showed the exact broken revision: it used to
  do `import { batchEntity } from "@/entities/batch"` (the assembled, client-module config) just
  to read `batchEntity.listColumns.length` for a skeleton. Commit `c6b5539` (PR #453, authored
  2026-07-15T07:11:53Z) already rewrote it to `return null`, removing the import entirely. That
  commit is an ancestor of the harness's starting commit (`6768bac`), and its timestamp is after
  the last Sentry event (06:05:31Z) — the bug was live-fixed during the same dev session that
  produced the error, before merging to main.
- 2026-07-15T00:15Z — Pattern scan: grepped every `src/app` file for `from "@/entities/<name>"`
  (assembled config) and cross-checked each hit's directive. Every server file (no `"use client"`)
  imports only `*Core` submodules or the server-safe `@/entities/cores` registry; every hit on an
  assembled config is inside a `"use client"` file. No other route currently reproduces this bug.
- 2026-07-15T00:20Z — Decision: no source fix needed (already shipped). Instead added a static
  regression test, `src/app/__tests__/server-entity-import-boundary.test.ts`, walking every
  non-`"use client"` file under `src/app` and failing if it imports an assembled `@/entities/<name>`
  config. Verified it fails when fed the exact pre-fix `loading.tsx` content (temporarily restored,
  tested, reverted — `git diff` confirmed clean after) and passes on current `main`.
- 2026-07-15T00:30Z — `/simplify` and `/code-review:code-review` skill invocations errored in this
  environment (returned `Execute skill: <name>` with no output); fell back to a manual review of
  the single new test file (regex scoping, recursive dirent path resolution vs. the existing
  `transition-call-sites.test.ts` idiom, false-positive check against every current
  `@/entities/<name>` import site in `src/app`).

## Verification log

- `bun run test -- src/app/__tests__/server-entity-import-boundary.test.ts` ran at 2026-07-15 —
  2/2 passed on current code; confirmed failing (1/2) when the pre-fix `loading.tsx` content was
  temporarily restored.
- `bun run typecheck` ran at 2026-07-15 — clean.
- `bun run test` (full suite) ran at 2026-07-15 — 181 files / 2452 tests passed.
- `bun lint` ran at 2026-07-15 — clean.
- `make check` — see PR CI.

## Outcome

- Ending commit: (see PR)
- Feature state: passing (`docs/feature_list.json` id `SENTRY-7611936203`)
- Followups: none identified — root cause fix already shipped in PR #453; this session only adds
  regression coverage for the same defect class.
