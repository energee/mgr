# Session: SENTRY-7452842898 — ReferenceError: SaveAllButton is not defined

- Started: 2026-05-12T22:58:00Z
- Branch: sentry-fix/SENTRY-7452842898
- Starting commit: d609595ea046cd7bdd647fca6ace2127f4f45144

## Plan
Fix Sentry issue MGR-2: `ReferenceError: SaveAllButton is not defined` occurring on `/production/recipes/:id` during Turbopack Fast Refresh. Move helper component definitions before the main exported component in `recipe-editor-page.tsx` and `recipe-sidebar.tsx` so they are always defined when the module re-evaluates during HMR. Add a regression test that asserts textual ordering via file reads.

## Decisions
- 2026-05-12T23:00Z — Moved `SaveAllButton`, `MobileEstimatesBar`, `RecipeEditorSkeleton` before `RecipeEditorPage` in `recipe-editor-page.tsx` — Turbopack HMR re-runs module code top-to-bottom; function declarations are not hoisted across chunk boundaries.
- 2026-05-12T23:01Z — Moved `EstimateCard`, `SummaryRow` before `RecipeSidebar` in `recipe-sidebar.tsx` — same pattern, same vulnerability.
- 2026-05-12T23:15Z — Also moved `formatNum`, `formatGrainWeight`, `formatHopWeight`, `bagLabel` before `RecipeSidebar` — code review found these four utility functions had the same post-declaration risk.
- 2026-05-12T23:02Z — Chose file-read regression test (not a render test) — avoids heavy React/Supabase mock setup while directly catching the structural invariant that matters.
- 2026-05-12T23:08Z — Removed no-op outer `div` with `justify-between` in `MobileEstimatesBar` (simplify pass) — single child makes `justify-between` a no-op.
- 2026-05-12T23:09Z — Removed JSDoc comments from helper functions (simplify pass) — comments explained WHAT, not WHY.

## Verification log
- `bun run typecheck` — clean
- `bun run test` — 1055/1055 pass (4 new assertions added)
- `bun lint` — clean

## Outcome
- Ending commit: pending (pre-commit)
- Feature state: passing
- Followups: none
