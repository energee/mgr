# Session: SENTRY-7542174707 — Upsert error for %s batch %d: %s (re-triage)

- Started: 2026-07-14T18:40:00Z
- Branch: sentry-fix/SENTRY-7542174707
- Starting commit: 89422c0

## Plan

Fix Sentry issue MGR-A (POST /api/integrations/mongodb/sync, 36 events/14d, first seen 2026-06-10, last seen 2026-07-14T18:33Z). A prior session (2026-07-13, PR #395) already fixed one root cause (duplicate onConflict-key rows failing an entire upsert chunk) — but the issue kept receiving events *after* that fix landed and last-seen is well past it, so this session re-triages from scratch instead of assuming the issue is already closed.

## Decisions

- 2026-07-14T18:42Z — Confirmed PR #395 (commit 30c9124) is merged to `main` and does include `dedupeByConflictKey()`. Since events continued after that fix, the recurrence is a *different* failure, not the same bug resurfacing.
- 2026-07-14T18:45Z — Event Context block in the Sentry issue is empty and the stack trace is `(unavailable)` — matches the harness's (D) observability-gap tell exactly. Read `src/lib/logger.ts`'s `forwardToSentry()` and its test file: the "calls captureMessage when first arg is a string" test asserts `Sentry.captureMessage` receives the *bare, un-interpolated* string when `logger.error("template %s", arg)` is called — confirming this in code, not just inference. The call site `logger.error("Upsert error for %s batch %d: %s", table, i / BATCH_SIZE, error.message)` at `src/integrations/mongodb/sync.ts:172` is a pino printf-style call; `forwardToSentry` has no branch for it (only Error-instance / `{err,...ctx}`+message / bare-string), so it falls into the bare-string branch and forwards the raw template with zero interpolation. This is why the Sentry event has no table name, batch index, or Postgres error message — the actual failure is unknown from this event alone.
- 2026-07-14T18:48Z — Classified **(D)**, not (A)/(B)/(C): the reporting itself is broken, so the underlying Postgres failure cannot be diagnosed from this event. Per the harness's own guidance, a (D) classification requires fixing the reporting AND opening a separate investigation issue for the still-unknown underlying failure — not claiming the Sentry issue resolved.
- 2026-07-14T18:52Z — Verified `PostgrestError` (`node_modules/@supabase/postgrest-js/src/PostgrestError.ts`) extends `Error`, so `error instanceof Error` holds at runtime for the `.upsert()` error in `sync.ts` — meaning the existing `{err, ...ctx}` branch of `forwardToSentry` (already correct, unmodified) is a valid target to route this call site through.
- 2026-07-14T18:55Z — Pattern scan (`Grep` for `logger\.(error|fatal)\(\s*["'\`][^"'\`]*%[sd]`) found 4 files / 13 call sites with the same printf-style anti-pattern (`sync.ts` x2, `email.ts` x2, `mongodb/client.ts` x1, `settings/api-key/route.ts` x8). Rejected editing all 13 call sites across 4 unrelated features as scope creep beyond this Sentry issue (AGENTS.md guardrail: no opportunistic refactors / don't modify unrelated code). Instead fixed the actual shared root cause once, in `forwardToSentry()` itself — a self-contained change to the one file that mishandles all of them — which resolves the observability gap for every current and future printf-style call site without touching those other files. Additionally upgraded the two call sites that this specific issue traces to (`upsertRows()` and `syncPhase()`, both in `sync.ts`, the file/route this issue is about) to structured `{err, ...ctx}` calls, since that gives strictly more diagnostic value (`Sentry.captureException` with a real stack) than even the fixed printf path (`Sentry.captureMessage`, no stack by default).
- 2026-07-14T19:05Z — `/simplify` and `/code-review:code-review` skill invocations both errored (`Execute skill: <name>`) in this environment — same failure mode noted in the 2026-07-13 session. Did a manual simplify pass (moved `printfArgs` construction inside the `if` branch that uses it, avoiding an unnecessary array allocation on every plain-string error log) and dispatched an independent `general-purpose` subagent with no prior context to adversarially review the diff.
- 2026-07-14T19:15Z — Independent review found no blocking bugs. It confirmed `PostgrestError instanceof Error` holds at runtime, confirmed `util.format` output is byte-identical to pino's own formatting for every real call site in the repo, confirmed no stale string-matching on the old log messages, and confirmed the new tests are non-tautological. It flagged two non-blocking observations: (1) forwarding real printf args to Sentry now also surfaces previously-silently-dropped diagnostic content for other files' printf call sites (e.g. `email.ts`'s Resend error messages) — accepted as consistent with the codebase's existing pattern of passing full error messages to `Sentry.captureException` elsewhere (e.g. the 2026-07-13 `SENTRY-7597067759` fix); (2) `util.format` diverges from pino's formatter for `%o`/`%O` object placeholders and overflow args — noted as a latent risk with no current call site that triggers it, not fixed since no real call site is affected.

## Verification log

- New test in `src/lib/__tests__/logger.test.ts` ("interpolates printf-style placeholders before forwarding to Sentry") confirmed FAILING against pre-fix `logger.ts` (via `git stash` of just that file) with the exact un-interpolated-template symptom, then PASSING after restoring the fix.
- New test in `src/integrations/mongodb/__tests__/sync.test.ts` ("logs a genuine upsert failure as a structured object...") confirmed FAILING against pre-fix `sync.ts` (via `git stash` of just that file), then PASSING after restoring the fix.
- `bun run test` (full suite) — 159 files, 2358/2358 pass.
- `bun run typecheck` — clean.
- `bun lint` — clean.
- `make check` — pending (final gate before PR).

## Outcome

- Ending commit: (this session's commit on `sentry-fix/SENTRY-7542174707`)
- Feature state: passing
- Followups: **NOT none.** This is a (D) fix — the reporting is fixed, but the underlying Postgres upsert failure that is still recurring in production after the 2026-07-13 dedup fix remains undiagnosed. An investigation issue was opened (see PR body) for the still-unknown root cause; the next production event for this issue will carry real diagnostic content (table, batch index, actual Postgres error message, or a full stack via `captureException`) and should be re-triaged when it arrives.
