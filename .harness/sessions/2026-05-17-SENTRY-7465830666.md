# Session: SENTRY-7465830666 — Module not found: react-activity-calendar

- Started: 2026-05-17T00:00:00Z
- Branch: sentry-fix/SENTRY-7465830666
- Starting commit: 9fad815

## Plan

Third pass on Sentry issue MGR-4. Previous passes (PRs #258, #261, #298) established that the package is correctly declared in `package.json`, added a regression test, and corrected a stale evidence SHA. This pass audits remaining code improvements: specifically, the `react-activity-calendar/tooltips.css` import still lives inside `batch-activity-heatmap.tsx` and should be moved to `dashboard/layout.tsx` to ship with the route's CSS bundle unconditionally.

## Decisions

- 2026-05-17T00:01Z — Confirmed `react-activity-calendar@3.2.0` is in `package.json` and `bun.lock`. Ran `bun install` to populate `node_modules`. Package resolves correctly; all exports (`ActivityCalendar`, `Activity`, `BlockElement`) match usage in `batch-activity-heatmap.tsx`.
- 2026-05-17T00:02Z — Identified remaining code gap: `import "react-activity-calendar/tooltips.css"` in `batch-activity-heatmap.tsx` is deferred to the component's chunk if the heatmap ever loads lazily, causing tooltip FOUC. The `audit/code-split-charts` branch (unmerged) already plans this move as part of F-142. Moving it now is a targeted fix directly related to MGR-4's error surface.
- 2026-05-17T00:03Z — Moved CSS import to `src/app/(app)/dashboard/layout.tsx`. This is the direct ancestor layout for the dashboard page where `BatchActivityHeatmap` renders. No other dashboard sub-routes reference the heatmap, but the stylesheet is small (tooltip styles only) and layout-level placement is the correct Next.js pattern.
- 2026-05-17T00:04Z — `/simplify` pass: efficiency agent noted CSS loads on all dashboard sub-routes; accepted as minor (small stylesheet, layout is correct boundary, change is future-proof for F-142 code-splitting). No code changes from simplify.
- 2026-05-17T00:05Z — `/code-review` pass: no bugs, logic errors, or security issues found. CSS import in Server Component layout is valid App Router usage.

## Verification log

- `bun run test src/components/dashboard/__tests__/react-activity-calendar-dep.test.ts` — 1 passed
- `bun run typecheck` — clean
- `bun run test` — 1102 / 1102 passed
- `bun lint` — clean
- `make check` — (to be run before PR merge)

## Outcome

- Ending commit: (see PR)
- Feature state: passing
- Followups: F-142 (`audit/code-split-charts` branch) — full `next/dynamic({ ssr: false })` code-splitting for `BatchActivityHeatmap` and `TrendChart`. The CSS is already in the right place for that change.
