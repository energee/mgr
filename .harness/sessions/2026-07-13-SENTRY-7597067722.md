# Session: SENTRY-7597067722 — Failed to fetch notifications:

- Started: 2026-07-13T00:00:00Z
- Branch: sentry-fix/SENTRY-7597067722
- Starting commit: 5a7d9ccbf68eda861686e87164283e798e75e00f

## Plan
Triage Sentry issue MGR-J ("Failed to fetch notifications:" on /dashboard, error extra: `AbortError: Lock broken by another request with the 'steal' option.`), classify root cause, and fix it if it's genuine application code (classification A).

## Decisions
- 2026-07-13T00:00:00Z — Classified **(A) application code**, not (B)/(C)/(D) — reason: no PostgrestError code present, no DB/RPC/permission object implicated; the error text names a browser Web Locks API contention condition ("Lock broken by another request with the 'steal' option"), which is a client-construction bug, not an infra failure.
- 2026-07-13T00:00:00Z — Traced root cause to `src/lib/supabase/client.ts:16` (`createClient()`) building a brand-new `createBrowserClient` (new `GoTrueClient`) on every invocation. Grepped 145+ call sites across `src/`; all share the same auth storage key, so concurrent instances (e.g. every re-render of `NotificationsProviderInner` at `src/contexts/notifications.tsx:109`) compete for the same `navigator.locks` name — a newer instance can steal the lock from an older instance's in-flight `supabase.auth.getSession()`/`getUser()` call, aborting it.
- 2026-07-13T00:00:00Z — Fix: converted `createClient()` to a module-scoped singleton (cache the client instance, return it on subsequent calls) — this is Supabase's own documented remediation for this exact bug class. Verified `src/lib/supabase/server.ts`'s `createClient()` is a separate, correctly per-request implementation (cookie-bound) and left untouched.
- 2026-07-13T00:00:00Z — Pattern scan: grepped all `createBrowserClient`/`createSupabaseClient`/`createClient` factories in `src/lib/supabase/*.ts` — no other non-singleton browser-client factories found; all 145 component call sites route through the one fixed function.

## Verification log
- `bun run test src/lib/supabase/__tests__/client.test.ts` ran pre-fix (via `git stash` of the fix only) — failed as expected (distinct instances per call).
- `bun run test src/lib/supabase/__tests__/client.test.ts` ran post-fix — passed.
- `bun run typecheck` ran post-fix — clean.
- `bun run test` (full suite) ran post-fix — 153 files / 2291 tests passed.
- `bun lint` ran post-fix — clean.
- Independent review via general-purpose subagent — confirmed singleton is SSR-safe (browser-only module, never imported server-side), test is sound (`vi.hoisted` + `vi.resetModules` correctly isolate module state per test), no convention violations, no other bugs found.
- `make check` — see PR for final gate result.

## Outcome
- Ending commit: (see PR)
- Feature state: passing
- Followups: none — this fix eliminates the client-construction race that caused the lock-steal abort. Dev-mode Fast Refresh module reloads (rare, distinct from normal re-renders) could in theory still spin up a fresh singleton if the `client.ts` module itself is HMR-invalidated, but that is a much rarer event than the per-render recreation this fix eliminates, and is not evidenced in the Sentry event data as a separate condition.
