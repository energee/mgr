# Session: SENTRY-7597067759 — Failed to fetch planned batches by day:

- Started: 2026-07-13T00:00:00Z
- Branch: sentry-fix/SENTRY-7597067759
- Starting commit: 5837834

## Plan

Fix Sentry issue MGR-M: `BatchActivityHeatmap` (rendered on `/dashboard`) logs "Failed to fetch planned batches by day:" when the `get_planned_batches_by_day` RPC errors. Trace the stack to source, determine root cause, scan for the same pattern elsewhere, fix, add regression tests, and validate.

## Decisions

- 2026-07-13T00:05Z — Traced the log call to `src/components/dashboard/batch-activity-heatmap.tsx:104`. Verified `get_planned_batches_by_day` (migration 00170) is `SECURITY INVOKER`, has `search_path = public` set, and is present in the live catalog snapshot — ruled out a missing/misconfigured function.
- 2026-07-13T00:12Z — Found the real root cause: the error handler builds a plain object (`{message, code, details, hint}`) from the `PostgrestError` instead of passing the error itself. `src/lib/client-logger.ts`'s `forward()` only routes to `Sentry.captureException` (full message/stack) when `arg instanceof Error` is true; the plain object fails that check and silently falls back to `Sentry.captureMessage` with the bare literal string — which exactly matches the Sentry issue title and the `(unavailable)` stack trace. This also explains why no further diagnostic detail was available to this harness run.
- 2026-07-13T00:15Z — Confirmed via convention scan that every sibling dashboard trend-fetcher (`dashboard/page.tsx`, `dashboard/sales/page.tsx`, `dashboard/inventory/page.tsx`, `entities/enum-value/core.ts`) already passes the real error object to `log.error` — `batch-activity-heatmap.tsx` was the outlier.
- 2026-07-13T00:18Z — Pattern scan turned up a second occurrence of the same defect in `src/contexts/notifications.tsx:126` (passing `error.message, error.code, error.details` as separate primitive args instead of the error object). Included in fix scope.
- 2026-07-13T00:20Z — Fix: pass the real `error` object to `log.error` in both files (minimal diff, matches established convention). Rejected alternative of loosening `client-logger.ts`'s `instanceof Error` check to duck-type error-shaped objects — would mask the actual defect at the call site.
- 2026-07-13T00:35Z — Added two regression tests (`batch-activity-heatmap.test.tsx`, new describe block in `notifications.test.tsx`); confirmed both fail against the pre-fix code (via `git stash`) and pass post-fix.
- 2026-07-13T00:50Z — `/simplify` and `/code-review` skill invocations returned no usable output in this environment; performed both passes manually. Simplify: removed redundant `toBeInstanceOf(Error)` assertions in the new tests (already implied by `toHaveBeenCalledWith` on the exact error reference). Code review: no further findings — diff is minimal, matches conventions, no security/RLS/query-key implications.

## Verification log

- `bun run test src/components/dashboard/__tests__/batch-activity-heatmap.test.tsx src/contexts/__tests__/notifications.test.tsx src/lib/__tests__/client-logger.test.ts` — 19/19 pass
- New tests confirmed to fail on pre-fix code via `git stash` revert — reproduces the defect
- `bun run test` (full suite) — 149 files, 2225/2225 pass
- `bun run typecheck` — clean
- `bun lint` — clean
- `make check` — pending (final gate before PR)

## Outcome

- Ending commit: (pending commit)
- Feature state: passing
- Followups: none — both known occurrences of the defect (heatmap RPC error, notifications fetch error) are fixed and covered by regression tests.
