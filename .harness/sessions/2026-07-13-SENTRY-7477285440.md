# Session: SENTRY-7477285440 — Hydration failed because the server rendered HTML didn't match the client (/dashboard)

- Started: 2026-07-13T00:00:00Z
- Branch: sentry-fix/SENTRY-7477285440
- Starting commit: 5837834018016755bbd21841c069e010934c7fbd2

## Plan
Trace the Sentry stack trace (culprit `/dashboard`, MGR-6) to source, confirm root cause, fix, and add regression coverage.

## Decisions
- Traced the stack trace's tree diff (`AppHeader` → `+div#main-content` / `-Suspense`) to `src/components/domain/shared/chat-layout.tsx` and found it already matched a fix merged in PR #308 (2026-07-05, commit `a7dae58`): `ChatLayout` had already been converted from `next/dynamic({ ssr: false })` to `React.lazy` + explicit `<Suspense>`, with a regression test already in place. Followed AGENTS.md's debugging process (state root cause, alternatives) before concluding the *originally reported* cause was already fixed.
- Noticed the issue's `last seen` (2026-07-11T02:25) postdates the fix and even postdates the release commit (`00fbe79`, 2026-07-10) that already contains it — `git merge-base --is-ancestor` confirmed the fix commit is an ancestor of that release. This meant a second, still-live cause on `/dashboard` was generating new events under the same Sentry grouping.
- Attempted to reproduce live via Playwright (dev server + `/api/auth/dev-login`) per the `e2e/auth.setup.ts`/`e2e/dashboard.spec.ts` fixtures, but the sandbox has no Supabase credentials/DB (only `.env.example`, no `.env`) — live browser reproduction wasn't possible here. Fell back to static analysis of Next.js's `next/dynamic` implementation (read `node_modules/next/dist/shared/lib/lazy-dynamic/{loadable,dynamic-bailout-to-csr}.js` directly) to understand the exact mechanism PR #308 worked around.
- Found `TrendChartLazy` and `BatchActivityHeatmapLazy` (`src/components/dashboard/`) use the identical `next/dynamic({ ssr: false })` pattern, and are rendered from `ProductionTrends`/`SalesTrends` — both of which sit inside a `<Suspense>` boundary that genuinely suspends (`usePeriod()` → `useSearchParams()`, per the existing "wrapped in Suspense for useSearchParams" comment on both dashboard pages). This nests a `dynamic(ssr:false)` client-only-bailout Suspense inside another real-suspending Suspense — structurally the same class of bug already fixed for ChatLayout, and uniquely tied to `/dashboard` + `/dashboard/sales` (matching the reported culprit, unlike ChatLayout which renders on every page).
- Checked `cogs-period-chart-lazy.tsx` and `batch-readings-chart-lazy.tsx` (same `dynamic(ssr:false)` pattern, different routes) — neither is nested inside a suspending boundary, so left untouched to keep the fix scoped to the actual culprit route and avoid an unjustified broad refactor against the documented `-lazy.tsx` convention (`.claude/agents/ui-systems-expert.md`).
- Converted `TrendChartLazy`/`BatchActivityHeatmapLazy` to `React.lazy` + explicit `<Suspense>` at each of the 4 call sites, matching the already-approved ChatLayout fix pattern exactly (same Skeleton fallback content/height, so no visual change).

## Verification log
- `bun install` + `bunx playwright install chromium` ran at session start (no `node_modules` present).
- `bun run typecheck` — clean.
- `bun run test` — 149 files / 2230 tests passed, including new `src/components/dashboard/__tests__/dashboard-lazy-hydration.test.ts` (confirmed failing on pre-fix source via `git stash`, passing after).
- `bun lint` — clean.
- `/simplify` and `/code-review:code-review` skills errored out in this environment (silent `Execute skill` failure); substituted a manual diff self-review instead (checked for: duplication across the 4 Suspense-wrapping call sites — accepted as consistent with existing minimalism convention; SSR-safety of `TrendChart`/`BatchActivityHeatmap` now that they're no longer force-bailed via `BailoutToCSR` — both already guard their own `isLoading` state the same way as every other section on these pages, and `next-themes`' `useTheme()` already follows the safe SSR-hydration pattern used elsewhere, e.g. `AppHeader`).
- `make check` — pending, run next.

## Outcome
- Ending commit: (pending PR)
- Feature state: passing
- Followups: none identified. Live browser reproduction against `/dashboard` was not possible in this sandbox (no Supabase credentials) — if this recurs after this fix, the next investigation should start with an authenticated Playwright run capturing browser console output directly.
