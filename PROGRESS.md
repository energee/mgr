# Progress

> **Single source of truth** for what's done, what's in flight, and what's next.
> Read at session start. Update at session end.

## Current state

- **2026-07-08 (Square bin-sync — Milestone C COMPLETE; C3/C6/C7 shipped; ALL Phase-2 code moved to NEW [PR #361](https://github.com/energee/mgr/pull/361); D deferred by user).** Resumed at C-remainder. C1–C5 were already live; this session added the additive C3/C6/C7 and stopped before D (user decision — D is the money/inventory path and D4 needs new modeling).
  - **⚠️ PR reconciliation — #360 was DOCS-ONLY and is MERGED.** PR #360's single squash commit on `main` (`9a2409a5`) is the Phase-1 **plan/spikes doc** — it carried **no code**. All Phase-2 code (A0 `00219`, A `00220`, B `00221`, C plumbing+C5 `00222`, C3/C6/C7) was committed to `feat/square-pos-bin-sync` **after** #360 merged, so it was never in an open PR and **is not on `main`** (16 commits ahead). The migrations were applied to the **live DB** directly → main↔live drift. **Fixed this session:** re-pushed the branch (remote had been deleted post-merge) and opened **new PR #361** (`feat/square-pos-bin-sync` → `main`) carrying all of A0/A/B/C. Prior PROGRESS entries saying "live on PR #360 / committed to PR #360" meant "on the branch behind #360" — the authoritative code PR is now **#361**.
  - C-remainder committed as `86d44b87`.
  - **C3 — Square locations pull.** `listSquareLocations(client)` in `src/integrations/square/client.ts` (Square `locations.list`; v44 SDK body-direct — `await client.locations.list()` resolves to `{ errors?, locations? }`; skips id-less entries since `id` is the PK; preserves null `name`/`status`). Route `POST /api/square/locations/refresh` (`withPermission("integrations:manage")`) upserts into `square_locations` onConflict `square_location_id` with a `synced_at` stamp — **upsert-only** (locations removed in Square are left in place; a stale row is harmless). 3 tests exercise the REAL normalization through a mocked SquareClient (`importActual` keeps `listSquareLocations`, mocks only `getSquareClient`).
  - **C6 — bin POS pickers + settings surface.** Bin entity (`src/entities/bin/{core,presentation}.tsx`): new "Point of Sale (Square)" section — `square_location_id` picker (dynamicOptions from `square_locations`; no entity to link, so detail shows raw id) + `pos_sales_channel_id` (relation→`sales_channel` + dynamicOptions from active `sales_channels`); both zod fields optional (`.nullable().optional()`). Settings → Integrations → Square card: a **Refresh Locations** button (POSTs the C3 route, toasts the count, invalidates `squareKeys.syncStatus()`) + a **POS Bins** list (bin → Square-location name · channel name). Data comes from an extended `GET /api/square/sync/status` (added a `posBins` field; names mapped separately — `square_locations` has no FK from `bins`, so no embed).
  - **C7 — no location dimension.** Documented (in `catalog.ts` `pushCatalog` header + plan doc): `square_catalog_map` is keyed by `(brand_id, selling_format_id, object_type)` with NO `square_location_id`; v1 shares one catalog object per variation across all Square locations (single price via `resolveChannelPrices`). Per-location price overrides (future) would add a location dimension here. No schema change.
  - **Gates:** `bun lint` + `typecheck` + **test 2044** all green (added 3 C3 tests; C6/C7 need no new tests — entity config + a doc note). No migration this session (all additive code); drift snapshot untouched.
  - **⚠️ D-relevant finding (webhook already degraded by 00222):** `handlePaymentCompleted` (`src/app/api/square/webhook/route.ts:268`) still resolves the sale via `locations.square_location_id` — a column 00222 **DROPPED**. It compiles (loose supabase filter typing) but at runtime that query errors → `locationId` is null → **every draft-sale line now fails** the "not mapped to an MGR location" guard. So **D1 (resolve Square location → *bin* via `bins.square_location_id`) is a bug-fix, not just an enhancement.**
  - **⚠️ D4 needs new modeling:** there is NO "remaining oz in a tapped keg" concept — `keg_filled_contents.quantity` is a count of whole filled kegs, not per-keg remaining volume. D4 (keg pours deplete a tapped keg + flip to empty at zero) is a mini-milestone needing its own migration. D1–D3 (packaged debit) need **no** new schema — `bin_inventory` exists; the sale decrements it (clamp-to-zero + flag per locked D3).
  - **NEXT — Milestone D (deferred):** D1 resolve Square location→POS bin + FIFO-pick FG within that bin (over `bin_inventory ⋈ fg`, replacing the global `finished_goods_with_availability` lookup) — also fixes the dropped-column bug above; D2 explicitly decrement `bin_inventory` for the resolved bin (allocation stays as the audit/TTB ledger — no double-count, only one path writes `bin_inventory`); D3 clamp-to-zero + flag on oversell (locked); D4 keg-pour depletion (new "remaining oz" model + migration — mini-milestone); D5 staged draft sales stay as audit (out of scope v1). Preserve the existing signature-verify + replay-window + `event_id` dedup. Owner: integrations-expert + data-layer-expert.
- **2026-07-08 (Square bin-sync — Milestone C1–C5 SHIPPED + reviewed; committed to PR #360).** Resumed at Milestone C. A prior partial session had already implemented C1–C5 but left it **uncommitted, unverified, and mid-write** (this session's initial reads were stale — the writer was still finishing). This session verified against live, got an integrations-expert review, fixed the findings, and committed it green.
  - **C1 `00222_square_bin_pos_config.sql`** — applied to live + **verified read-only against live**: `bins.square_location_id` (text, null) + `bins.pos_sales_channel_id` (uuid → `sales_channels`, ON DELETE SET NULL, null); partial-unique `bins_unique_square_location` + `idx_bins_pos_sales_channel_id`; new `square_locations` (PK `square_location_id`; RLS **staff-READ / `integrations:manage`-WRITE**); data-migrated `locations.(square_location_id, pos_bin_id)` → the referenced bin (taproom channel) then **DROPPED** those two `locations` columns + the `locations_with_pos` view. Data migration was a **no-op on live** (0 pre-existing location POS configs). Fixed a self-contradictory RLS header comment (claimed admin-read; actual + intended = staff-read). **No fix-forward migration needed.**
  - **C2** types regenerated. **C4** `resolveTaproomPrices` → `resolveChannelPrices(brandIds, salesChannelId)` (dropped the hardcoded `code='taproom'` lookup); deleted the intermediate `resolveTaproom*` scaffolding + its tests once C5 stopped using them. **C5** both sync routes rewritten to the **POS-on-bin** model: iterate `bins` with BOTH `square_location_id` + `pos_sales_channel_id` set; read stock from **`sellable_inventory`** (unified packaged + keg, single query). Catalog prices each variation via its bin's channel (one `resolveChannelPrices` call per DISTINCT channel; deterministic `.order("id")` earliest-bin tie-break; v1 single-price). Inventory converts packaged cases × `unit_count` and pushes kegs **as-is** (the view's `quantity` is raw), scoped to `bin.square_location_id`, one `square_sync_log` per bin (`location_id` = the view's location uuid). Location entity POS field-section + `locations_with_pos` viewTable removed.
  - **integrations-expert review → 4 findings fixed before commit:** **F1 (HIGH)** guarded `deleteStaleItems` against an empty active-brand set — previously a transient empty POS bin (configured but zero stock) would `deleteStaleItems([])`, which treats `[]` as "delete the ENTIRE catalog" and **wipes all of Square**; now skipped. **F2** stable `.order("id")` on the bins query (the multi-channel price tie-break was non-deterministic — docs/tests claimed a determinism the code didn't provide). **F3** flag variations whose channel has no price row (would push $0 to Square) into the log `details.unpricedVariations` + response. **F4** persist per-bin "no catalog mapping" errors to `square_sync_log` (were reaching only the HTTP response when other bins succeeded). Also removed dead `sales_channels` test-mock scaffolding.
  - **Characterization tests:** `pricing.test.ts` (channel parameterization + taproom parity by exact cents) + `sync-routes.test.ts` (channel tie-break, packaged-vs-keg conversion, empty-bin no-wipe, unpriced-flag, durable per-bin error log). **Gates:** `bun lint` + `typecheck` + **test 2041** green; drift snapshot matches live (`00222` base-table changes captured: +`TABLE|square_locations`).
  - **Harness:** added a personal `PostToolUse` git-commit nudge hook (`.claude/settings.local.json`, gitignored) that reminds to update this file after commits.
  - **NEXT (C remainder → D):** C3 `listSquareLocations()` (Square `locations.list`) + `POST /api/square/locations/refresh` upserting `square_locations`; C6 bin-entity `square_location_id`/`pos_sales_channel_id` pickers + settings "Refresh Square locations" action + per-bin POS surface; C7 document `square_catalog_map` (no location dimension for v1); then **D** (webhook: resolve Square location→bin, debit `bin_inventory` clamp-to-zero+flag per D3, keg pours per D4).
- **2026-07-08 (Square bin-sync Phase 2 STARTED — #344 MERGED; Milestone A0 SHIPPED to live).** Decisions confirmed with user: #344 merge = go; **D3 oversell = clamp-to-zero + flag**; A6 = no backfill.
  - **PR #344 MERGED to main** (squash `c3dba263`). Resolved the 2 non-code conflicts (took main's `live-catalog.snapshot.txt` + union-merged the backlog doc), pushed `fix-audit-p2-integrity`, squash-merged. `00207` (`keg_filled_contents` + netted `keg_inventory`) now on main + merged into `feat/square-pos-bin-sync`.
  - **Milestone A0 (`00219_bin_placement.sql`) — applied live + self-verified.** Adds `packaging_sessions.default_bin_id` + `bins.is_default_fg` (partial-unique per location); `AFTER INSERT ON finished_goods` trigger `place_finished_good_in_bin()` (SECURITY DEFINER; keg-skip via `containers.type='keg'`; resolution: session `default_bin_id` → **primary-location** `is_default_fg` bin → `NOTICE`+skip, **never RAISE/blocks the brewer**; `ON CONFLICT DO NOTHING`); `revise_packaging_session` mirrors FG-qty deltas onto `bin_inventory` (reproduced byte-identical from `00217` + one `UPDATE bin_inventory` line). **Schema reality that shaped the design:** `finished_goods`/`packaging_sessions`/`session_line_items` carry **no location**, so the fallback keys off the primary location, not the FG's (plan assumed a session location that doesn't exist). In-migration self-rolling-back DO block passed live (placement / keg-skip / fallback / never-block, rows rolled back). Entity UI: bin `is_default_fg` switch + packaging-session `default_bin_id` picker. Types regenerated; `bun lint` + `typecheck` + **2030 tests** all green. **Drift snapshot regenerated** (via Supabase MCP catalog dump — `psql` unavailable locally; md5-verified **byte-identical** to live `0278bf2c…`; this also captured the pre-existing `00209–00218` snapshot lag, so CI drift should go green).
  - **Migration-history reconcile:** remote `00210` (`vessel_transfer_integrity`, PR #348 — applied live out-of-band, no local file) blocked `db push --include-all`. Reconciled with **`supabase migration repair --status reverted 00210`** (user-approved option). #348 must re-apply `00210` idempotently when it merges.
  - **A0 committed + pushed** (`f8a80c26`, PR #360).
  - **Milestone A SHIPPED to live** (`00220_keg_bin_tracking.sql`): `from_bin_id`/`to_bin_id` on `keg_transactions` (FK→bins, ON DELETE SET NULL, nullable = off-premise); `record_keg_transaction` gained `p_from_bin_id`/`p_to_bin_id` (appended — the old 14-arg overload was DROPped so exactly one function remains; the 16-arg version covers all prior callers via DEFAULT NULL bins); `keg_inventory` / `keg_inventory_with_details` / `keg_filled_contents` gained a `bin_id` dimension parallel to location (md5 id key extended; netting preserved; `keg_inventory_summary` unchanged, sums across bins). Keg-transaction entity UI: From/To Bin selectors. A6 = no backfill. **Netting proven on live** via rolling-back check (receive 50 → fill 10 → ship 10 = 50 total; 40 empty in bin X, 10 shipped off-premise/no-bin, filled@X nets to 0; `keg_filled_contents` carries bin X). The in-migration verify block was fixed to add a `customer_id` on its ship leg (`valid_ship_transaction` CHECK). Types + drift snapshot regenerated — the canonical `check-live-drift.sh` now runs locally (`.env.local` DB URL fixed: it was unquoted with a `#` in the password → quoted + %-encoded; `libpq` installed); snapshot green at 314 objects. `bun lint` + `typecheck` + 2030 tests green.
  - **Milestone B SHIPPED to live** (`00221_sellable_inventory_view.sql`): `CREATE VIEW sellable_inventory` = packaged FGs in bins (`bin_inventory` JOIN fg JOIN selling_formats JOIN containers WHERE `type <> 'keg'` AND qty>0, source='packaged') `UNION ALL` `keg_filled_contents` (source='keg'); shape (bin_id, location_id, brand_id, selling_format_id, finished_good_id, quantity, source); `security_invoker`. The `<> 'keg'` filter is the whole double-count guard (kegs are never in `bin_inventory`). **Verified on live** via the migration's self-rolling-back block (packaged qty 25 + keg qty 7 both surface; zero finished_goods double-counted across sources). Types regenerated; drift snapshot unchanged (views aren't captured); typecheck + lint green. Authored inline (simple view — not a full expert dispatch, a cost call). B3 query-key/hook deferred (C consumes the view server-side).
  - **Milestone C PLUMBING SHIPPED to live** (`00222_square_bin_pos_config.sql` + code): **C1** — `bins.square_location_id` (partial-UNIQUE) + `bins.pos_sales_channel_id` (→ sales_channels); new `square_locations` table (staff read, `integrations:manage` write); data-migrated existing `locations.(square_location_id, pos_bin_id)` → the bin (channel = taproom) + seeded `square_locations`; **DROPped** `locations.square_location_id`/`pos_bin_id` + the `locations_with_pos` view. **C4** — `resolveChannelPrices(brandIds, salesChannelId)` (drops the hardcoded taproom lookup) + parity test. **C5** — both `sync/catalog` + `sync/inventory` routes rewritten to iterate **POS-configured bins** (both cols set) reading the unified **`sellable_inventory`** view, price via each bin's channel (distinct-channel resolution; per-location price override deferred), push to `bin.square_location_id`; packaged×`unit_count` vs keg-as-is preserved; new `sync-routes.test.ts`. **Cascade fix:** `location` entity dropped its POS section + `locations_with_pos` viewTable. Verified live (C1 self-verify block passed); `bun lint`+`typecheck`+**2044 tests** green; types + drift snapshot regenerated (315 objects). ⚠️ Destructive migration is live — deployed `main`'s old sync routes error until this branch deploys (manual+admin sync, low risk).
  - **NEXT (C remainder, additive):** **C3** `listSquareLocations()` + `POST /api/square/locations/refresh` (populate `square_locations` from Square). **C6** bin entity Square-location + `pos_sales_channel_id` pickers + settings "Refresh Square locations" action + per-bin POS surface. **C7** document `square_catalog_map` needs no location dim in v1. Then **D** (webhook bin debit per D0/D3). #348 independent.
  - **SESSION 2026-07-08** continued through **Milestone C plumbing** (C1/C4/C5 above); A0/A/B + C1/C4/C5 all live on PR #360. **Resume at C3/C6** (additive: locations-pull route + bin/settings UI) then D — the plan doc's `## Milestone-C continuation prompt` still applies (skip its already-done C1/C4/C5). Tooling now working locally: `supabase db push` (CLI creds), Supabase MCP for rolling-back live verification, and the canonical `check-live-drift.sh` (needs `PATH=/opt/homebrew/opt/libpq/bin:$PATH ./node_modules/.bin/dotenv -e .env.local -- bash scripts/check-live-drift.sh [--update]`). Working read-only DB URL lives in gitignored `.env.local` (quoted + %-encoded — password contains `#`).
  - **⚠️ SECURITY FOLLOW-UP:** `.env` was tracked in git and is now **untracked** (`903acaff`, ignored via the existing `.env*` rule) — but its secrets remain in **git history**. **Rotate** `SUPABASE_SERVICE_ROLE_KEY`, `QBO_CLIENT_SECRET`, `RESEND_API_KEY`, `MONGODB_URI` (+ review the rest of `.env`); optionally scrub history (`git filter-repo`/BFG) **after** rotating. Not yet done — owner action.
- **2026-07-08 (Square bin-sync — spikes B0/D0 DONE; Milestone A0 added; #344 authorship resolved).** **[PR #360](https://github.com/energee/mgr/pull/360)** opened (docs/plan + spikes; **no code yet — awaiting approval before Phase 2**). Both read-only spikes resolved via code trace **+ live DB** (`phwjrfdtebftetctkhdr`):
  - **B0 — no double-count, deeper reason than expected.** `create_finished_goods_from_packaging` (00183) creates a `finished_goods` row (keg `selling_format`) **+** a `fill` keg_transaction referencing it for keg lines; `keg_filled_contents` (00207) nets those. Filling a keg **never touches `bin_inventory`** — in fact **nothing writes `bin_inventory` anywhere** (no migration INSERT/UPDATE, no dynamic SQL, no app write, no seed; only a version-bump trigger). **Live: `bin_inventory` = 0 rows / 0 qty** (fg_total=1, keg_txn=1). Union rule → `sellable_inventory = bin_inventory (WHERE container.type<>'keg') ⊎ keg_filled_contents`; the `<>'keg'` filter is the entire double-count guard.
  - **D0 — independent ledgers.** `finished_goods_with_availability.available = fg.quantity − Σallocations` (00191) — never reads `bin_inventory`. The webhook's `taproom_sale` allocation lowers *logical* availability but leaves the *physical* `bin_inventory` count (what the outbound sync pushes) untouched → split-brain. **D2 rule:** the sale must **explicitly decrement `bin_inventory`** for the resolved bin (plan option a); the allocation stays as the audit/TTB ledger. No double-count since only that one path writes `bin_inventory`.
  - **NEW Milestone A0 (prerequisite, before B) — populate `bin_inventory`.** Because no writer exists (and live is empty), the bin-driven sync has no data. Design: an `AFTER INSERT ON finished_goods` **trigger** that places non-keg FGs into a bin **in the same transaction** (atomic → cannot orphan; covers packaging *and* `revise_packaging_session` paths uniformly). Resolution order: `session_line_items.target_bin_id` (future per-line override) → `packaging_sessions.default_bin_id` (**new col**) → location's `bins.is_default_fg` (**new col**, partial-unique per location) → else `RAISE`. Reversible via existing `bin_inventory … ON DELETE CASCADE` (full teardown) + a one-line qty mirror in `revise_packaging_session` (partial). **Decided w/ user:** on a missing session bin, **fall back to the location-default bin (never hard-block the brewer)**; **session-level grain** (per-line override deferred).
  - **PR #344 authorship RESOLVED.** Commit `5cf07031` authored by `claude[bot]` (prior session/CI) + co-author Ted — **benign, snapshot-only** (+312 lines `live-catalog.snapshot.txt`, no code). #344 conflicts vs main = **only 2 non-code files** (backlog doc content + snapshot add/add); `00207` and all code files merge clean. Resolution = take main's snapshot + merged backlog doc.
  - **Open decisions before Phase-2 code:** (1) **PR #344 merge** go/no-go (trivial 2-file resolve, then merge to main → unblocks Milestone A); (2) **D3 oversell** policy (rec: clamp-to-zero + flag, mirrors #357). **A6 = NULL / no backfill** (plan default taken; 1 keg_transaction live → nil impact). Proposed order: **#344 → A0 → A → B → C → D**; C4 (pricing generalization) preppable in parallel; #348 independent. **Phase-2 execution continuation prompt ready** in the plan doc (`## Phase-2 continuation prompt`). **Migration renumber (A0 inserted):** A0=`00219`, A=`00220`, B=`00221`, C=`00222` (00207 fills a gap, top stays 00218 → next-free 00219).
- **NEXT — Square POS bin-sync feature (Phase-1 plan ready, awaiting approval).** Branch **`feat/square-pos-bin-sync`**, plan **[`docs/plans/2026-07-07-square-pos-bin-sync.md`](docs/plans/2026-07-07-square-pos-bin-sync.md)** (Milestones A–D + a paste-in continuation prompt). Config moves to the **bin** (bin↔Square-location 1:1, selectable sales channel, no hardcoded taproom, single-price-now); keg fleet gains a **bin dimension** (empties included); a **`sellable_inventory`** view unifies packaged FG + filled-keg contents; **Milestone D** debits bins from inbound Square sales. **Spikes B0+D0 DONE (2026-07-08 — see next bullet); new Milestone A0 added (populate `bin_inventory`, which has no writer today).** **Hard dep: PR #344 must merge first** (creates `keg_filled_contents` / rewrites `keg_inventory`, which Milestone A extends).
- **2026-07-07 (merge & reconcile — DONE).** Audit migration batch **merged to main**: #345, #346, #347, #349, #350, #352, #353, #354, #355, #356, #357, #358, #359 squash-merged (#351 docs already merged). The recurring `docs/plans/2026-07-06-audit-fix-backlog.md` conflict was resolved by **union-of-ticks** (git `merge=union` driver). **DB drift verified expected**: live catalog vs the `00205` snapshot showed **only** the `00206`–`00218` additions/body-changes (6 new funcs + 8 new triggers + 3 rewrites) and **zero out-of-band drops**; `get_price_for_customer` md5 was byte-identical, confirming #354's no-op capture. Two review-found bugs fixed en route: **#344** — Square catalog embed went through the aggregate `keg_filled_contents` view (PostgREST can't resolve it → filled kegs silently vanish); now reads `brand_id` directly + a separate `brands` query. **#355** — batch-loss reconciliation idempotence re-keyed onto the `idempotency_key` system column, not user-editable `reason_code` (+1 test). **Still open:** **#344** (open — carries an unattributed `5cf07031 "add corrected live-catalog snapshot…"` commit that appears to do the snapshot reconcile; confirm authorship, then merge — it's also the Square feature's hard dep), and **#348** (held; decision = **free the source vessel on full-remainder loss**, not yet built).
- **2026-07-07 (audit backlog remainder — session C, worktree `p2-integrity-2`)**: cleared the **entire remaining 2026-07-06 backlog** — all of item **#18** plus M9-copy — in **4 more focused PRs (NOT merged); migrations `00216`–`00218` applied live + verified**. Nothing left in the backlog except the explicit STOP/product items (#10, #17, #19, #20) and M9's invite-gate (folded into #20). Each PR: `bun run lint` + `typecheck` + `test` green; forked decisions taken with the user.
  - **[#356](https://github.com/energee/mgr/pull/356)** — **M9 (#14a)** customer-form pricing copy: "default tier"/"override" copy was false (no per-channel default; `get_price_for_customer` needs BOTH tier + channel). App-only. **Invite-gate DEFERRED to #20** (portal rebuild, on hold) — user decision.
  - **[#357](https://github.com/energee/mgr/pull/357)** — **M14** (`00216`): BEFORE UPDATE trigger `guard_finished_good_outbound` blocks reducing `finished_goods.quantity` below active allocations. **Block-hard** (mirrors #12/00212; corrections go through an adjustment allocation, per the ledger model). Rollback-tested live.
  - **[#358](https://github.com/energee/mgr/pull/358)** — **M7/M8** (`00217`): whole-unit material ceiling made **per-batch** (decided). Revise RPC now ceils via a new `whole_unit_material_qty` helper (delta-of-ceils, not ceil-of-delta); preview hook ceils per-batch to match completion. TS↔SQL parity test. Only the RPC's material section changes (rest reproduced from 00184).
  - **[#359](https://github.com/energee/mgr/pull/359)** — **M1/M2** (`00218`): `recipes_with_estimates` IBU switched to gravity-adjusted **Tinseth** (decided canonical), via a new `hop_utilization_factor` helper fed the sidebar's preliminary OG; fixes the stepped-lookup + ~2.3× first-wort gap. Verified view↔TS parity on 5 live recipes; parity test added. Only the `hop_ibu` CTE changes (rest reproduced from 00191).
  - **Forks resolved (user):** M8 → per-batch · M1/M2 → Tinseth (port TS→SQL) · M9 invite-gate → defer to #20. M14 → block-hard (defaulted, mirrors #12 + ledger model).
  - **Split-brain note:** M7 and M1/M2 both stemmed from TS-formula-vs-SQL-formula drift; both now isolate the shared math in an IMMUTABLE SQL helper (`whole_unit_material_qty`, `hop_utilization_factor`) with a TS↔SQL parity test guarding future drift.
  - **Migrations/scaffolding:** live now at **`00218`**; **next = `00219`**. `00206`–`00218` carried as untracked scaffolding on each fresh branch off main so `db push --include-all` history-matches live. **Drift baseline still transiently red** (main's snapshot at `00205`; live has `00206`–`00218`) — reconciles in one pass when the migration batch merges; migration PRs deliberately carry no snapshot update.
- **2026-07-07 (data-layer P2 integrity — continuation, worktree `p2-integrity-2`)**: **#343 now MERGED to `origin/main` (`75301441`)** — the live-drift CI + `00205` + `live-catalog.snapshot.txt` are on main. Continued the 2026-07-06 audit backlog with **4 more focused PRs (NOT merged); migrations `00213`–`00215` applied live + verified**. All data-layer items in the backlog's "remaining" set are now done; the rest are other-lane (flagged below). `bun run test` green throughout; lint + typecheck clean per PR.
  - **[#352](https://github.com/energee/mgr/pull/352)** — **M15a** count-increase lost-update race: `recordInventoryCount` compare-and-swaps on the pre-image `inventory_lots.quantity` (that table has **no `version` column**) → returns `CONFLICT` on a stale write. App-only, no migration. +1 test (suite 2026).
  - **[#353](https://github.com/energee/mgr/pull/353)** — **#15** broad-ledger extension (`00213`): `entity_revisions` triggers on `inventory_lots` / `packaging_sessions` / `pricing_tier_prices` / `keg_transactions` via the shared `log_entity_revision()`; rollback-tested (inventory_lots update → exactly 1 revision).
  - **[#354](https://github.com/energee/mgr/pull/354)** — **#14 (b)+(c)** (`00214`): captures the out-of-band live `get_price_for_customer` into the chain **byte-identical** (prosrc md5 unchanged before/after push → true no-op), closing migration-reconciliation drift item #4; documents the dual pricing model + "no default tier per channel" in `docs/knowledge/entity-model.md`.
  - **[#355](https://github.com/energee/mgr/pull/355)** — **#15** mutable-notes idempotence (`00215`): packaging depletion now keys on a dedicated `allocations.idempotency_key` (`pkg_session:<id>` + partial index); batch-loss reconciliation keys on the structured `reason_code='reconciliation'`. Neither keys on the user-editable `notes` anymore. 0-row backfill (verified); rollback-tested.
  - **Design decisions:** M15 → quantity-CAS (no `version` column exists; ABA ceiling noted in a `ponytail:` comment). #15 idempotency_key → plain partial index, **NOT** unique (depletion writes many rows per key). #14a M9 (customer-form copy + invite-gate) deferred to entity-architect.
  - **Drift baseline:** main's daily live-drift watchdog is **transiently red** — live has `00206`–`00215` objects but main's snapshot is at `00205` (only #343 merged). Migration PRs carry **no snapshot update** (same as siblings #344–#350); the snapshot reconciles in one pass when the batch merges. No new drift introduced (each DDL rollback-tested against live before push).
  - **Remaining (flagged, other-lane — not auto-continued):** **M9** (#14a, entity-architect: fix form copy + decide invite-gate policy), **M14** (FG-edit outbound guard — clean DB-trigger fix but needs the negative-correction policy call, same as #12), **M7/M8** + **M1/M2** (brewing-domain-expert). **Next migration = `00216`**; M14/M7 are migration-bearing → keep serialized.
  - **Ops:** pruned **25 stale `fix-pr-*` worktrees** (durable E2BIG fix; ~34 worktrees still remain → ran git/supabase/bun with sandbox bypass). `00213`–`00215` carried as untracked scaffolding on each fresh branch off main so `db push --include-all` history-matches live.
- **2026-07-07 (data-layer P2 integrity session)**: worktree `.claude/worktrees/p2-integrity`, branches off `origin/main` `9b4f2fec`. **Seven focused PRs opened (NOT merged), migrations `00206`–`00212` applied live and verified**, following the 2026-07-06 audit backlog (`docs/plans/2026-07-06-audit-fix-backlog.md`). #343 (item #9, `00205` + live-drift CI) was still **open/unmerged** at session start, so each branch carries `00205`–`0021x` as untracked local files to history-match live for `db push`; PROGRESS + the live-catalog snapshot stay #343's until it merges.
  - **[#345](https://github.com/energee/mgr/pull/345)** — #13 `chk_fg_entry_point` relaxed (H8, `00206`)
  - **[#344](https://github.com/energee/mgr/pull/344)** — #11 `keg_inventory` netting (H4, `00207`) — regroup on format/owner/state/location; new `keg_filled_contents` for Square; netting proven 50→70→**50**
  - **[#346](https://github.com/energee/mgr/pull/346)** — #21 `get_inventory_overview` rewrite off dropped `package_types`/`catalog_type`/`bin_inventory` (`00208`); invoked live, returns
  - **[#347](https://github.com/energee/mgr/pull/347)** — #22 `start_batch_fermentation` recreated off dropped `batches.fermenter` → `vessels.current_batch_id` (`00209`)
  - **[#348](https://github.com/energee/mgr/pull/348)** — #16 vessel integrity M3/M4/M5 (`00210`): idempotency_key + partial unique index, double-booking guard, `empties_source`; guards proven via rolled-back tests
  - **[#349](https://github.com/energee/mgr/pull/349)** — #15 allocation ledger hardening M11/M13 (`00211`): entity_revisions trigger, `created_by` default, block completed-deletes, adjustment `reason_code` CHECK
  - **[#350](https://github.com/energee/mgr/pull/350)** — #12 availability guard H7 (`00212`): BEFORE INSERT/UPDATE, `FOR UPDATE` row lock, exempts `destination_type='adjustment'`; oversell blocked / exact allowed / adjustment exempt proven
  - **Design decisions (user-chosen this session):** #16 → client idempotency key + `empties_source` column; #12 → block oversell, exempt count-corrections; #15 → allocations-focused scope.
  - **Deliberately NOT done (product-decision / other-agent — flagged, not auto-continued):** #10 (change-request rebuild), #17 (order-item price validation, entity-architect), #19 (planned-batch ordering), #20 (portal rebuild). Also `bun run test` = **2025** green throughout; lint + typecheck clean per PR.
  - **Ops caveat:** ~56 git worktrees now trip sandbox `E2BIG` on plain commands — ran git/supabase/bun with sandbox bypass all session; durable fix is pruning stale `fix-pr-*` worktrees (memory: `project_sandbox_e2big_worktrees`).
- **2026-07-07 (this session)**: branch `fix-audit-p2-enforcement` off `origin/main` `9b4f2fec` (post-#341). **[PR #343](https://github.com/energee/mgr/pull/343)** open (not merged) — audit backlog **#9 (C2) server-side enforcement restore** + **live-drift CI**. Migration `00205` applied live and verified; `db push` a clean no-op. Since the snapshot below was written, **#341 (P0, 00201) and #342 (P1, 00202–00204) already merged** and are live. Lint + typecheck clean, suite **2025 tests** green. **Drift CI verified working 2026-07-07** — `SUPABASE_DB_URL` secret configured, workflow run green (`OK: live database catalog matches supabase/live-catalog.snapshot.txt`). Remaining user action: flip Supabase "Allow new signups" OFF. Next migration number = **00206** (00205 is claimed by open PR #343).
- **Reflects**: `feat/integrations-expert-agent` rebased onto `origin/main` `ea713bb9` (the #336 squash-merge) — 2026-07-05 (second session). **PR #336 merged mid-session** with only its original commit; the follow-up commits live in **[PR #338](https://github.com/energee/mgr/pull/338)**
- **Last verified**: 2026-07-05 — lint + typecheck clean, suite green at **1,959 tests** (+2: transition call-site enforcement). Migration `00200` applied to live and verified (`is_sensitive_setting` returns true for all six `*_api_key` keys, false for non-secrets)
- **Diverged from `main`**: yes — five follow-up commits in PR #338: `04609164` (routing-gap review fixes), `9dad496b` (remaining 14 review findings), `03fd5aa9` (00200 RLS fix, applied live), `0d6ceb41` (backlog #7), plus this docs refresh
- **Gap notice**: this file went un-updated between 2026-05-12 and 2026-07-05. The "Completed (historical)" and "Deferred" sections below are a May 2026 snapshot — re-verify before acting on them.

## Completed (this branch — `feat/integrations-expert-agent`; #336 merged, follow-ups in PR #338)

- [x] **integrations-expert agent** added (`.claude/agents/integrations-expert.md`); entity-architect + data-layer-expert scopes extended; CLAUDE.md agent table updated (`37539de1`)
- [x] **xhigh 10-angle review of PR #336** — 15 verified findings (14 CONFIRMED / 1 PLAUSIBLE). Top: the doc's claim that `system_settings_hide_sensitive` conceals integration API keys is false (`is_sensitive_setting()` covers only the 3 `qbo_*` token keys — see Known issues); the "wire the 00100 RPCs" guidance would silently break QBO OAuth (they're SECURITY INVOKER and UPDATE-only); `notify_all_users` anchored to the superseded `00090` body (00191 added email dispatch); `(auth)`↔`(app)` staff-auth-surface mix-up
- [x] **Review fixes applied on-branch**: entity API routes (`api/{batches,orders,customers,recipes,users}`) assigned to entity-architect; `api/auth` + `update-password` to data-layer-expert; explicit "no expert owner" table row (`api/{dev,health}`; `chat` deferred to `ai-features-expert`, Phase 4A); services bullet trimmed to invariants + module-header pointer; stale "~30" query-key factory counts corrected (43 actual → "dozens"); backlog fix #7 added (transfer-route side-effects bypass) (`04609164`)
- [x] **Remaining 14 review findings fixed in doc text** (`9dad496b`): integrations-expert — API keys not RLS-hidden (pre-00200), qbo tokens bypass the api-key route, email's `email_settings`/`dispatch_email_notification` dependency, `square_draft_sales` reframed as replay drift (live column dropped), 00100 RPCs must NOT be wired (INVOKER/UPDATE-only), `mapAddress` is tested, `notify_all_users` anchored to 00191, four email templates, broader name-resolution risks; data-layer-expert — staff gate is `(app)/layout.tsx`, portal RLS via `customer_portal_users` junction, `src/proxy.ts` replaces root `middleware.ts`; CLAUDE.md — integrations route dirs named, entity triad convention; all agents — redundant Search tooling section dropped
- [x] **00200_extend_sensitive_settings.sql** (`03fd5aa9`): `is_sensitive_setting()` extended with `%_api_key` LIKE — closes the Known-issues API-key exposure. Applied to live via `db push --include-all` + verified; agent docs updated to post-00200 state
- [x] **Backlog #7 — transfer-route side-effects bypass** (`0d6ceb41`): `POST /api/batches/[id]/transfer` now calls `runTransitionSideEffects` (was silently skipping `completeBatchConsumption` on `packaging → completed`); new `src/services/__tests__/transition-call-sites.test.ts` walks all API routes for future bypasses (red → green; suite 1,957 → 1,959)

## Completed (historical — Sentry fixes + harness rollout, May 2026)

- [x] **SENTRY-7479939863 — Error: No QueryClient set, use QueryClientProvider to set one** (Sentry fix, MGR-8)
  `NotificationsProvider` called `useQueryClient()` unconditionally at render time (line 80 of `src/contexts/notifications.tsx`), which throws immediately if no `QueryClientProvider` ancestor exists. The error was captured from the `polish` git worktree at `GET /` (localhost:3002). The fix adds an outer guard component that reads `QueryClientContext` directly via `useContext` (which returns `undefined` safely instead of throwing when no provider is present). If no QueryClient is found, the guard renders children with a stable no-op `EMPTY_NOTIFICATIONS` context and logs a production error for observability; in the normal path it delegates to the original inner implementation unchanged. Three Vitest tests at `src/contexts/__tests__/notifications.test.tsx` cover the no-QueryClient render, empty context defaults, and no-op action handlers. All 41 test files (1105 tests) pass; typecheck and lint clean.

- [x] **SENTRY-7454377645 — ReferenceError: sgToPlato is not defined** (Sentry fix)
  Sentry issue MGR-3 fired on 2026-05-01 when `RecipeSidebar` rendered the OG/FG estimates with `displayUnit = "plato"`. A local `formatSg` helper had been added inline to `recipe-sidebar.tsx` that called `sgToPlato` directly without importing it, causing a `ReferenceError` at runtime. The structural fix had already landed in commit `73c49c7` (added `formatGravityFromSg` to `@/lib/units` where `sgToPlato` is in scope, then updated `recipe-sidebar.tsx` to import and use it). This session closed the gap by adding six Vitest tests for `formatGravityFromSg` — covering null/undefined guards, SG passthrough, Plato conversion, and custom decimal places — which would have caught the missing-import bug before it reached the development environment. All 1036 tests pass; lint and typecheck clean.

- [x] **SENTRY-7452842898 — MGR-2: ReferenceError: SaveAllButton is not defined** (sentry-fix/SENTRY-7452842898)
  Root cause: During Turbopack Fast Refresh (HMR), module code is re-evaluated top-to-bottom without the JavaScript hoisting guarantee for function declarations. `SaveAllButton`, `MobileEstimatesBar`, and `RecipeEditorSkeleton` were declared after `RecipeEditorPage` in `recipe-editor-page.tsx`, and `EstimateCard`, `SummaryRow`, `formatNum`, `formatGrainWeight`, `formatHopWeight`, and `bagLabel` were declared after `RecipeSidebar` in `recipe-sidebar.tsx`. Moving all helper functions and components before the exported component that uses them eliminates the HMR re-evaluation race. Regression coverage: `src/components/domain/recipe-editor/__tests__/recipe-editor-page-definition-order.test.ts` reads the source files and asserts that each helper is textually defined before the main component.

- [x] **SENTRY-7465830666 — MGR-4: Module not found: react-activity-calendar** (sentry-fix/SENTRY-7465830666)
  Root cause: `react-activity-calendar` was removed from `package.json` by a knip dependency audit in commit `33c0793` (April 1, 2026). The `batch-activity-heatmap.tsx` component was then added in PR #248 (commit `e3c1af3`, May 6, 2026) which correctly re-added the package. The Sentry error (MGR-4) fired the next day when a developer loaded the dashboard without running `bun install` after pulling. The first harness pass (PR #258) closed the Sentry issue and added harness tracking files. This pass adds a Vitest regression test at `src/components/dashboard/__tests__/react-activity-calendar-dep.test.ts` that verifies `ActivityCalendar` is exported as a valid React element type — if the package is removed, the test file fails to load immediately.

- [x] **Audit MGR against walkinglabs harness framework** (12 lectures + resource library) — produced gap checklist
- [x] **Step 1 — Feedback subsystem** (F001)
  - `Makefile` with layered gate (`check-fast` / `check` / `check-all` / `check-db`)
  - `scripts/init.sh` bootstrap with the four-line bootstrap contract
  - Cleanups: cut Makefile sprawl (22 → 17 targets), removed `WARNINGS` counter and `require make` from init.sh, added `OK: check passed` line, merged `init` into `setup`
- [x] **Step 9 — AGENTS.md fixes** (F002)
  - Reframed `AGENTS.md` as 165-line router; deleted `CLAUDE.md`
  - Split content into `docs/agents/{patterns,query-keys,db-security,ui-rules,debugging}.md`
  - Updated cross-references: `.github/scripts/sentry-harness/prompt.{ts,test.ts}`, `README.md`, `src/entities/user-profile.tsx`, `docs/data-model/kegs.md`
- [x] **Step 3 — Scope subsystem** (F003)
  - `docs/feature_list.json` — 44 features (9 harness rollout F001-F009 + 35 backfilled from `docs/plans/` and `docs/superpowers/`)
  - `scripts/verify-feature.sh` reads feature list, runs `verification` command (or reports manual / null)
  - WIP=1 per branch rule documented in `AGENTS.md`
- [x] **Step 4 — Executable DB security checks** (F004) — wired into `make check`
  - `scripts/check-security-invoker.ts` — migration-history-aware
  - `scripts/check-rls.ts`
  - `scripts/check-auth-users-leak.ts` — migration-history-aware
  - All three default to fail-on-violation; whitelist via `-- check-X: skip <reason>` comments
- [x] **Corrective migration `00156_security_invoker_corrections.sql`**
  - 9 `ALTER VIEW … SET (security_invoker = true)` for legacy views from 00004–00011
  - `recent_vessel_cleanings` rebuilt with `user_profiles.email` instead of `auth.users.email`
  - `NOTIFY pgrst, 'reload schema'`
  - **Pending**: `bun run db:migrate` against the live database
- [x] **Skip comments** added to two `notify_all_users` SECURITY DEFINER functions (server-side admin broadcast, IDs only)
- [x] **Step 5 — State subsystem** (F005)
  - `PROGRESS.md` (this file)
  - `DECISIONS.md` (project-level decision log; distinct from schema `docs/spec/decisions.md`)
  - `docs/agents/session-handoff.md` template
- [x] **Step 7 — Cleanup** (F006)
  - `docs/agents/clean-state-checklist.md`
  - `scripts/cleanup-session.sh` (idempotent)
- [x] **Step 8 — Evaluator** (F007)
  - `docs/agents/evaluator-rubric.md` (six dimensions, 0–2 scoring)
- [x] **Step 6 — Observability** (F008)
  - `docs/agents/observability.md` — Sentry / agent traces / quality snapshot
  - `docs/agents/quality.md` — A–D grading template (most domains `_TBD_` pending baseline pass)
  - `.harness/sessions/` directory + worked example `2026-05-02-harness-rollout.md`
- [x] **Sentry verification** (F009)
  - Confirmed `prompt.test.ts` 6/6 pass after CLAUDE.md → AGENTS.md rename
  - Bumped CI workflows from `bun-version: 1.2.9` → `1.3.10` to match `engines.bun: ">=1.3"`
- [x] **Version pins**
  - `package.json` — `engines.node: ">=24"`, `engines.bun: ">=1.3"`, `packageManager: bun@1.3.10`

## In progress

- **[PR #338](https://github.com/energee/mgr/pull/338)** (this branch) — open, ready to merge. PR #336 was squash-merged mid-session with only its original commit; #338 carries all 15 review-finding fixes, the 00200 API-key RLS migration (already applied to live), and backlog #7 (transfer-route side effects + enforcement test).

## Deferred

Open work that's logged but not currently being executed. Pull from this list when picking up new work.

- **`notify_all_users` SECURITY DEFINER refactor** — function still reads from `auth.users` (defined in `00090_slack_integration.sql:117`; callers in `00070`, `00145`, `00148`). Whitelisted via `check-auth-users-leak` skip comments. Should move to `user_profiles`. Low priority.
- **Pick list — no bin-level detail** — `pick_list_items` lacks `bin_id`; UI shows only location name. Legacy `OrderPickList` displays bin + location, which matters for warehouse travel. Needs migration + `generate_pick_list` update + UI change. See `docs/plans/deferred-gaps.md`.
- **Pick list — no location-based sort** — items sorted by `sort_order` (generation order). Should sort `location_name → bin_name → lot_number`. See `docs/plans/deferred-gaps.md`.
- **Hardcoded 7-day PO lead time** — `src/lib/purchasing/po-generator.ts:215` uses `+ 7`; should use `Math.max(...leadTimes)` from shortfall data. See `docs/plans/deferred-gaps.md`.
- **Normalize `containers.volume_oz`** — heuristic disambiguation in `src/hooks/use-catalog.ts:100` (per-unit vs rolled-up case totals). Should be normalized at the schema level so the heuristic can be dropped.
- **Deeper E2E flows** — `test.skip` in `e2e/{batch-transfer, customer-order, dashboard, production-workflow, recipe-editor, packaging-session}.spec.ts` pending seed data. Smoke routes pass; F114/F128/F134/F135/F136 are marked `passing` on that basis.
- **Tighten DB allowlists** — `~30` grandfathered entries across permissive-RLS / SECURITY DEFINER / search_path / data-model docs allowlists. Tighten over time.
- **Raise `src/lib/**` coverage threshold** — currently 50% in `vitest.config.ts`; raise gradually as coverage improves.
- **Harness soak** — promote `Harness` layer grade A− → A after a 2-week multi-session soak proves drift detection works in practice (target: 2026-05-18).

## Recent history

- **2026-07-07** — Audit P0/P1 shipped (#341 P0/00201; #342 P1/00202–00204, both merged + live). **#9 P2 enforcement restore (PR #343, open, 00205 applied live)**: live had lost a cluster of 00075–00150-era objects out-of-band (all 8 state-transition triggers + `validate_state_transition`, pick-list triggers, advisory-lock number generators, `calculate_ingredient_shortfalls` on_order_qty, `get_yeast_lineage_root`, `get_unaccepted_po_receives`) while `db push` stayed a no-op; `get_state_transitions` was the stale 00167 map (no orders picking/packed). Restored + re-synced the map to entities. New live-drift CI: `scripts/check-live-drift.sh` + `supabase/live-catalog.snapshot.txt` (298 objects, body-hash) + `.github/workflows/live-drift.yml` (needs `SUPABASE_DB_URL`). Deferred/filed: backlog #21 (`get_inventory_overview` package_types rewrite), #22 (`start_batch_fermentation` fermenter-col rewrite).
- **2026-07-05** — Big merge day: #331 Sentry-harness restore (automation tiered to Sonnet), #295 user-invite dialog + hardened invite API, #332 ExportMenu wired into five report pages, #322 RLS coverage-gap completion (10/10 tasks), #334 react-harness test migration (−295 LOC), #333 expert-agent team + knowledge base + fix backlog, #335 animated-icon factory (−2,064 LOC), #337 guards for 00197/00198 against out-of-band table drops. #336 (integrations-expert agent) opened + given a 10-angle verified review (15 findings)
- **2026-06-30** — #328 dedup/simplification merged: dead-code removal, engine-file splits, prefill-store zustand→sessionStorage, two orphaned components wired; characterization coverage for recipe displays + schedule editors (#329/#330). Full detail: `docs/plans/2026-06-30-dedup-extraction-backlog.md`
- **2026-05-12** — #254 BOM whole-unit math + intuitive BOM/receive UX
- **2026-05-04..** — Sentry-harness hardening: #257 grants Claude Code the required toolset; #256 streams Claude Code Action output to job log; #255 reads org/project from `vars` to avoid output redaction
- **2026-05-04** — Migration `00156_security_invoker_corrections` applied to production via `bun run db:migrate -- --include-all` (9 legacy views + `recent_vessel_cleanings` rebuild now live)
- **2026-05-04** — `docs/agents/quality.md` baselines filled (no remaining `_TBD_` cells); F200 dashboard activity heatmap marked passing (#252); redundant Vercel deploy workflow removed (#253)
- **2026-05-03** — #251 autoharness screening + Claude OAuth shim; #250 `src/lib` dead-code removal + dedup
- **2026-05-02..03** — Walkinglabs harness rollout (#249, merged): `Makefile` with layered gates, `AGENTS.md` router + topic docs, `feature_list.json` (48 features), 8 executable DB checks, ESLint custom rules (DEC-008 / centralized query keys / `EntityDetail` re-introduction block), WIP=1 enforcement, bootstrap contract validation, Sentry harness writes harness state, coverage thresholds, migration dry-run

## Known issues

- ~~Integration API keys readable by any authenticated user~~ — **FIXED 2026-07-05** by `00200_extend_sensitive_settings.sql` (`is_sensitive_setting()` now also matches `%_api_key`; applied to live and verified). Was: only the 3 `qbo_*` token keys were hidden from the permissive SELECT policy (00097:402-403).
- `make` warnings about `xcrun_db` permission errors only appear inside Claude Code's sandbox; normal terminals don't show them.
- `make check` cannot be exercised end-to-end inside the agent sandbox because Turbopack `next build` requires port binding. Verified piecewise: `make check-fast`, `bun run test`, `make check-db`.

## Next steps

1. **Review + merge PR #343** (audit #9 enforcement restore + live-drift CI); add the read-only `SUPABASE_DB_URL` repo secret so the drift job runs.
2. **Audit fix backlog** (`docs/plans/2026-07-06-audit-fix-backlog.md`): P0 #1–#3 (#341) and P1 #4–#7 (#342) shipped; #9 = PR #343. Remaining data-layer items to pick up next as separate PRs: **#11 keg_inventory netting, #12 server-side availability guard, #13 chk_fg_entry_point, #15 ledger audit hardening, #16 vessel integrity, and the new #21 (get_inventory_overview rewrite) / #22 (start_batch_fermentation rewrite)**. Product-decision/other-agent items (#10, #17, #19, #20) need the user / entity-architect.
3. Migration-replay drift: from-scratch replay still recreates `square_draft_sales.keg_type_id NOT NULL` + its stale UNIQUE index (00091, never dropped) — fold into the next replay-repair pass.
4. Older **Deferred** items below are a May 2026 snapshot — re-verify before pulling.
