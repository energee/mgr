# Quality snapshot

Codebase health at a glance. Grade each domain and architectural layer A–D.
Update weekly, or whenever a major change shifts a grade. Compare against
previous snapshots in `git log -- docs/agents/quality.md`.

This snapshot prioritizes audit areas but is not defect evidence. Follow
[`health-audit-and-issue-triage.md`](health-audit-and-issue-triage.md) when
turning a health review into confirmed findings or GitHub issues.

## Grades

- **A** — solid; would not be the bottleneck if a competent agent took over today.
- **B** — works; a minor refactor would clean it up.
- **C** — surfaces issues regularly (bugs, agent confusion, slow iteration). Plan a focused session.
- **D** — actively painful; consider a corrective rewrite.

## Domains

| Domain | Grade | As of | Notes |
|---|---|---|---|
| production | A− | 2026-08-03 | 12 entities (batch, brand, brew-log, beer-style, packaging-session, recipe, session-line-item, vessel, vessel-transfer, yeast-pitch, yeast-pitch-event, yeast-strain). Most active domain — F134 (recipe editor fixes, #246), F136 (packaging redesign), F112 (recipe/batch/brew-log cohesion) all recent. Universal entity pattern carries it. Grade held (rule 2): the TTB-cellar corner flagged last week did resurface — #670 (Totals column mixing cellar/keg volume, opened 07-30) was fixed same day by 1d987a2, but #697 (migration 00139's projection RPCs not live) and #698 (Total column still doesn't cross-foot) opened 07-30 on the same module, and the original #618 (filed 07-25) remains open — now a 3-week-old thread. Separately, legacy keg-type debt cleared this week: #712/#701 and #723 retired the `keg_transactions.keg_type_id`/`keg_owner_deposits.keg_type_id` NOT NULL constraints so keg packaging completes. All of this is ≤4 days old, too fresh to move (rule 2); escalate past "watch next week" if #697/#698 are still open at the next regrade. |
| inventory  | B  | 2026-05-04 | 12 entities (largest surface). F137 unified material planning landed in commit a2704f7 — load-bearing. Allocations pattern is robust. Some legacy: keg-inventory and keg-transactions migrations (00029–00032) are in the permissive-RLS allowlist. |
| sales      | B  | 2026-07-27 | 7 entities + customer portal (F114). Single-tenant permissive RLS on order_materials family (00162) — documented but coarse. Pricing tiers (F110, F117) and selling formats (F124, F130) work. Grade held (rule 2): #620 (07-26) fixed customer portal revocation not being durable (#605) — a real correctness gap, but the fix is 1 day old; revisit if a similar portal-auth issue resurfaces. |
| purchasing | B− | 2026-05-04 | 4 entities (purchase-order, po-line-item, po-receive, supplier). Smaller surface, demand-planning view (F105) is shared with inventory. F132 (COGS projections) recent. Few dedicated tests. |
| catalog    | B+ | 2026-05-04 | Brands / styles / yeasts / water-profiles / containers / selling-formats. Mostly stable; brands+styles design (F104) shipped early. Schema is wide and self-documenting via `_schema_registry`. |
| packaging  | A− | 2026-07-27 | F136 redesign (PackagingDayView, in-progress flow) shipped in commit 196cb56-era work. Custom status-based detail view pattern is one of the cleanest in the repo. New E2E smoke spec covers it. Grade held (rule 2): #621 (07-26) fixed an inexact BOM ratio at packaging completion and unfroze the summary view; #567 (07-24) fixed a live drop of legacy packaging-format xor constraints. Both real bugs, both under 3 days old. |
| auth       | B− | 2026-08-10 | F116 (permission-based roles), `user_profiles` mirrored from `auth.users` via 00036 trigger. `is_admin_rls` and similar SECURITY DEFINER helpers grandfathered. F131 login redesign recent. RLS now checked executably (#617, 07-26). **Moved B → B−**: the dev-login uncredentialed-admin cluster flagged last regrade hasn't closed — #679 (core "which DB does this touch" decision, needs-human), #683 (unconditional `roles: ["admin"]` upsert), #691 (no interface binding), and #692 (no dev shared secret) are all still open 11 days after filing (07-30), and the scheduled health audit added a fifth, related finding: #737 (severity:medium, opened 08-05) — `isValidRedirect()` doesn't reject backslash-prefixed paths, a plausible open-redirect on the same dev-login route, untested by the existing redirect-safety test. Two same-day mitigations landed 07-30 (ce98e9f gates dev-login on which DB it would touch; f76e3f0 fixes the loopback check) but the core design decision is unresolved and the cluster is growing, not closing — the "bug rate spike" rule 2 calls out, not one fresh event. |
| ai         | A− | 2026-05-04 | Dedicated `src/domain/ai/`, multiple DB functions (`analyze_recipe_style_compliance`, `get_recipe_summary`, etc.), separate `docs/spec/ai-integration.md`. F100, F106, F111 all shipped. AI chat panel works end-to-end. |

## Architectural layers

| Layer | Grade | As of | Notes |
|---|---|---|---|
| Entity configs (`src/entities/`)              | A  | 2026-05-04 | 40 entity configs driving universal list/detail/edit. The project's defining strength. New entities take ~1–2 hours including pages. |
| Universal components (`src/components/universal/`) | A  | 2026-05-04 | 17 files, recently consolidated to `EntityDetailUnified` (F113). `EntityDetail` / `EntityForm` removed; ESLint blocks re-introduction. `StatusBadge` derives from `stateMachine.stateDisplay`. |
| Domain components (`src/components/domain/`)  | B  | 2026-05-04 | 114 files of mixed maturity. Recipe editor is the gold standard (independent-save sections, shared context, client-side calc). Other corners are simpler one-off forms. Test coverage spotty (only 2 domain components have tests). |
| Hooks (`src/hooks/`, `src/lib/queries/`)      | B  | 2026-05-04 | 19 hooks, no dedicated test files for hooks themselves. Centralized query keys (`src/lib/query-keys.ts`) now ESLint-enforced — strong invariant. |
| DB schema (`supabase/migrations/`)            | B  | 2026-08-03 | 261 migrations (count refreshed 2026-08-03, up from 256 on 2026-07-27; grade itself not re-derived since 2026-05-04). Eight executable checks now in place (security_invoker, RLS, auth.users, search_path, SECURITY DEFINER, permissive RLS, schema_registry, data-model docs). Corrective migration 00156 applied to production 2026-05-04. Allowlists hold ~30 grandfathered entries to be tightened over time. Real process wins this week: the unaudited feature-tracker migrations backlog reached 0 (#696 audited 31/32, #724/#728 captured the last live-only table, `channel_formats`), and deployment state is now a structured, machine-enforced field rather than free-text (#686, 07-30) — closing exactly the gap this doc's process section calls out. Held at B, not promoted: the specific gaps this row has tracked for two weeks are unchanged — #440 (F201 migration 00250 live-apply) is now open 20+ days after filing, and #580 (live-drift watchdog dead, `SUPABASE_DB_URL` auth failure) / #584 (live drift detected) are both still open 9+ days after filing — the drift detector that would catch a #584-style gap is still not running. |
| Tests (vitest + Playwright)                   | B  | 2026-08-10 | 2811 vitest passing across 213 files (measured locally 2026-08-10 via `bunx vitest run --coverage`, main @ 46cbed0 — identical to the 2026-08-03 count, confirming zero test-related merges landed during the 07-31→08-11 stall; see Harness row). Coverage gate: per-directory floors in `vitest.config.ts` (stmts/branch/funcs/lines — lib 50/45/38/50, domain 85/85/88/85, services 85/74/90/85, contexts 62/66/60/62), enforced in CI (`test.yml` runs `test:coverage`) and `make check-coverage`. Measured 2026-08-10, unchanged to two decimals from 08-03: lib 64.34/57.1/53.66/64.51 (its one real jump since the gate went in was 07-26→08-03; flat since), domain 97.75/94.47/100/97.43, services 95.93/83.8/98.48/97.65, contexts 85.49/79.59/75.51/84.16 — every gated directory still clears its floor. Shared test infra: `src/test/supabase-mock.ts`, `src/test/react-harness.ts`. Hooks and entity configs still lack dedicated tests. E2E: five multi-step flows run end to end with DB-side assertions (recipe editing, vessel transfer, packaging-session completion, order confirm→allocate→fulfil, recipe→batch→brew-log→packaging chain, all #437) over `e2e/seed.ts` fixtures; be4ecc2 (#702, 07-30) now runs Playwright on every PR. One `test.skip` remains (portal order placement, unbuildable, #706). New concern, held at B pending resolution: the nightly *schedule*-triggered run of `test.yml` ("Test") was red for 4 consecutive nights (08-04 through 08-07) while PR-triggered runs of the identical workflow passed (08-06, 08-09 both green) — tracked in #735 (opened 08-04), confirmed still open as of 08-12. **Closed 2026-08-12: #713** — `E2E Tests` is now one of four required status checks on `main` (ruleset 11725742), so a red PR-level E2E run blocks the merge instead of being a visible X anyone can merge past. Watch whether #735 closes and whether coverage moves again now that merges have resumed (see Harness row). |
| AI integration (`src/domain/ai/`, DB functions)  | A− | 2026-05-04 | Mature library + DB functions + spec doc + AGENTS.md routing. Recipe style compliance / brewing science / write-actions all wired. AI chat panel (F100) works in the app. |
| Harness (this folder + `Makefile` + scripts/) | B  | 2026-08-10 | Rolled out 2026-05-02 to 2026-05-04 across PRs and #249. 16 ultrareview findings addressed. Eight executable DB checks, ESLint custom rules, WIP=1 enforcement, bootstrap contract validation. **Moved A− → B** on three compounding, non-fresh signals, all measured 08-10: (1) the merge pipeline had fully stalled for 11 straight days — zero PRs merged to main from #729 (07-31T20:57:30Z) through 08-11T23:20Z, while the automated loops kept producing work regardless: 9 PRs sat open the entire stall with zero reviews (bug-patrol #730/#731/#734/#739/#740, quality-regrade #733, feedback-distill #732/#741, dependabot #738), oldest (#730, opened 08-01) waiting 10 days; (2) the nightly schedule-triggered run of `test.yml` was red for 4 consecutive nights, #735 (opened 08-04, still open — see Tests row for the streak detail); (3) the scheduled health audit found a high-severity gap in the regrade/distill workflows themselves, #736 (opened 08-05, `severity:high`): both jobs allowlist unscoped `Bash(gh api:*)` on a write-capable token with no `github_token:` passthrough, which — per the exchange mechanism this repo's own `health-audit.yml` already documented for a different workflow (#689, since fixed there) — can reach the PR-merge and Contents-write endpoints despite each prompt's own "never merge, never touch code" constraint; this is the same loop class that produced this file. **Postscript, 08-11T23:20Z–08-12T00:46Z**: all 9 stalled PRs (including #733), plus a 10th (#743, opened and merged in the same window), landed in a single ~86-minute burst, confirmed via `gh pr view` (mergedAt timestamps 23:20–00:46; `reviews: []` on every one) — closing the stall at exactly 11 days (07-31→08-11) rather than leaving it open-ended, but with zero human review recorded on any of the 10. The demotion stands: #735/#736 are still open, and a 10-PR unreviewed merge burst is further evidence for B, not against it — the human-review gate `improvement-loop.md` calls the safety net for these loops ("a bad automated PR cannot merge green") still hasn't fired once. Re-promote once #735/#736 close and merges land with actual review, not just a burst after a long queue. |

## Trend log

> Append a one-liner whenever a grade moves. Don't edit prior entries.

- **2026-05-02** — Harness rolled out (A−). DB schema bumped from C to B because executable security checks now exist, even though the corrective migration 00156 still needed to be applied.
- **2026-05-03** — Round 2 hardening landed (PR #249, commits 4f18fd4 + 3e8b24f): ESLint custom rules, schema-registry / data-model-docs / WIP / permissive-RLS / search_path / SECURITY DEFINER checks, Linux bootstrap fix, parser-correctness fixes for the new DB checks.
- **2026-05-04** — Initial baseline grades filled in (this commit). 00156_security_invoker_corrections applied to production database via `bun run db:migrate -- --include-all`; the 9 legacy views and `recent_vessel_cleanings` rebuild are now live.
- **2026-07-01** — Domain test-coverage loop (PR #330) + review fixes (grade held at B− per rule 2 — promote to B after the widened gate soaks in CI): 1920 tests / 109 files; coverage include widened to lib+domain+services+contexts with per-directory threshold floors (a single aggregate let strong dirs mask decay — src/lib functions sits at 41%, now floored at 38 and gated independently); coverage enforced in CI for the first time. Measured 2026-07-01: lib 54/41, domain 89/92, services 89/93, contexts 66/63 (statements/functions).
- **2026-07-24** — Fact refresh, no grade moves: migration count 147 → 244; Tests row corrected — the "suite TZ pinned to America/New_York" claim was false since #538 (threads pool ignores runtime TZ; issue #568). Rows not touched retain 2026-05-04/2026-07-01 evidence and should be treated as stale until the next full regrade (quality-regrade.yml removed 2026-07-24; scheduled-agent replacement pending).
- **2026-07-27** — One move: Tests B− → B (2389 tests/193 files vs 1920/109 on 07-01; every coverage-gate directory clears its floor; lib is flat and stays the tightest). Facts refreshed: migrations 244 → 256; PR/issue sweep since 07-24 shows very high merge velocity (~27 substantive PRs in 3 days) touching production, sales, packaging, auth/RLS, and the harness itself — added as notes on those rows but **no other grade moved**, per rule 2 (all under 3 days old). Harness held at A− rather than promoted: the 2-week quiet-soak bar keeps resetting (5+ ci: fixes just in 07-24–26) and #580/#584/#586/#587 are open live-drift/observability gaps. DB schema held at B: #626 had to recover two migrations that had drifted out of the committed chain, and #440 (F201 migration 00250 live-apply) is still open 13+ days. Next regrade: check whether #618 (TTB cellar reconciliation) and the portal/BOM/RLS fixes from this week held, and whether Harness's open live-drift issues are closed.
- **2026-08-03** — No grade moves; facts refreshed across production, auth, DB schema, Tests, and Harness. 64 PRs merged since 07-27. Tests: 2811/213 (vs 2389/193); coverage measured — lib 64.34/57.1/53.66/64.51, its first real jump since the gate went in (was flat at ~54/50/45/54 for a month), domain/services/contexts essentially unmoved; E2E now runs on every PR (be4ecc2, #702) but isn't a required check yet (#713 — main has no required status checks at all, so every CI gate stays advisory). DB schema: unaudited feature-tracker migrations backlog reached 0 (#696/#724) and deployment state is now a structured, enforced field (#686) — real wins — but held at B because #440 (F201 live-apply, now 20+ days open) and #580/#584 (live-drift watchdog dead / drift detected, both 9+ days open) are unchanged. Harness: closed #586 and #587 from last week's watch list, but #713 (advisory-only CI gates) is a new and arguably more fundamental gap that undercuts this doc's own "bad automated PR cannot merge green" claim — held at A−. (#713 closed 2026-08-12; see the trend-log entry for that date. The grade is not moved on it here, per rule 2.) Production: the TTB-cellar corner predicted to resurface did (#670 fixed same day; #697/#698 opened) with #618 now 3 weeks open. Auth: new dev-login uncredentialed-admin cluster (#679 + #683/#691/#692) partially mitigated same-day, core decision still open — check first next week. Everything measured this pass is ≤6 days old; nothing cleared the soak bar needed to move a grade.
- **2026-08-10** — Two moves. **Harness A− → B**: the merge pipeline fully stalled for 11 days (07-31→08-11, zero PRs merged) despite 9 automated PRs sitting open the whole time with zero reviews (oldest #730, waited 10 days); the nightly `test.yml` schedule run was red 4 nights running (#735); and the scheduled health audit found a high-severity, self-referential gap (#736): the regrade/distill workflows' own unscoped `gh api` write-token grant can defeat their "never merge" design. **Auth B → B−**: the dev-login cluster (#679/#683/#691/#692, all open 11+ days) grew a fifth open finding, #737 (open-redirect via backslash-prefixed paths) — the cluster is still growing, not closing. No other grade moved. Facts refreshed, all flat since the 2026-08-03 entry above: migrations flat at 261 (no schema merge since 07-31); tests flat at 2811/213 with identical coverage — the whole suite was frozen along with the merge pipeline. Postscript (08-11T23:20Z–08-12T00:46Z, discovered while resolving this entry's merge conflict against #733): the stall broke — all 9 previously-open PRs (including #733 itself), plus a 10th (#743), merged in a single ~86-minute unreviewed burst, which bounds the stall at exactly 11 days rather than leaving it open-ended. #735/#736 remain open and the review gate still hasn't fired even once, so the Harness demotion stands. Next regrade: check whether #735/#736 closed, whether the dev-login cluster finally got its core decision, and whether post-burst merges start getting actual review.

- **2026-08-12** — **No grade moves** (rule 2: this is the grader's own same-day work). Two long-standing gaps named in the rows above are now closed, and both are load-bearing for the Harness grade, so they are recorded here for the *next* regrade to act on once they have soaked. **#713 closed**: `main` now carries a `required_status_checks` rule (ruleset 11725742) requiring `Static Checks`, `Unit Tests`, `E2E Tests` and `Database Lint and Integration Tests`. Until today every CI gate in this repo was advisory — the exact condition that made this file's own "a bad automated PR cannot merge green" claim untrue, and a contributing factor to the 10-PR unreviewed merge burst recorded in the 2026-08-10 entry. `Production Build` is deliberately excluded (schedule-gated; requiring it would leave every PR pending) and a contract test now pins that it stays schedule-gated. The fix landed as a nine-line deletion of `db-lint.yml`'s `paths:` filter rather than the shim #713 proposed, because the issue's premise that db-lint was "the slowest job here" was wrong on measurement: db-lint ~3m13s against `E2E Tests` ~5m50s on every PR anyway. **#769 closed**: live schema drift reconciled — 00287 and 00288 applied, snapshot regenerated, `check-live-drift.sh` exits 0 on both assertions after failing every scheduled run since at least 08-10; the deployment tracker now reads 24 migration-backed / 24 live / **0 pending**. Also this pass: Tiers 2–3 of `docs/plans/backend-extraction.md` shipped (#789/#792/#793), each with characterization tests written against the pre-extraction code and an independent `refactor-reviewer` gate; three orphaned projection RPCs dropped and `src/lib/reports/` moved to `src/domain/reports/` (#790); worktrees pruned 26 → 2. Migration count 261 → 265 (highest number now 00289). Test suite 2811/213 → 2940/223, measured on the p1c branch immediately before merge. **Next regrade should ask:** did the new required checks actually block anything, and did merges start carrying real review — the review gate `improvement-loop.md` calls the safety net still has not fired once, and required checks constrain *green-ness*, not *review*. #735 and #736 were not touched by this work and should be re-checked; the Harness row's B stands until they close and merges land reviewed.

## How to update

1. Read the current grades. Disagree with one? Append a trend-log line, then update the cell.
2. Don't grade your own current work — wait until it's been live for at least a few days.
3. **Move a grade only when evidence has changed**: a bug rate spike, a successful refactor, a new pattern that simplifies a domain, an audit that surfaces hidden debt.
4. Trend lines matter more than absolute values. A `B → C` matters more than the difference between two `B`s.
