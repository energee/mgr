- **2026-07-29 (E2E lane could never authenticate — issue #644, branch `ci/644-e2e-dev-login-in-ci`).** `e2e/auth.setup.ts` signs in through `/api/auth/dev-login` (no hosted E2E credentials exist and the job boots a throwaway local Supabase instead), but that route hard-404'd unless `NODE_ENV === "development"` while `test.yml`'s e2e job serves a production `next build` via `bun start` — so the `setup` project timed out on `waitForURL("**/dashboard**")` and every authenticated chromium spec failed. Latent until now only because the job never ran (`needs: build`, red on `bun audit`, issue #639). Fixed by gating the route on `E2E_DEV_LOGIN === "1"` **or** `NODE_ENV === "development"`, hard-floored by `VERCEL_ENV !== "production"` so a misconfigured hosted deployment can never serve a credential-less admin login; the flag is set job-scoped on the e2e job only. Pinned by a new contract test (flag present in the e2e job's `env:`, absent from every other workflow) plus 12 route-gate tests covering exact-`"1"` matching, the production 404, and the Vercel floor. Red-checked both against the pre-fix gate.
