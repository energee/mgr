- **2026-07-29 (F201 deployment evidence).** Verified against the committed tree that `supabase/live-catalog.snapshot.txt` carries every migration 00250 object (`beer_order_import_runs`, `beer_order_customer_mappings`, `beer_order_brand_mappings` with RLS + read policies, and `apply_beer_order_import(p_run_id uuid)`) and recorded that deployment fact — plus the still-outstanding live admin smoke test — in F201's `evidence` field in `docs/feature_list.json`, so a reader of the tracker can no longer mistake `passing` for "deployed and smoke-tested" (refs #440).
