- **2026-07-14 (SECURITY DEFINER lockdown — PR #410, 00245–00247 APPLIED LIVE).** Closed two real RLS-bypass holes, both found by checking the live database rather than reading the migration chain. **00245**: `dispatch_email_notification` (from 00190) was `SECURITY DEFINER` with no `REVOKE`/`GRANT`, so Postgres' default `EXECUTE` to `PUBLIC` left it callable over PostgREST RPC by any authenticated role — including customer-portal accounts — with every argument caller-controlled (recipient, title, body, action URL): system-branded phishing from the brewery's own return address. **00247**: verifying 00245 against `pg_proc` ACLs then found **seven more SECURITY DEFINER functions executable by `anon`** — no login at all: `credit_bin_inventory`/`debit_bin_inventory` (mutate `bin_inventory`, bypassing its RLS), `create_notification`/`notify_all_users` (write notifications to any/every user), `cleanup_old_notifications` (DELETE), `check_low_inventory`, and the vestigial `user_has_brewery_access`. The bin functions *already carried* `REVOKE`/`GRANT` in 00232/00241 — **the chain said locked while live said open**: Supabase's `ALTER DEFAULT PRIVILEGES` re-grants EXECUTE to `anon`/`authenticated` on `CREATE OR REPLACE`, so a later replace silently undid the earlier REVOKE. **Any future `CREATE OR REPLACE` of these functions must re-run its REVOKE — this trap will re-fire.** `is_admin_rls` is deliberately left executable (two `user_profiles` RLS policies call it; policies evaluate as the querying role). Verified live: the anon-callable SECURITY DEFINER set is now empty except `is_admin_rls`. Also **00246** backfilled the `_schema_registry` rows and `docs/data-model/` sections the 00199/00222 capture migrations skipped (`containers`, `selling_formats`, `email_settings`, `square_locations`), and the three "unjustified SECURITY DEFINER" check failures proved to be linter false positives (the checker greps for a literal token on the line immediately above `CREATE`). **All eight DB checks and `make check` now pass for the first time.** Outstanding: `supabase/live-catalog.snapshot.txt` was NOT regenerated (needs `SUPABASE_DB_URL`, a direct `postgresql://` DSN — *not* `NEXT_PUBLIC_SUPABASE_URL`), so the drift watchdog will WARN until `scripts/db-push.sh` is re-run with it.
