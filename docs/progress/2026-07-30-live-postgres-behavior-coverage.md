- **2026-07-30 (live-Postgres behavior coverage for allocation, vessel and TTB paths).** Closed the remaining gaps in issue #437's acceptance criteria 5 and 6 with four new `src/__tests__/integration/` suites (+21 tests, 26 files / 181 → 30 / 202; run by `db-lint.yml`, which triggers on `src/__tests__/integration/**` — note that no workflow is a *required* check on `main`: the ruleset declares no `required_status_checks` rule at all, so this reports but does not block; verified against the ruleset API, filed as #713 and cross-noted on #702). `ttb-loss-reconciliation.test.ts` drives `transition_entity_atomic`'s unattributed-remainder loss through to Form 5130.9 and pins the removal non-overlap invariant — cellar and packaged-beer removals partition a batch, so all classes together sum to the production baseline exactly once (the class of mistake PR #685 made). That sum is **one-sided**, and the suite now says so rather than claiming otherwise: dropping the cellar line breaks it, but counting packaging as a cellar removal moves no number at all — proven by mutation (deleting 00274's exclusion clause left all 9 numeric TTB tests green), which is what 00274's own header already said. The clause is a forward guard, so it is held by a structural assertion instead (`keeps internal batch movements out of the removals CTE`; the same mutation now fails there and nowhere else). `order-fulfillment-atomicity.test.ts` covers reservation completion, volume back-fill, cancellation that never reverses a completed removal, and FIFO — which the shortfall case could not distinguish from LIFO, so `draws the oldest lot first` re-numbers the lots against their production dates and demands less than total stock (dropping the `production_date` term from 00256's draw now fails it: 60/10 becomes 30/40). `vessel-release-transfer.test.ts` covers batch-completion vessel release plus `handle_vessel_transfer`'s destination claim, ledger-proven release, and the 00228/00235 `empties_source` corrections; `inventory-guard-concurrency.test.ts` closes the concurrency coverage #557 left open, driving two real transactions until one is observably blocked on a lock. Every failure path asserts the absence of partial state; `availableUnits` now cross-checks itself against `guard_allocation_availability` rather than only re-implementing it (disabling that trigger fails 6 of 7 fulfillment tests). Keg packaging output — the half of criterion 5 no fixture in the repo covered — is now tested by a schema-aware case that requires the outcome the schema mandates: where the chain's `keg_transactions.keg_type_id NOT NULL` survives the line must fail leaving nothing partial (#701, reproduced), and where the column was dropped out-of-band it must complete with finished good, keg fill and batch allocation all present. Green both ways: 30 files / 202 tests against a from-scratch 258-migration replay, and 21/21 for these four suites against a local Supabase database that predates the squash (where 6 pre-existing tests in 3 unrelated files fail because 00282 is not applied there). `fileParallelism: false` on this config, re-measured on an idle machine: parallel 5 of 5 full runs failed on `deadlock detected` (2-4 tests each, six different files), serialized 8 of 8 clean — and serialized is *slower* in wall clock (5.4-7.9s vs 1.1-2.0s), so the earlier "faster" claim was wrong too — but serializing files does not serialize the connections one file opens or anything outside the run, so every suite taking a table-level lock now opens through a `beginBounded` helper with `lock_timeout`/`statement_timeout`, turning contention into a named statement instead of an opaque 15s timeout. Documented the non-overlap rule, and the fact that only one direction of it is arithmetically detectable, in `docs/knowledge/brewing-domain.md`.
