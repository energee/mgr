- **2026-08-12 (material-planning backend extraction, T3.1).** Split `src/hooks/use-material-planning.ts` into pure math (`src/domain/material-planning.ts`), Supabase reads (`src/services/material-planning-service.ts`) and a thin React-Query hook; both new modules are React-free, so the packaging BOM rollup no longer requires a renderer to compute. Wrote 28 characterization tests against the pre-extraction hook first (green before the split, unchanged after) pinning whole-unit ratio recovery, the per-batch ceiling (M8), on-hand flooring and shortfall sort; added 28 direct domain tests replaying `WHOLE_UNIT_PARITY_CASES` (+56 overall, 2884 to 2940); verified equivalence with a throwaway differential fuzz of the old algorithm over 3000 random sessions. Deliberately did **not** merge with `computeBomConsumption` — that is the actual-quantity depletion path and differs on non-positive quantities and zero rows. Corrected the plan entry: its named target `useCalculateOrderMaterials` had already been moved into migration 00265 by #505 and no longer existed.
