- **2026-07-31 (PO expected_date UTC date math).** Fixed a DST off-by-one in `createDraftPO`: `expected_date` was computed with local `getDate`/`setDate` on a UTC-parsed date, shifting one day early across spring-forward. Now uses `getUTCDate`/`setUTCDate` (same rule as QuickBooks `addDays`). Regression test added and red-checked (fails 1/7 without the fix, passes 7/7 with it). PR #722.
