- **2026-07-13 (PR #402 review fixes).** Hardened shared worktree reuse so an existing name cannot silently resolve to a different requested branch, with regression coverage; completed the `verify` skill migration into canonical `.agents/skills`; made agent-config validation bidirectional and part of `make check`; declared `jq` in setup; and aligned autonomous bug skills with the mandatory full pre-commit gate.
