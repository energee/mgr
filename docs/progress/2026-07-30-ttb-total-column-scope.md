- **2026-07-30 (TTB Total column is scoped per line).** The Form 5130.9 report's Total column added the cellar row's *brewed* volume to the keg/bottled rows' *packaged* volume, counting a batch brewed and packaged in one month twice (#670). The scope is now decided per column by `TOTAL_SCOPE_BY_COLUMN`, on what each column's cells actually measure: Part I's inventory and production lines sum the identity-checked (packaged) classes only — derived from the same `IDENTITY_EXEMPT_TAX_CLASSES` list #618 scopes the accounting identities with, and marked `†` on all three surfaces — while Part II's removals and the beer-in-process line sum every class. Cellar removals stay in: migration 00274 (#603) admitted them to the form and excludes packaging and inter-vessel movements so they cannot overlap a packaged removal, so dropping them would understate removals on a filing-prep figure. The "Beer Produced" summary card is relabelled "Beer Packaged" on the per-tax-class path, since that is now what it sums. All three surfaces take their Total from one `totalForColumn` helper instead of re-deriving it, with a parity test that fails if any of them drifts. Known residue, disclosed on every surface and filed as #698: the Total column no longer cross-foots — its removals include the cellar's, which have no packaged inventory line to reduce.
