- **2026-07-25 (packaging BOM ceiling + summary-view drift).** Fixed #616 (00279: `exact_material_qty()` recovers the integer ratio behind the 4-decimal `quantity_per_unit`, `whole_unit_material_qty()` is now `ceil()` of it, and `transition_entity_atomic` sums the exact per-line requirement and ceils each (batch, item) group total once — 240 units at a stored `1 per 24` consumes 10 cases, not 11) and #612 (00278 recreates `packaging_sessions_with_summary` so the frozen `ps.*` expansion picks up `default_bin_id`, which the detail form had been nulling on every save); added a real Postgres regression suite for the depletion path, a shared TS/SQL parity fixture table, and a general guard test asserting every editable section field exists on the relation its entity reads.
