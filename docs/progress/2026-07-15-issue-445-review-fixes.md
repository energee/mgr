- **2026-07-15 (Issue #445 review fixes).** Addressed PR #475 review: a failed success-path `qbo_sync_log` write no longer flips a completed Invoice/Bill sync (remote doc created + mapping saved) into a reported failure — the new `completeSyncLogSuccess` helper logs it loudly instead. Extracted the shared success/error sync-log tail into `completeSyncLogSuccess` and `recordSyncFailure` so the durability protocol lives in one place across sync-invoice/sync-bill. Documented the two request-ID ceilings inherent to Intuit idempotency: the ID is payload-independent (a create-retry after an edit returns the original document until the next update sync) and dedup is time-bounded (a retry after the window can create a duplicate).
