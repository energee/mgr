- **2026-07-11 (create-mode status bypass closed — branch `fix/create-mode-status-bypass`, backlog P2 #13, audit EA-1 + EA-9; NO migration).** Create forms could birth an entity in ANY machine state (packaging session created "Completed" = no FG creation, no material depletion, stuck read-only; the 00143/00205 triggers fire on UPDATE only — INSERT is invisible). Fixed at the universal layer, zero per-entity edits: **(1)** `createModeStateOptions()` + `clampCreateStateField()` in `src/types/entity.ts` — UnifiedField clamps every create-mode state select to `[initialState]` (dynamicOptions suppressed for it), `buildDefaultValues`/`buildQuickCreateDefaults` seed the state field with `initialState` (also un-breaks vessel quick-create, whose non-nullable enum status previously parsed a `null`), and both create save paths (EntityDetailUnified handleSave, QuickCreateForm submit) force `initialState` as the backstop against prefill-store/defaultValues staging; **(2)** EA-9 — `resolveServerCore` wraps every stateful core's formSchema with a state-membership guard (`superRefine`; zod 4 returns `this`, so `.shape` survives), covering all consumers (forms, quick-create, entityService, AI chat cores); `src/lib/schemas/batch.ts` status became a real `z.enum(batchStates)` because `api/batches` imports the raw schema, bypassing the config seam. **Deliberately SKIPPED the optional 00247 INSERT-time DB check:** legit non-initial INSERTs exist by design — Square webhook inserts completed `taproom_sale` allocations (webhook route :736; backlog #14/EA-3 leans harder on that path), consumption-service inserts completed reconciliation allocations, the MongoDB import inserts completed brew_logs, and the 00184 revise flow — so a safe check needs per-table exemptions, not "small and safe". Tests +14: `src/entities/__tests__/create-mode-state.test.ts` (registry-driven: create-editable state selects clamp to initialState; schemas reject non-machine states, accept every machine state — pins enum↔machine drift, the 93f944a3 failure mode — and pin the delivery/location_transfer omit-set), `entity-helpers.test.ts` (unit coverage for the new helpers + guard), quick-create defaults seeding. Docs: entity-model.md lifecycle-entry rule. Gates: lint + typecheck + full test green.
