- **2026-07-16 (PR #504 review fix: order row lock on submission).** Rebased `fix/issue-488-change-request-workflow` onto main (kept both DEC-GAP entries; PR's decision renumbered to DEC-GAP-015) and closed the review's TOCTOU finding: `submit_order_change_request` now reads the cutoff under `FOR UPDATE OF o`, enabled by a lock-only `orders_customer_lock` policy (`WITH CHECK (false)`) mirroring the migration's own `change_requests_customer_lock` pattern.
