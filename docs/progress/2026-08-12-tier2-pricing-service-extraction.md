- **2026-08-12 (Tier 2 — customer-tier pricing lifted out of React).** Executed the `backend-extraction.md` Tier 2 node. T2.1 (PO receiving status rules) was found **already shipped** — commits `8bbbe5d9` / `157fce39` / `0b93f10f` moved the fulfilled-vs-partial decision into the `receive_purchase_order_items` Postgres function behind `src/services/po-receiving-service.ts`, superseding the plan's `src/domain/purchasing/po-receipt-status.ts` with a stronger atomic boundary; nothing to redo. T2.2 done: added `src/services/pricing-service.ts` (`getPriceForCustomer`, React-free) wrapping the `get_price_for_customer` RPC, and routed **both** raw call sites through it — `order-items-editor.tsx` (which was reaching for it via `dynamicRpc`) and `reorder.ts` (the duplicate-order re-pricing path the plan did not list). Four characterization tests pinning the "Apply tier price" flow (args sent plus price written back plus tier-named toast in one case; empty result; RPC error, which collapses to the same "no tier price found" as an empty result; button hidden with no customer) were written and passing against the pre-extraction code first, then re-run unchanged against the service; six more blocks cover the service directly, one of them an `it.each` of three, for +12 tests overall (2884 to 2896). Structural win, not a line-count one: production code is −28 lines across the two components against +88 in the new service. `make check` green.
