- **2026-08-11 (Phase 4C: confirm-gated AI chat writes).** First direct write path for the chat assistant: new `recordBatchReading` tool proposes a `ConfirmWriteIntent` (writes nothing), the chat panel renders a Confirm/Cancel card, and confirm POSTs to the new `POST /api/chat/write`, which re-validates via `src/lib/schemas/chat-write.ts` and inserts the `batch_logs` measurement under the caller's session client (RLS-trusting; `withPermission("batches:write")` for friendly 403s). Settles Phase 4B's NavigationIntent-vs-direct-write fork in favor of direct writes behind a confirmation gate; other write tools stay navigational. Tests: route (validation, ineligible state, RLS-denial mapping) + tool (pending shape, no insert, refusals).
