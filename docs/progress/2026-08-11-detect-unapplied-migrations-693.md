- **2026-08-11 (live-drift watchdog now detects committed-but-unapplied migrations).** Action 2 of #693: `scripts/check-live-drift.sh` additionally diffs the migration-chain version prefixes (`supabase/migrations/*.sql`) against `supabase_migrations.schema_migrations` on live — committed-but-unapplied versions FAIL the run, live-only versions WARN, matching the watchdog's missing=FAIL/adds=WARN philosophy. Comparison core extracted to `scripts/compare-migration-versions.sh` (DB-free, unit-tested in `scripts/__tests__/compare-migration-versions.test.sh`, wired into `make check-agent-config`). Action 1 (applying 00282 live via `scripts/db-push.sh`) remains an operator step.
