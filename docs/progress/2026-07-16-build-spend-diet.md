- **2026-07-16 (build spend diet).** Added a Vercel ignored-build step (`vercel.json` `ignoreCommand` → `scripts/vercel-ignore-build.sh`) that skips builds for commits touching only docs/, *.md, .github/, .claude/, or .agents/ — docs-only pushes and automated PROGRESS.md rebuild merges no longer consume Vercel build minutes. Verified Dependabot grouping was already in place (github-actions bumps land as one grouped weekly PR since PR #419; no npm ecosystem section exists).
