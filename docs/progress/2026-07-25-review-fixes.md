- **2026-07-25 (Code-review fixes on the ponytail cut passes — branch `chore/harness-gaps`, commit `c4b30f5d`).** Reviewed the full `origin/main...HEAD` range (21 commits, 231 files, −7.4k lines) and fixed six findings. **Real bugs the cuts introduced or exposed:** the data-table filter list's join-operator query-state key was `""` while `entity-data-table` reads `"joinOperator"`, so switching a list's filter join from and→or wrote `?=or` and the query kept ANDing — latent before, but the cuts removed `TableMeta.queryKeys`, the only override path; `getFiltersStateParser` `safeParse`d the whole filter array, so one entry with a retired `variant`/`operator` (the cuts shrank both enums) or a removed column silently wiped *every* filter on a saved/bookmarked URL, now validated per item; the reorder `-R` suffix probe went through the consolidated `escapeIlikePattern`, which escapes `*`, but PostgREST rewrites `*`→`%` with no escape syntax, so an order number containing `*` made the probe match nothing and `nextDuplicateOrderNumber` could hand back an existing number (unique violation) — a local `escapeLikePrefix` lets `*` widen instead, safe because the caller re-checks each row with `startsWith(base)`; and folding the portal error boundary onto the shared `RouteError` added a "Go Home" → `/` button that bounces portal customers off the staff gate (new optional `homeHref`, portal passes `/portal`). **Enforcement regression:** `sideEffectTables()` in `transition-call-sites.test.ts` had been hardcoded to five entries with a one-directional assertion, so registering a new `transition_entity_atomic` side effect would silently stop extending API-route atomicity enforcement — it parses the `p_table_name = '<table>'` guards out of migration 00256 again, with the sanity test guarding the parse against yielding `[]`. **Docs gate:** dropped the `api-layer.md` rows for the two routes this branch deleted. Tests +6 (new `src/lib/__tests__/parsers.test.ts` for per-filter degradation; one reorder case pinning the probe pattern) → 191 files / 2379 tests, `make check` green. **Also found, but pre-existing on `origin/main` and NOT fixed here:** `00272_data_integrity_cron.sql`'s `negative_on_hand` check selects `allocations.inventory_item_id`, a column gone since 00010 (that ledger is now `allocations_legacy`), so the nightly watchdog errors every run and records nothing; and `getEditableFieldsFromSections` filters on `f.type` only, ignoring `editable: false`, so the `""`→`null` normalization makes `batchSchema.safeParse` reject `batch_code` on create with no way for the user to clear it (same shape for `brew_logs.brew_number`, `yeast_pitch_events.pitched_at`) — either `.nullable()` those three fields or skip non-editable fields in the null pass.
