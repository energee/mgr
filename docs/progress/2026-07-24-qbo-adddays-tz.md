- **2026-07-24 (QBO due-date timezone fix).** First real CI run after restoring Actions (repo now public) exposed 4 QuickBooks sync test failures: `addDays` mixed a UTC parse with local-time day arithmetic, so Bill/Invoice due dates shifted by one day depending on host timezone and DST (CI/Vercel run UTC; the tests characterized America/New_York behavior). Deduplicated the two private copies into a UTC-safe `addDays` in `sync-utils.ts` and updated the 4 expectations to plain calendar math. Also discovered the vitest `env: { TZ: "America/New_York" }` pin has been silently inert since the threads-pool switch (#538) — Node worker threads ignore runtime TZ changes — left as a follow-up.
