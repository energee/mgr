- **2026-07-30 (Repair main's red contract after #702 and #708 merged past each other).** #702 set `E2E_MIN_PASSING: "16"` against the 17 enabled specs it saw, and #708 implemented three scaffold flows and pruned two render cases, taking the enabled count to 20 — each PR green alone, both red together, because the ratchet in `.github/scripts/ci-workflows.test.ts` requires the floor to sit within 2 of the enabled `test(` count and neither branch was rebased on the other before merging. `main` was left failing at `expected 16 to be greater than or equal to 18`. Raised the floor to 19 (20 specs + the auth setup project = 21 passing, minus the 2 of slack the ratchet is designed to allow) and corrected the workflow comment, which still described 18 passing against 5 skips. Red-checked all three bounds: 16 (too low, main's actual state), 25 (unreachable, reds the lane for everyone) and 0 (vacuous) each fail the contract; 19 passes at 64/64. The ratchet worked exactly as designed — it just fired after the merge rather than before, which is what having no required status checks on `main` allows.
