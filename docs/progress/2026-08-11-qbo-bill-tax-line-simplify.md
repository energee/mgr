- **2026-08-11 (QBO Bill tax-line cleanup + settings gap fix).** `/simplify`+`/code-review --fix` pass on PR #731 (`syncBill` posting `purchase_orders.tax` as its own Bill line). Extracted the duplicated shipping/tax account-mapping-lookup-with-fallback logic in `src/integrations/quickbooks/sync-bill.ts` into a shared `resolveExtraCostAccount()` helper, hoisted `shippingCost` to compute it once instead of twice, and ran the two independent mapping lookups concurrently via `Promise.all`. Correctness review then found the settings UI (`src/app/(app)/settings/integrations/quickbooks/page.tsx`) had no `tax` entry in its `CATEGORIES` list, so operators could never configure a distinct tax account mapping — every taxed Bill would permanently fall back to COGS with a recurring warning. Added `{ key: "tax", label: "Tax Expense" }` alongside the existing `shipping` entry. `bun run test src/integrations/quickbooks` (31 passed) and `make check-fast` both clean.
