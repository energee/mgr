- **2026-07-25 (Nightly data-integrity watchdog had never run — branch `fix/data-integrity-cron-allocations`, migration `00273` NOT applied live).** Found while reviewing PR #590; pre-existing on `main` since #573 (2026-07-24). 00272's check #1 read `allocations.inventory_item_id`, a column gone since 00010 — that migration renamed the old signed ledger to `allocations_legacy` and rebuilt `allocations` around `source_type`/`source_id` + `destination_type`/`destination_id` with `CHECK (quantity >= 0)`, so 00272's own header ("positive for additions, negative for usage; the sum is the on-hand") described a table it wasn't querying, and on the real one `SUM(quantity)` can never be negative. **PL/pgSQL plans statements on first EXECUTION, not at CREATE FUNCTION time**, so the migration applied cleanly and the 05:30 UTC job then aborted every run with `column a.inventory_item_id does not exist`; since the body is one transaction, checks #2 and #3 never ran either — the watchdog has recorded nothing since it was scheduled. Reproduced directly against local Postgres before fixing. **00273** replaces check #1 with `over_allocated_lot`: `inventory_lots_with_quantities.remaining_quantity < 0`, i.e. more planned+completed allocation against a lot than was ever received. That view (00014) is the on-hand seam the app itself uses (`consumption-service`, `inventory-count-service`), and the condition is exactly what 00212's `trg_guard_allocation_availability` exists to prevent — so a finding means the guard was bypassed, which is the silent corruption the job was built to surface. Checks #2/#3 unchanged; no data cleanup needed since the broken check could never insert a row. **Two guards against the same class of bug recurring:** the migration ends with a `DO $$ … PERFORM check_data_integrity() … RAISE EXCEPTION` self-check, so a body that no longer plans rolls the migration back instead of scheduling a broken job; and new `src/__tests__/integration/data-integrity-check.test.ts` (+5, real Postgres, BEGIN/ROLLBACK) actually *calls* the function — the first test is simply "does not raise", which is what was missing. Verified locally: 00273 applies, the self-check passes, and a seeded 150-against-100 lot (guard trigger disabled) produces exactly one `over_allocated_lot` finding reading `remaining_quantity is -50.0000 (received 100.0000, allocated 150.0000)`; an exactly-consumed lot produces none; re-running upserts rather than duplicating. Docs: `scheduled-jobs.md` gains a "cron SQL is not verified by the migration applying" rule with the two-step requirement, `data-model/system.md` renames the check. `make check` green. **Deploy = `scripts/db-push.sh` after merge.**
