- **2026-08-21 (schema audit fixes, steps 1–4).** Executed the first four steps of `docs/plans/2026-08-21-schema-audit.md`: removed the dangling `recipe_variant` entity relation (batch core + schema + AI chat tool); 00294 drops the dead `recipe_variants` family, `recipe_collaborators`, `vessel_cleanings` (+view+enum), `packages`, and `pricing_channel_formats` behind loud empty-guards; 00295 fixes the `batches` registry `key_fields`, adds `notifications`/`notification_preferences` FKs to `auth.users` (CASCADE), re-asserts `orders_status_check`, and adds `batch_blends` self-blend CHECK + cascading FKs; 00296 captures the live-only `email_notification_log` table into the chain (byte-exact policies), adds nightly pg_cron log retention (`prune_log_tables(90, 365)`, tunable) and finally schedules `cleanup_old_notifications(90)`.
