- **2026-08-12 (2.0 streamlining, waves 1–2).** Ran a dependency-graph agent wave over the 2.0 audit backlog: nine PRs merged (#771–#774, #779–#782, #784). Wave 1 (deletions) removed the 50-file hand-rolled animated-icon farm and the `motion` dep (#771), trimmed `ai-elements` to the exports `chat-panel` actually imports plus `ui/hover-card` + `ui/button-group` (#772), retired the `@deprecated` `OrderPickList` and routed its URL to the canonical pick-list detail page (#773), and unified four parallel Postgres-error mappers while dropping dead lib surface and two shadowed Radix deps (#774) — roughly −7,000 LOC and three dependencies. Wave 2 (consolidations) extracted a shared line-items editor behind four of five row editors (#780), a report-page scaffold plus testable summary math behind five of six reports (#782), a shared sortable-row primitive behind all five schedule editors (#784), and split the PO accept dialog and recipe additions display (#779). Two audit findings were overturned by the agents doing the work: `yaml` is imported by a CI script and the four `@streamdown/*` plugins are live in the chat renderer, so neither was removed. Two read-only verify nodes returned KEEP and cancelled their planned cuts — the MongoDB sync is mid-production-migration (#770, migration 00286 already live) and the advanced filter/sort builder is mounted on all 35 entity list routes. Wave 2 is a structural win rather than an LOC win: the report scaffold in particular is a net wash on lines. Review gates caught what `make check` could not — a trash-can action icon silently remapped to lucide's backspace glyph, a dropped `htmlFor` breaking click-to-focus and the accessible name on a date input, and a fabricated migration lineage in a doc comment. Landed #781 for a latent bug the icon work introduced: `ActionMenuItem`'s `href` branch dropped `disabled`, and under Radix `asChild` a disabled link stayed reachable via keyboard, screen-reader link lists and open-in-new-tab. New characterization coverage where none existed: 41 tests for `src/domain/water-chemistry.ts`, 13 for line-item commit-on-blur, 11 for the sortable-row ID backfill (mutation-checked), and a 407-line report-scaffold render test. Filed #785 (the `code-review` skill resolves the session cwd instead of the invoking agent's worktree, so `--fix` edits land in the wrong checkout — hit three nodes) and #786 (the pre-PR hook resolves `git rev-parse --git-dir` against the main checkout, so the sentinel is worktree-blind and one agent's confirmation can satisfy another's gate). Remaining from the plan: Wave 3 nav freeze, and a keep/delete decision on Square.
