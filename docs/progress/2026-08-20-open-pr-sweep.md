- **2026-08-20 (open-PR sweep: every red or stuck PR triaged to a root cause).** Six PRs were open; none was blocked by what its status page suggested. **#843** (dependabot `js-minor-patch`, next/eslint-config-next 16.3.0 → 16.3.1) failed *three* checks — Static Checks, E2E Tests and Database Lint — for one reason: dependabot bumped `package.json` without regenerating `bun.lock`, so every job died at `bun install --frozen-lockfile` before running a line of its own work; regenerated the lockfile (15 lines, only the two packages) and re-verified `--frozen-lockfile` plus `tsc --noEmit` clean against 16.3.1. **#838** (TTB CSV/print decimals) failed Unit Tests on a stale assertion the fix's author missed: the commit swapped `formatBbl` for `formatTtbBbl` at six call sites so exports match the screen and Form 5130.9's fixed 2 decimals, but `ttb-report.test.ts:630` still asserted the trimmed `["62.5","40","25","65"]`. Updated it to the 2-decimal form — the fix is right and the test characterized the bug; that line is the only assertion in the file comparing raw CSV cell strings (the rest parse via `Number()`, so `"62.50"` was already fine). `make check` green. **#829, #836, #837** were all `BLOCKED` with nothing red at all, which turned out to be two different faults sharing a symptom. #829 had simply had its E2E job cancelled (rerun). #836/#837 had **no check runs whatsoever**, and that is a structural bug now filed as **#844**: since #713 made four contexts required on `main` (ruleset 11725742, 2026-08-12), a PR that does not run them can never merge — and a PR opened with `secrets.GITHUB_TOKEN` triggers no workflows, a GitHub behaviour `ci.md` already documented for the agent loops but which nobody connected to the required-checks change. So `progress.yml` and the codegraph-refresh lane open PRs that are unmergeable by construction, with nothing to fix, while `gh pr merge --auto` waits forever; both bot PRs had sat since 2026-08-18 and started their checks only after a manual close/reopen (the workaround, since the reopen fires as a human). The durable fix needs an app/PAT credential on those lanes and is an operator call, so it is #844 rather than a patch here. Corrected the stale claim that caused it in all four places it lived — `test.yml`, `progress.yml`, `bug-patrol.yml` and `ci-workflows.test.ts` each asserted "ruleset `main` declares no `required_status_checks` rule", true when written and false since 2026-08-12 — and recorded the GITHUB_TOKEN corollary beside each, because a fact is only fixed when every copy of it is. `ci-workflows.test.ts` 73 tests green; `docs/agents/ci.md`'s "Required status checks" section was already accurate and needed no change.
