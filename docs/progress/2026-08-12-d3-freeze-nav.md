- **2026-08-12 (2.0 streamlining node D3 — nav freeze was already in effect; pinned it instead — branch `chore/2-0-d3-freeze-nav`).** The D3 brief called for removing QuickBooks, Slack, beer-orders-XLSX, keg-owner/deposits and the Inventory/Sales dashboards from `src/components/domain/shared/nav-items.ts`. All five are absent from that file, and `git log -S` across full history confirms QuickBooks and the keg-owner routes were **never** in it; the dashboards were dropped by the 2026-07-12 mobile-UX nav simplification (#746) in favor of the `DashboardSwitcher` pills. So the removal was a no-op and no nav entry was deleted. The features are reachable from elsewhere — the three integrations from the `/settings/integrations` cards, keg owners/deposit reports from the `/inventory/kegs` page links — but those are out of a nav-only node's scope and were left untouched. Converted the node into a durable guarantee instead: `nav-items.test.ts` gains a prefix-matched "keeps the D3 freeze-list out of the nav" case pinning `/settings/integrations`, `/inventory/kegs/owners` and `/inventory/kegs/reports` as frozen roots, plus a case asserting `/inventory/kegs` itself stays (keg FILL identity via `keg_filled_contents` is live and must not be swept up with owner/deposit tracking). Mutation-checked: adding either a `/settings/integrations` or a `/inventory/kegs/owners` nav item turns the suite red. `nav-items.ts` gains a header comment recording the freeze list, the reachable-instead route for each, and the two explicit carve-outs — Square POS is retained by product decision, and pick lists stay under Sales because their completion transitions allocations. No production behavior change; comments and tests only. `make check` green.
