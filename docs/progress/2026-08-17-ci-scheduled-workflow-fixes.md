- **2026-08-17 (scheduled-workflow red-run sweep).** Fixed the recurring scheduled CI failures: sentry-harness pack step no longer names gitignored paths in `:(exclude)` on `git add` (the bug that discarded real fix patches since the credential split; contract-pinned), `progress.yml` retries `gh pr merge --auto` through the "unstable status" race (contract-pinned; bot PR #805 was stranded by it), `prod-health.yml` and `sentry-harness.ts` retry `gh` calls through transient GitHub API 503s, bug-patrol gained grep/jq/diff in its allowlist plus a prompt rule that permission denials never excuse ending without a durable outcome, and the two weekly Claude jobs (quality-regrade, feedback-distill) went from 60 to 100 max turns after both died at the cap. Earlier same day: nanoid override 3.3.17→3.3.18 (PR #817) unblocked the nightly `bun audit` gate.
