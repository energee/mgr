- **2026-07-25 (Square refund reversal + sync-signal fidelity).** Fixed four Square defects on `fix/606-square-refund-reversal`: migration 00277 teaches `ingest_square_refund_atomic` to find an already-reconciled draft keg pour through the reconciler's `square_draft_sale:<id>` idempotency key instead of an exact `notes` match, does the reversal arithmetic in NUMERIC so a fractional keg draw no longer floors to zero, skips the bin credit for draft-origin rows (the keg sale never debited a bin), and only stamps `voided_at` on rows it actually neutralized (#606); the same migration records a refund whose sale has not been ingested yet as a durable `state=sale_missing` claim and returns a retryable `sale_missing` kind that the webhook turns into a 503 + `Retry-After`, re-opening that claim when the sale lands, instead of 200-acking and dropping the reversal forever (#607); `GET /api/square/sync/status` now applies the reconciler's `voided_at IS NULL` filter so the settings badge can reach zero, and 00277 narrows the supporting partial index to match (#608); and the Sync button reads the combined route's in-body inventory result through a new `squareSyncOutcome` helper (warning toast on a failed push), `last_inventory_sync_at` is stamped only when nothing failed, and `logSyncFailure` now writes a non-zero `items_failed` plus a `hardFailure` marker so a thrown sync is distinguishable from a clean no-op (#610). Red-first throughout: two real-Postgres cases in `square-atomic-ingestion.test.ts`, plus route/unit tests in `webhook-route.test.ts`, a new `status-route.test.ts`, `sync-routes.test.ts`, and `sync-outcome.test.ts`.
