- **2026-08-12 (Chat 4A: buildSearchTool factory).** Consolidated the nine hand-written single-table search tools in `src/app/api/chat/tools.ts` (`getFinishedGoods`, `searchOrders`, `getCustomers`, `searchBrewLogs`, `searchPurchaseOrders`, `searchSuppliers`, `searchPickLists`, `searchYeastPitches`, `getKegInventory`) behind one `buildSearchTool` factory in the new `src/app/api/chat/search-tools.ts`; each is now a `SearchToolConfig` naming only its table, column list, order, limit and per-argument predicate, with escape hatches for base filters, `!inner`-switching selects and post-query enrichment. `lookupEntity`'s four identical name lookups became a `LOOKUP_TARGETS` table (batches stay bespoke — two columns plus de-duplication). Characterization tests landed first as their own commit, pinning every tool's advertised argument defaults, exact PostgREST query and result shape, and passed unedited across the refactor. `tools.ts` 1313 → 935 lines; net production LOC is +51 once the factory module is counted — a structural win, not a reduction.
