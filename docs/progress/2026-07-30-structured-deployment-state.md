- **2026-07-30 (Structured deployment state in the feature tracker).** `docs/feature_list.json` entries now carry a required three-valued `migrations` field (an array of anchor migrations, `[]` when audited as needing no schema change, or `"unaudited"` when nobody has established which) and, once `passing` with a non-empty list, a fixed-shape `deployment` record (`live` + `observed_live_on` + a `verified_by` that must cite the live-catalog snapshot, or `pending` + `tracking_issue` + a `pending_since` that expires after 90 days); `scripts/check-feature-deployment.ts` reads it from `make check`, CI's static job and `make verify-feature`, exiting 4 (UNVERIFIED) on a violation, and for `live` it opens `supabase/live-catalog.snapshot.txt` and requires every table/function/trigger the declared migrations create to be present there. 8 entries are audited migration-backed (F104/F110/F114/F116/F130/F136/F200/F201, all snapshot-verified), 22 audited as needing no schema change, and 32 left explicitly `"unaudited"` with the count ratcheted and tracked by #696; the snapshot cross-check found that three of migration 00139's four functions are not on live (#697), F201's free-text deployment claim moved onto the field, and F003's self-recursive `verification` command was replaced (#654).
