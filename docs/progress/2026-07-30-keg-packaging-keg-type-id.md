- **2026-07-30 (keg packaging unblocked on replayed chains).** #701: completing a packaging session whose selling format used a keg container failed on any database built from the migration chain — `create_finished_goods_from_packaging` writes the fill `keg_transactions` row with `selling_format_id`, while `keg_type_id` had been NOT NULL since 00032 with nothing deriving it; the same constraint blocked keg order fulfillment and every manual keg movement. Reproduced against the local cluster, then fixed by 00283, which retires the column the way 00254 retired `square_draft_sales.keg_type_id` (lossless backfill of `selling_format_id` from the legacy id, abort rather than drop an unmappable reference). Live already dropped `keg_types` and the column out of band — `supabase/live-catalog.snapshot.txt` has no `TABLE|keg_types` and the live-generated `src/types/supabase.ts` has no `keg_type_id`, so supplying a value in the function would have broken production instead; whether live still carries the column at all remains unverified and needs an operator to read `information_schema.columns`. Coverage: three cases in `packaging-completion-trigger.test.ts` (keg fill recorded, package line writes none, column absent), red-checked against the pre-fix schema.
