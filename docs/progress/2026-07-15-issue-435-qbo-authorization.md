- **2026-07-15 (QuickBooks OAuth authorization — issue #435).** Protected OAuth start and sync-log with `integrations:manage`; extracted the existing role lookup into a shared fail-closed `requirePermission` helper so the redirect-based callback can recheck authorization without changing its response contract; bound the one-time OAuth state to the initiating user with a matching HttpOnly cookie and reject changed sessions before token exchange. Added route regression coverage proving unauthenticated and non-manager callers cannot read QBO credentials, set OAuth state, exchange/save tokens, or create a service-role sync-log query, while authorized flow and independent CSRF validation remain intact. No QuickBooks endpoint or token exchange was invoked during verification.
