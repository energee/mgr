- **2026-07-24 (CI security hardening for the public repo).** test.yml now runs static+unit on every PR (paths-ignore removed) so Static Checks / Unit Tests can become required status checks; progress.yml drops `[skip ci]` and merges its bot PR with `gh pr merge --auto` so it survives required checks. All non-`actions/*` actions (oven-sh/setup-bun, supabase/setup-cli, anthropics/claude-code-action) are SHA-pinned with a contract test enforcing the pin; trust basis recorded in docs/security/dependency-policy.md. `persist-credentials: false` added to every checkout in jobs that never push. claude.yml gained a job-level author-association gate (OWNER/MEMBER/COLLABORATOR). Sentry harness: prompt.ts fences all Sentry-derived data in explicit UNTRUSTED markers, and the fix job's `--allowed-tools` is narrowed from bare `Bash` to specific git/gh/bun/make command families (no `gh pr merge` grantable). scripts/db-push.sh now fails closed: confirmed pre-existing drift aborts without `--rebaseline-drift`, and REMOVED snapshot lines require interactive y/N (or `MGR_DB_PUSH_ACK=1`) before the refreshed snapshot is kept.
