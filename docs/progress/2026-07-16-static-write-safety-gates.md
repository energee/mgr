- **2026-07-16 (Static write-safety gates).** Mechanical enforcement for the two failure classes the issue-insights pass surfaced (docs/plans/2026-07-16-issue-insights.md): (1) `scripts/check-write-atomicity.ts` — ratchet gate in `make check-fast`/`make check` failing any non-allowlisted file with 2+ awaited PostgREST mutations and no `// tx-ok:` justification (30 pre-existing files grandfathered in the allowlist as the fix backlog); (2) ESLint `no-restricted-syntax` rule banning destructured `{message, code}` copies passed to `log.error`, which reach Sentry with no stack trace (#400/#401/#433 root cause).
