- **2026-07-15 (Durable QuickBooks document mappings).** Made Invoice and Bill creates persist their exact pending intent and use deterministic QuickBooks request IDs, so a lost response or failed local mapping write retries against the original remote document instead of creating a duplicate. Mapping and sync-log writes now fail loudly, remote-success/local-failure responses carry reconciliation context, and the UI tells operators to retry safely; added equivalent Invoice/Bill regression coverage plus lost-response and write-error tests for #445.
