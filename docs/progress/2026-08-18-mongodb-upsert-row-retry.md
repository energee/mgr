- **2026-08-18 (mongodb sync: isolate bad rows in a chunk upsert).** Sentry MGR-18 (#775) reported `syncTransfers()`'s raw `upsertRows()` tripping `handle_vessel_transfer()`'s occupancy guard during historical replay. That path was already fixed on this branch by migration `00288_mongodb_sync_replay_fixes.sql` — `syncTransfers()` uses `reconcile_mongodb_transfers` via `reconcileAggregate()`, which suppresses the trigger's live vessel claim/free under a transaction-local `mgr.mongodb_sync` flag instead of the session-replication-role + ledger-re-derivation approach the issue suggested (vessel occupancy stays owned by the live app, never reconstructed from replayed history). Remaining ask: hardened `upsertRows()` (`src/integrations/mongodb/sync.ts`) to retry a failed chunk row-by-row instead of failing all up to 50 rows in it — the same collateral-damage shape the issue described, still present for every other entity (`suppliers`, `malts`, `hops`, `yeasts`, `beer_styles`, `brands`, `vessels`, `batches`) that goes through the raw upsert path. Investigating the actual local Mongo dataset behind the recurring `batchIndex: 2` conflict (issue's item 3) wasn't feasible in this sandbox — the MongoDB URI lives in `system_settings`, not env, and there's no network path to the developer's Mongo instance here.
