- **2026-07-30 (dependency policy cleanup).** Pinned `brace-expansion` to the `1.1.18` 1.x backport, which patches GHSA-mh99-v99m-4gvg in the code that actually runs while keeping the callable module export `minimatch` 3.x needs (`bun lint` exits 0) — the 2026-07-29 exception record had claimed "first and only patched version 5.0.8, with no backport to the 1.x line", wrong within a day. The `--ignore` stays because the advisory's range is a flat `<= 5.0.7` that still matches 1.1.18 and `npm audit` offers only the major `5.0.9`, so the suppression is now metadata-driven rather than risk-driven; the record says so and its expiry moved 2026-08-29 → 2026-08-28, since 31 days violated this document's own 30-day cap. Also recorded runtime proof for the out-of-range `sharp` 0.35.3 override — `GET /_next/image` against a real `next start` returned a genuinely resized `PNG image data, 32 x 32`, so Next 16.2.12 does drive sharp 0.35's API — and corrected the audit contract test, which rejected `--audit-level=moderate|low` as "downgrades" when both are stricter than `high` (refs #653).
