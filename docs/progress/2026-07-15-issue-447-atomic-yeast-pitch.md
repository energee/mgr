- **2026-07-15 (Atomic yeast pitching).** Replaced the cached client-side balance check and two-step yeast pitch write with an idempotent `pitch_yeast_atomic` command that locks the source, recomputes committed usage, inserts the event, and maintains depleted status in one transaction. Added direct-write, immutability, and source-quantity guards plus migration preflight, aligned the yeast state machine, bound each request UUID to its exact payload with an explicit close/reset path, refreshed affected caches after both commits and conflicts, separated the full batch summary from the harvest strain projection so React Query cannot substitute partial rows, and added client-boundary and real-Postgres regressions for rollback, retries, permissions, direct DML, and concurrent overdraw. Local browser verification recorded partial and final-depletion pitches through one HTTP 200 RPC each, rendered complete 2.00/3.00 lb event rows, and confirmed two events totaling 5.00 lb with zero remaining before removing every verification row (issue #447).
