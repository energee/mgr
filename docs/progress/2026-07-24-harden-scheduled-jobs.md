- **2026-07-24 (harden scheduled jobs).** Batch C of the always-running-repo work: `nightly-watch.yml` (red scheduled Test runs now open/close one deduped `nightly-red` issue), live-drift.yml upserts/closes a single `live-drift` issue on real drift (infra failures excluded via exit-code split), grouped weekly npm/bun Dependabot block (minor+patch only), migration 00272 `check_data_integrity()` pg_cron sweep (negative on-hand / bin / lot quantities → RLS-protected `data_integrity_findings` table), report-only weekly `hygiene.yml` stale-branch report, and `docs/agents/scheduled-jobs.md` cataloguing every Actions cron, pg_cron job, and recommended Claude scheduled agent with kill criteria.
