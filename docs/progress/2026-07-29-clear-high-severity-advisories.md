- **2026-07-29 (Cleared the 8 high-severity advisories reddening the nightly).** The nightly Test workflow had failed every night since 2026-07-26 on `bun audit --audit-level=high` in the `Production Build` job (E2E only *looked* broken because it declares `needs: build`); patched `fast-uri` 3.1.2→3.1.4, added exact `postcss` 8.5.25 and `sharp` 0.35.3 overrides for versions `next` pins beyond reach, and refreshed `brace-expansion` per major line (1.1.12→1.1.17, 5.0.4→5.0.8), clearing 7 of 8 — the eighth (GHSA-mh99-v99m-4gvg, patched only in 5.0.8 with no 1.x backport, dev-only via three latest-release ESLint plugins pinned to `minimatch` 3.x) is a dated 30-day exception tracked in #653, now pinned by contract tests that reject any `--ignore` lacking a concrete advisory id and a policy record.
