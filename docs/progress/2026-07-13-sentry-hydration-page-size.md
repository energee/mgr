- **2026-07-13 (Sentry MGR-7 — /dashboard hydration error, recurrence traced to an already-merged fix; pattern-scan fix for a live localStorage-during-render bug — branch `sentry-fix/SENTRY-7477285482`).** Investigated the recurring "Hydration Error" on `/dashboard` (`SENTRY-7477285482`). This issue ID has a three-act history: PR #297 fixed a prefill-store `sessionStorage` read happening during render; PR #306 fixed unpinned `toLocaleDateString()`/`toLocaleString()` locale mismatches; the issue kept recurring afterward (last seen `2026-07-11T02:25:44Z`, release `00fbe79`). The sibling issue MGR-6 (`SENTRY-7477285440`) — identical culprit, release, and last-seen minute — was root-caused to `TrendChartLazy`/`BatchActivityHeatmapLazy` using `next/dynamic({ssr:false})` nested inside a genuinely-suspending `Suspense` (`usePeriod()` → `useSearchParams()`), fixed by PR #396 (already merged before this run started). Confirmed `dashboard/page.tsx` and `dashboard/sales/page.tsx` already reflect that fix, so this recurrence needed no further `/dashboard`-specific change. Pattern scan for the same defect class (browser-storage read during render instead of in an effect) found one more live instance: `usePersistedPageSize` (`src/hooks/use-persisted-page-size.ts`), used by `EntityDataTable` on every entity list page, read `localStorage` inside a `useState` lazy initializer — the server renders the default page size while the client's first render reads the stored value whenever a user has changed it, a genuine hydration mismatch. Fixed by deferring the read to `useEffect`, mirroring the established `usePrefillHydration` pattern. New regression test `src/hooks/__tests__/use-persisted-page-size.test.tsx` (confirmed red against the pre-fix lazy-initializer code, green after). `bun run typecheck`, `bun run test` (2295 tests), and `bun lint` all clean.
