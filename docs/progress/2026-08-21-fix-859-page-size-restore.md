- **2026-08-21 (persisted page size is actually restored on reload, #859).** `entity-data-table` kept its own `pagination` state seeded from `usePersistedPageSize`'s first-render value — always the default by SSR-safety design — and never synced back, so the stored rows-per-page choice persisted but was never restored. New `usePersistedPagination` hook derives `pagination` from the persisted size (single source of truth), routes size changes through persistence with a pageIndex snap-back, and the table now consumes it; first paint still uses the default so the server prefetch key parity from the loading audit holds, with the post-hydration refetch covered by `placeholderData`. Tests in `use-persisted-page-size.test.tsx`.
