- **2026-07-15 (Issue #444 review fixes).** Addressed PR #474 review: `completeSyncLog` now logs (not throws) on a sync-log write failure so already-committed reconciliation isn't misreported as failed / halting later phases; `reconcile_mongodb_batch_reading_aggregate` preserves `created_at` on conflict so idempotent retries don't churn the audit timestamp; removed the unreachable client-side failure re-check in the MongoDB sync page (the route already returns non-2xx on failure); documented that recipe name / brew_number adoption is load-bearing first-run migration behavior (adopts pre-migration rows that lack ownership mappings) with its manual-collision tradeoff.
