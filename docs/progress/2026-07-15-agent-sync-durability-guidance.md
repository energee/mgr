- **2026-07-15 (Integrations-expert durability guidance).** Encoded the root-cause class from PRs #474 (MongoDB) and #475 (QuickBooks) into `integrations-expert`'s review checklist: item 5 now separates the primary write (external call + local mapping) from the closing audit-log write, requiring a post-commit `*_sync_log`/`mongodb_sync_log` failure to be logged-not-rethrown (so a succeeded sync isn't reported as failure or used to halt dependent phases); item 7 now requires documenting provider idempotency ceilings in code (request IDs are payload-independent and dedup is time-bounded). Both cite the landed reference implementations.
