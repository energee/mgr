- **2026-07-15 (Inactive-account revocation — issue #441).** Made `user_profiles.status` an enforced authorization boundary across staff/customer layouts, shared API wrappers, permission helpers, and every authenticated RLS table. Pending, inactive, missing, and unreadable profiles fail closed even with an old valid JWT; self-service status/role escalation and direct admin status writes are blocked; user deactivation/reactivation now uses a dedicated per-user database fence with database-first deactivation, Auth-first reactivation, and re-ban compensation. Concurrent commands conflict before Auth, and self-deactivation is rejected. Added real PostgreSQL role tests for active/pending/inactive/missing profiles, customer policies, direct reads/writes, fencing, self-reactivation, role escalation, helper exposure, plus UI/API/orchestration regressions. Migration `00255` is repository-only and must be deployed through the authorized database release process after merge.
