- **2026-07-16 (CI minutes diet).** Actions usage was ~7,000–9,000 billable min/mo vs the 3,000 allowance (measured via job durations across the last 30 days: Test ~4.5k, db-lint ~1.1k, Sentry harness ~0.8k, Claude review ~0.4k). Cuts, targeting ≤3,000 while keeping every gate: Test now runs static+unit only on PRs (unsharded — sharding cost ~5 extra billable min/run in duplicated installs and a coverage-merge job), skips docs-only diffs, and moved build+E2E from every merge to a weekday-nightly schedule (local `make check` builds pre-commit; Vercel builds every deploy); db-lint is PR-only and no longer triggers on `package.json`/`bun.lock` bumps; the automated per-PR Claude review workflow was removed entirely (~360 min/mo — use local `/code-review` before pushing or an on-demand `@claude` PR comment); Sentry harness runs weekdays once (was 2×/day) with max 3 fixes/run and a 45-min job timeout; all Claude jobs got timeout ceilings. Contracts pinned in `.github/scripts/ci-workflows.test.ts`. Dead `VITEST_COVERAGE_SHARD` escape hatch removed from `vitest.config.ts`. Safety note: main has no per-merge CI now — squash-merge drift is caught by the nightly within 24h.
