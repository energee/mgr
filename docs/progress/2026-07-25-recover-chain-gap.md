- **2026-07-25 (Recover migrations 00270/00271 into the committed chain).** `supabase db push` failed with "Remote migration versions not found in local migrations directory" for 00270 and 00271: both are applied to the live database but existed only on `feat/revenue-by-year`, a local-only branch with no upstream (572 ahead / 548 behind `origin/main`) that was never pushed. The CLI's suggested `migration repair --status reverted` would have been wrong — it records a rollback that never happened, leaving the objects live and the chain permanently divergent. Recovered both files verbatim by git object (checksums verified identical to the source blobs) rather than renumbering, since the live `supabase_migrations` history already records those exact versions. `00270_revenue_by_month.sql` adds `get_revenue_by_month()` (`CREATE OR REPLACE`, idempotent; currently orphaned — no committed caller, adopted rather than dropped because removing a live read-only `STABLE` function costs another migration for no gain). `00271_fulfill_past_orders.sql` is the one-time data migration marking ~200 historical XLSX keg orders `fulfilled` per the 2026-07-19 DECISIONS.md reversal of issue #425; it is replay-safe (`CREATE TABLE IF NOT EXISTS`, `ON CONFLICT DO NOTHING`, status-scoped candidates CTE) and writes a `_backup_fulfill_past_orders` rollback preimage, so re-running it on fresh replay or `make db-local` is a no-op. Recovering 00271 into the chain surfaced a second, pre-existing gap that CI caught and the text-based `make check-db` could not: 00255 installed the `current_user_enabled` restrictive gate by looping over the public RLS tables existing *at that moment*, so `_backup_fulfill_past_orders` — created on a branch that diverged before 00255 — has RLS enabled but no gate, failing the "adds an authenticated restrictive gate to every public RLS table" integration test. Added `00280_reapply_enabled_account_gate.sql`, which re-runs 00255's idempotent loop (catching any other straggler created since) and aborts if the invariant still fails. No effective access change — the table has zero permissive policies, so it already denied every non-BYPASSRLS role, and a RESTRICTIVE policy only narrows. Fixed forward in a new migration rather than by amending the already-applied 00271, which would have recreated the very chain-vs-live divergence this branch repairs. Unblocks `scripts/db-push.sh` and the six queued migrations 00274–00279. Related: #584 (live drift), #439.
