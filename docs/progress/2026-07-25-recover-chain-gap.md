- **2026-07-25 (Recover migrations 00270/00271 into the committed chain).** `supabase db push` failed with "Remote migration versions not found in local migrations directory" for 00270 and 00271: both are applied to the live database but existed only on `feat/revenue-by-year`, a local-only branch with no upstream (572 ahead / 548 behind `origin/main`) that was never pushed. The CLI's suggested `migration repair --status reverted` would have been wrong — it records a rollback that never happened, leaving the objects live and the chain permanently divergent. Recovered both files verbatim by git object (checksums verified identical to the source blobs) rather than renumbering, since the live `supabase_migrations` history already records those exact versions. `00270_revenue_by_month.sql` adds `get_revenue_by_month()` (`CREATE OR REPLACE`, idempotent; currently orphaned — no committed caller, adopted rather than dropped because removing a live read-only `STABLE` function costs another migration for no gain). `00271_fulfill_past_orders.sql` is the one-time data migration marking ~200 historical XLSX keg orders `fulfilled` per the 2026-07-19 DECISIONS.md reversal of issue #425; it is replay-safe (`CREATE TABLE IF NOT EXISTS`, `ON CONFLICT DO NOTHING`, status-scoped candidates CTE) and writes a `_backup_fulfill_past_orders` rollback preimage, so re-running it on fresh replay or `make db-local` is a no-op. Unblocks `scripts/db-push.sh` and the six queued migrations 00274–00279. Related: #584 (live drift), #439.
