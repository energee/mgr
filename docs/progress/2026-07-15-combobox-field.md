- **2026-07-15 (searchable dropdowns show preselected values).** Root-caused (confirmed in-browser) the packaging format/batch selects rendering blank for existing line items: Dice UI comboboxes resolve a selected value's label only from registered option items, which register on first open, so an existing selection showed an empty input. Extracted a shared `ComboboxField` primitive (controls input text from a `selectedLabel`) and migrated every affected searchable dropdown to it — packaging FormatCell/keg-owner, orders item editor (replacing the local `CatalogCombobox`), entity-form `RelationCombobox`, PO line items, PO accept-inventory, transfer lines, packaging batch dialog. Separately fixed the packaging batch `Select`: `useBatchesForBrand` only returns active batches, so a line item on an already-packaged batch fell out of options — inject the known batch as a fallback option. Added a `ComboboxField` display-on-mount unit test. `tsc`/lint clean; 117 editor tests + 2 new pass.
