- **2026-08-12 (nightly `bun audit` failure fix, issue #735).** The scheduled Test workflow's `Production Build` job had failed every run from 2026-08-05 through 2026-08-11 on `bun audit --audit-level=high`, reporting 4 high-severity transitive advisories: `undici` (7.28.0, needs >=7.29.0), `fast-uri` (3.1.4, needs >=3.1.5), `nanoid` (3.3.16, needs >=3.3.17), `js-yaml` (4.3.0, needs >=4.3.1) — each exactly one patch release behind. Bumped the two existing `overrides` entries (`undici`, `fast-uri`) and added two new ones (`nanoid`, `js-yaml`) in `package.json`, regenerated `bun.lock` with `bun install`, and confirmed `make check` (lint, typecheck, unit tests, production build) stays green. Recorded the triage in `docs/security/dependency-policy.md` under a new "2026-08-12 high-severity triage" section, following the existing 2026-07-15/2026-07-29 format. Note: `bun audit` itself could not be re-run in the fix environment (its advisory-endpoint network call was blocked there, unlike `bun install`), so the fix was verified by confirming each installed version against the exact minimum patched version `bun audit` printed in the failing nightly run's log rather than by a fresh local audit run.
