- **2026-07-16 (worktree root moved into the repo checkout).** `scripts/agent-worktree` now defaults to `<main-checkout>/.agents/worktrees/` instead of `$HOME/.agents/worktrees` (`AGENT_WORKTREE_ROOT` still overrides). Nested-checkout hazard neutralized by gitignoring `/.agents/worktrees/` and excluding it from `tsconfig.json` and `eslint.config.mjs`. Docs updated: `AGENTS.md`, `DECISIONS.md` (2026-07-14 update entry), worktree-manager skill.
