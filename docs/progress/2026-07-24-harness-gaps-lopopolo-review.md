- **2026-07-24 (harness gaps from the Lopopolo repository review).** Ran `playbooks/repository-review.md` against mgr and closed the four gaps it surfaced. **Zero-to-running:** `make db-local` / `scripts/db-local.sh` boots local Supabase, replays the chain, and loads both fixture sets — verified end-to-end, the integration suite goes 128/128 against it (and fails with a bad `DATABASE_URL`, so the pass is real). Two defects fell out of building it: `seed-roles.sql` needs a superuser for `DISABLE TRIGGER ALL` (script connects as `supabase_admin`), and `supabase/seed.sql` has rotted against the schema (`batch_number`→`batch_code`, `inventory_items.supplier` gone) — demo data is best-effort with a warning, issue #581. **Durable lessons:** `docs/agents/gotchas.md` graduates the environment failure modes that were living in one harness's private memory into the repo, routed from AGENTS.md. **Delivery/authority:** `docs/agents/ci.md` gains a live-apply-and-rollback section (no down migrations; reverse forward, push with `db-push.sh`, re-run the watchdog). **Watchdog is dead:** `live-drift.yml` has not checked anything for days — scheduled runs 07-20→07-24 died on billing, and a manual dispatch got `password authentication failed` from the stale `SUPABASE_DB_URL` secret, so the out-of-band-drift class has been unwatched. Needs a credential rotation: issue #580.
