- **2026-08-11 (codegraph: /code-review fixes, split across two sessions).** All 8 confirmed findings fixed. This session: staleness fingerprint now hashes untracked files; `summarize.ts` passes the loaded fingerprint through `save()` instead of re-stamping a stale graph fresh; `runLlmPass` reports `failed` files (kept out of `reExtracted`, so their cached LLM facts survive), tolerates corrupt cache entries as misses, and writes the cache atomically; `update.ts` counts uncommitted/untracked files as changed; the eval gate fails below 100% F1 and on zero selected cases; `tested_by` resolves nested tests via unique-basename fallback (flow tests with no same-named subject correctly get no edge). Peer session (aa7c44c7): single-scan SQL statement lexer (comments/strings/tagged dollar quotes), `invalidates` guard requiring an enclosing invalidation call, `dynamicRpc` exact-match, `--hops` NaN guard, `isStale`-aware update early-return. Graph rebuilt: 3,827 entities / 8,192 relations (false invalidates and fabricated tested_by candidates removed); evals 100%.
