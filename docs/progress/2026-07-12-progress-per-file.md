- **2026-07-12 (PROGRESS.md moved to per-file entries — branch `docs/progress-per-file`; treadmill closed).** PROGRESS.md is now **generated**: one file per session entry in `docs/progress/YYYY-MM-DD-slug.md` (the bullet verbatim), `scripts/build-progress.sh` concatenates them newest-first over `docs/progress/archive.md` (all pre-2026-07-12 content, frozen), and `.github/workflows/progress.yml` rebuilds + commits PROGRESS.md on main whenever `docs/progress/**` changes. Concurrent PRs now touch different files, so PROGRESS conflicts are structurally impossible — including on GitHub, whose PR merge ignores the `merge=union` attribute (the reason union alone left a re-dirty treadmill: every merged PROGRESS-touching PR re-conflicted all open PRs). The union attribute stays as a safety net for in-flight old-format branches. Convention recorded in CLAUDE.md; PR #392 (old-format sweep entry) folded into the archive and closed. Ceiling: a hand edit to PROGRESS.md that sneaks into main gets clobbered on the next rebuild (recoverable from git history; the header warns loudly).
