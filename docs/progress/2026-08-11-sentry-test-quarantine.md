- **2026-08-11 (sentry-harness: quarantine agent-authored tests from fix-PR CI, #699).** Closed the #668 residual: the agent's required repro test must now be a NEW `src/**/<name>.sentry.test.ts(x)` file (lander rejects every other test path, including edits to existing tests, `e2e/`, and the integration suite), and `vitest.config.ts` excludes `**/*.sentry.test.{ts,tsx}` when the CI head ref is a `sentry-fix/*` branch — so the test runs in the agent's own job, locally, and after review/merge, but never in CI on the unreviewed fix PR. Pinned by contracts in `ci-workflows.test.ts` and `outbox.test.ts`.
