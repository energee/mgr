- **2026-07-13 (worktree hook passthrough + dashboard switcher — PR #402).** Fixed `scripts/agent-worktree create-from-claude`, which read only `.name` from the Claude `WorktreeCreate` payload and so pinned every hook-created worktree to `agent/<name>`; a later `create <name> --branch <requested>` then failed the strict reuse check and the worktree had to be removed to recover. It now forwards `branch` and `base` when present, with regression coverage in `scripts/__tests__/agent-worktree.test.sh`. Completed the `verify` skill migration into canonical `.agents/skills/` with a `.claude/skills` compatibility symlink, so all six skills are consistent under `check-agent-config.sh`. Also landed the dashboard switcher pills (Production / Inventory / Sales), now that the nav has a single Dashboard entry, plus an `aria-label` on the MGR logo svg, which had `role="img"` with no accessible name. `make check` is green except for four pre-existing SECURITY DEFINER justification failures in untouched migrations (00190, 00219, 00232, 00241) that fail on `main` too. Note: the `check-db` scripts exit 1 with no output under the Claude command sandbox — a false negative; they pass unsandboxed.
