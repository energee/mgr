- **2026-07-16 (test import diet).** Cut full `vitest run` wall time ~48% (87.9s → 45.6s locally; aggregate worker time: environment 293s → 75s, setup 33s → 9s, import 355s → 251s) with zero test changes: (1) split the suite into `node`/`jsdom` projects in `vitest.config.ts` — pure-logic dirs (`src/lib`, `src/domain`, `src/services`, `src/integrations`, `src/entities`, `src/__tests__`, `src/app/api`, `.github/scripts`) no longer pay per-file jsdom instantiation, per-file `@vitest-environment` docblocks still override; (2) switched `pool` from forks to worker threads; (3) `src/test/setup.ts` now loads `@testing-library/jest-dom` only when a DOM exists. Coverage thresholds untouched and verified still enforced (negative control: raising a per-dir floor fails the run), and the CI shard + `--merge-reports` blob path reproduces identical merged coverage.
