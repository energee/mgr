- **2026-07-13 (backend audit → security fix → Tier 1 extraction).** Audited whether the backend is a sound foundation for a frontend rebuild (the frontend is 60% of the source and has usability problems). Verdict: the database and entity layers are solid — 39 entities on a clean triad, `query-keys.ts`, `transition-side-effects.ts`, and all of `src/domain/` are reusable — but business logic is stranded inside React (211 raw `.from()` calls in `src/components/`, 223 in `src/app/`, vs 60 in `src/services/`; ~1,700–2,000 lines of rules living in components and hooks). Found and fixed a real security hole: `dispatch_email_notification` (00190) was `SECURITY DEFINER` with no `REVOKE`/`GRANT`, so **any authenticated user — including customer-portal accounts — could send system-branded email with an arbitrary body and link to any other user** (PR #410, migration 00245 revokes from PUBLIC/anon/authenticated, grants to service_role). The other three SECURITY DEFINER check failures were linter false positives (the checker greps for a literal token on the line immediately above `CREATE`). Also backfilled the `_schema_registry` rows and `docs/data-model/` sections the 00199/00222 capture migrations skipped, so **all eight DB checks and `make check` now pass for the first time**. Then executed Tier 1 of `docs/plans/backend-extraction.md` (PR #411): moved eight already-React-free modules out of `src/components/domain/` into `src/domain/`, writing 48 characterization tests against the old code first for the three that had none. Those tests surfaced five latent bugs, locked in as-is rather than fixed mid-refactor — notably `isDuplicateTransfer` treats a future timestamp as a duplicate (clock skew blocks a legitimate transfer) and treats an unparseable date as *not* a duplicate. Suite 2282→2339. Tiers 2–3 (PO-receiving status rules, tier pricing, material-planning and packaging hooks) are planned and pending. **00245 is not yet applied to production — the RPC stays PUBLIC-callable until `scripts/db-push.sh` runs.**
