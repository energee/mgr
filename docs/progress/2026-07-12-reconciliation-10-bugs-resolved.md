- **2026-07-12 (#10 tracked live-behind bugs — all already resolved; doc PR #391).** Preflight caught that all 5 tracked function bugs from `docs/plans/2026-06-30-migration-reconciliation-10.md` were fixed by concurrent sessions and merged (00075/00142/00150/00208/00214, PRs #346/#354). No code written; tracking doc + memory corrected, PR #391 (branch `docs/reconciliation-10-bugs-resolved`). Residual: verify 00208 applied to live DB (Supabase MCP was disconnected).
- **2026-07-12 (PROGRESS.md merge conflicts fixed — branch `chore/progress-union-merge`).** New `.gitattributes` sets `PROGRESS.md merge=union`: concurrent branches prepending entries here no longer conflict — git keeps both sides. Ceilings (documented in the file): same-line edits to OLD entries on two branches are silently kept twice (dedupe by eye), and GitHub's web PR merge may still report a conflict — merge main into the branch locally, where the attribute applies. Gates: lint + typecheck + test 2130 green.
- **2026-07-12 (Mobile-first UX simplification — design spec committed to main, `5511d574`; implementation NOT started).** Brainstormed + user-approved design in `docs/superpowers/specs/2026-07-12-mobile-ux-simplification-design.md`: nav restructure (7 collapsible sections/36 links → 2 direct links + 4 always-open sections/~26 links; Dashboard + Reports become direct links with in-page switchers; Packaging merges into Production), mobile bottom tab bar (Batches/Inventory/Picks/More — More reuses the shadcn sidebar mobile sheet), shared-primitives mobile pass (`ui/dialog.tsx` → bottom sheet on max-sm covers all ~28 domain dialogs; `entity-detail-unified.tsx` mobile layout covers every detail page; ≥44px touch targets), plus 3 targeted floor screens (pick lists, packaging day view, PO receiving). Personas: brewhouse + warehouse staff. No new routes/deps; URLs unchanged. Implementation plan committed (`f2f39068`): `docs/superpowers/plans/2026-07-12-mobile-ux-simplification.md` — 7 tasks (nav+consumers / dashboard switcher / tab bar+shell / dialog bottom-sheet+input sizing / detail-page mobile / measure-first floor screens / verify+PR), TDD steps with full code, worktree branch `feat/mobile-ux-simplification`. Two spec items found already satisfied: Planning page already links Backward Planning; relation tables already scroll via ui/table's overflow wrapper. **Execution (subagent-driven) PHASE 1 DONE in worktree `.claude/worktrees/mobile-ux`:** Tasks 1-3 implemented + reviewer-approved (nav restructure `6140c0c`, dashboard switcher `c660348`, mobile tab bar `5090478`+`a15fd93`; suite 2139 green, lint/typecheck clean; SDD ledger at worktree `.superpowers/sdd/progress.md`, git-ignored). Paused at cost checkpoint (~$20, user guardrail). Next: Tasks 4-7 (dialog bottom sheets + input sizing, detail-page mobile pass, measure-first floor screens, final verify + whole-branch review + PR). Nothing pushed.
