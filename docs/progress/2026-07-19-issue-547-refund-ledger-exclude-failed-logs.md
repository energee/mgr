- **2026-07-19 (refund ledger excludes all failed logs, #547).** Rebased PR #556 onto main after #553 shipped the `manual_reconcile` quarantine (00267). Added migration `00268_refund_ledger_exclude_failed_logs.sql`: broadens the `ingest_square_refund_atomic` prior-refund aggregation to `items_failed = 0`, closing a residual poison-pill 00267 left open — pre-atomic v1 (00257) unsizeable failure logs and pre-00267 v2 mismatch rows carry a `refund_amount`/divergent `order_total` but no `manual_reconcile` flag, so they still phantom-inflated prior money and poisoned the mismatch check. New regression test in `square-atomic-ingestion.test.ts` inserts an unflagged completed `items_failed = 1` log and asserts a later consistent refund still auto-processes with `prior_refund_amount: 0`. Docs updated (DECISIONS, spec DEC-INT-001, sales.md).
