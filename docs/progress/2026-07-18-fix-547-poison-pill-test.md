- **2026-07-18 (00262 order-total-mismatch poison pill: regression test + automatic recovery, #547).** Real-Postgres regression proved the 00262 mismatch branch permanently blocked all later automatic refund reversals for an order (the failed log's divergent `order_total` re-tripped the check forever). Migration `00267` quarantines the mismatch-failure log as `manual_reconcile: true`, excluding it from future sizing — later refunds with consistent totals now recover automatically, while the mismatched refund itself stays a sealed manual-reconcile audit record. Verified red→green against a CI-equivalent Postgres 15 migration replay; full integration suite (118), unit suite (2510), tsc, lint, and check-db all green locally (CI billing-blocked).
