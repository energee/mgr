- **2026-07-30 (Correct #655's egress claim and close the contract's vacuity holes).** #655 (merged as 80853cfc) shipped a comment in `sentry-harness.yml` asserting that "an injected instruction with no outbound network tool has no path off the runner" — that is false, and `docs/agents/ci.md` had encoded the same overstatement as a repo-wide rule ("an outbound network tool is what turns a prompt injection into exfiltration"). The same job still grants `Bash(git:*)`, `Bash(bun:*)`, `Bash(bunx:*)`, `Bash(gh issue:*)` and checks out without `persist-credentials: false`, so `git push`/`git ls-remote` to an attacker URL, `bun -e 'fetch(...)'`, `bunx <pkg>`, and (public repo) `gh issue create` all remain — #645's stated impact, exfiltrating the persisted push token, was never closed. Rewrote both to say what denying `WebFetch`/`WebSearch` actually buys (removes the only single-tool-call egress channel; makes exfiltration require conspicuous shell work that shows in the job log) and to list what stays open, with the residual tracked in #668 rather than papered over; narrowing the allowlist was rejected as a fix because the job must `git push` and open issues regardless, and its documented failure mode is silent breakage from tool-permission denials. Also rewrote the contract in `.github/scripts/ci-workflows.test.ts`, which an adversarial review showed could pass while checking **nothing**: reimplementing its logic against synthetic workflows gave `checkedJobs=0, zero failures` for a job written `contents: "write"` (the write-scope regex only matched a bare scalar), for `permissions: write-all`, for a job whose only write scopes were `actions`/`packages` or whose credential arrived as a PAT in `env:`, and for a job key carrying a trailing comment or YAML anchor (the hand-rolled line splitter discarded the whole job body); a correct unquoted `--disallowedTools` meanwhile failed with "passes no --disallowedTools". Replaced the text matching with a real YAML parse plus shell-style argument tokenising in a new `.github/scripts/workflow-egress.ts` (`yaml` promoted to a direct devDependency), and dropped the credential branch entirely — *every* job invoking `claude-code-action` must now deny both tools, with a `web-egress: allowed (job: <name>) — <rationale>` escape hatch mirroring `durable-state: exempt`, so there is no scope list left to get wrong. Replaced the `checkedJobs >= 5` floor (which could not detect a newly added workflow being skipped, since the known jobs already met it) with three non-vacuity guarantees: a file naming the action but parsing to no agent job fails, unparseable YAML fails, and each real generative workflow is mutation-tested — remove whatever makes it pass (its denial, or its exemption marker) and the audit must go red. 43 contract tests, each previously-slipping shape pinned as a case. On `claude.yml` — which #655 had also changed, out of scope for #645, presenting its threat model as equivalent to sentry-harness's when it is materially weaker (`persist-credentials: false` is set, the action authenticates its own writes, runs are insider-gated and attended) — #661 independently dropped that denial hours later while granting the job a scoped Bash allowlist, so the file now carries an explicit `web-egress: allowed (job: claude)` marker recording why that is defensible: `Bash(bunx:*)`/`Bash(bun run:*)`/`Bash(make:*)` already reach the network, so denying the two web tools beside them would be the very false posture this change documents. #669 owns whether to tighten it. It is the repo's only exemption and the contract enumerates it, so a second one cannot appear silently.
