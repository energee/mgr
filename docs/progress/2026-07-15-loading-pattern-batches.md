- **2026-07-15 (Loading pattern — batches reference).** Phase 0 of the sitewide loading pattern (`docs/plans/2026-07-15-sitewide-loading-pattern.md`): extracted a shared `listQueryOptions`/`runListQuery`/`defaultListParams` factory (`src/components/universal/list-query-options.ts`) from `entity-data-table.tsx` so the client `useQuery` and a server prefetch build the *same* React Query key + fetch. Converted `/production/batches` to a server component that prefetches the initial paged query and hydrates `<BatchesClient/>` (route `loading.tsx` renders `ListSkeleton`), so only one skeleton shows and rows are present on first paint. Verified live: rows hydrated on first paint, search/refetch works with no 4xx/5xx, no new hydration warning (the `RedirectErrorBoundary` attribute mismatch is pre-existing and app-wide). **Design constraint found for Phases 1–4:** assembled entity configs (`batchEntity`) are *client* modules (presentation.tsx renders JSX), so a Server Component can't import them — the page prefetches from the server-safe `batchCore` via a minimal `ListQueryEntity` type. Entities whose *list* columns include a `relation` still need that FK metadata server-side; batches have none. `make check` green.
