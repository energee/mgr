- **2026-07-18 (issue #437 slice: workflow E2E + live-Postgres behavioral coverage).** Implemented 2 of the skipped E2E workflow scaffolds against the local Supabase stack — full recipe-editing flow (create → rename → grain bill → live OG/FG/ABV estimates → reload persistence) and full batch-transfer flow (planned batch → Transfer dialog → suggested fermenting transition → vessels_with_batch occupancy + vessel_transfers row) — backed by a new service-role seed helper (`e2e/seed.ts`, per-spec deterministic UUID namespaces, delete-and-recreate idempotent reset). The packaging-session E2E flow stays skipped with a precise tracked reason: the replayed migration chain still enforces the 00080 `chk_sli_format_xor`/`chk_fg_format_xor` constraints that live has dropped (uncaptured drift), so the UI's selling_format-only line-item inserts are rejected locally; its DB semantics are covered instead by a new live-Postgres suite `src/__tests__/integration/packaging-completion-trigger.test.ts` (00232 completion trigger: per-line finished goods + allocations + YYYYMMDD-NNN lots, injected-failure no-partial-state, zero-line-item guard, revise below-committed rejection). Also added `src/__tests__/integration/inventory-guards.test.ts` (00212 allocation availability guard: oversell rejected, adjustment exempt, cancelled ignored; 00216 finished-good outbound guard: below-committed reduction rejected, boundary + increases allowed). Remaining #437 skips (portal customer-order flow, dashboard heatmap/trends, full production chain) annotated with tracked reasons.
