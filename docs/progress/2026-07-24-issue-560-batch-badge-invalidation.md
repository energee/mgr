- **2026-07-24 (fix #560: stale batch status badge after transition).** Batch mutations invalidated only `batchKeys.detail(id)` while the unified detail header caches under the `batches_with_brew_info` view key — so the status badge (and vessel info) stayed stale until reload. Added `batchRecordInvalidationKeys(id)` to `src/lib/query-keys.ts`, applied at all six batch-detail invalidation sites, and fixed the same miss in the brew-logs detail page (`brew_logs_with_batches`). Coverage pinned in `src/lib/__tests__/query-keys.test.ts`; `docs/agents/query-keys.md` documents the viewTable-invalidation rule.
