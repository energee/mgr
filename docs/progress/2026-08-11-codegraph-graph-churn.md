- **2026-08-11 (codegraph: kill graph.json rebuild churn; carry LLM edges + profiles).** Per-edge `commit` stamps made every rebuild rewrite all ~17k lines of the committed `graph.json` (guaranteed merge conflict for parallel branches, since `query.ts` auto-refreshes); the commit now lives only at the top level and a same-state rebuild diffs exactly one line (`built_at`). Along the way found and fixed a worse bug the churn was hiding: no rebuild path carried LLM edges or `summarize.ts` hub profiles, so `build.ts`/`update.ts` silently deleted them — the committed graph had ZERO LLM edges. New `llmPart`/`carryProfiles` helpers in `store.ts` are wired into build, update, and query auto-refresh; LLM edges (92) regenerated from cache and all 46 profiles preserved.
