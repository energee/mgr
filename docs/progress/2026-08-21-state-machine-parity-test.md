- **2026-08-21 (TS↔DB state-machine parity test — branch `test/state-machine-parity`; audit TC-6, backlog P3 #22).** A parity block already existed in `src/lib/__tests__/state-machines.test.ts`, but it was pinned to the filename `00143_server_side_state_machine.sql` — the **first** of three `CREATE OR REPLACE FUNCTION get_state_transitions()` definitions. `00167_fix_remaining_batch_number_refs.sql` and `00205_restore_server_side_enforcement.sql` both replace it later, so the suite was asserting against a function body Postgres has not run since 00167; it passed while comparing TypeScript to a two-generations-stale map. That is the same class of blind spot as the 'revised' outage the item was filed for, so the fix is the resolution strategy, not the assertion: `findWinningDefinition()` now scans every `supabase/migrations/*.sql` in apply order (zero-padded names sort numerically) and takes the **last** definition, in-file last-match included, failing loudly if none is found. A regression test asserts more than one migration defines the function and that the one selected is the highest — re-pinning to a single filename now breaks the build. Coverage additions on top of the re-point: `deliveries` → `deliveryEntity` was mapped (00205 carries a `deliveries` arm that nothing compared, though the table has no validation trigger yet); every SQL table must now resolve to a TypeScript entity; and every entity with a `stateMachine` must be either DB-enforced or named in `TS_ONLY_STATEFUL_ENTITIES` (`location-transfer`, `user-profile`, `vessel`, `yeast-pitch`), so a new stateful entity forces a deliberate call on server-side enforcement instead of defaulting to client-only. Comparison logic was extracted into `diffTransitionMaps()` and given a self-check test that feeds it a map with `completed: ["revised"]` dropped and asserts both the missing state and the shortened target list surface — proving the comparison has teeth without mutating production code. Missing-SQL-map cases changed from `it.skip` (silent pass) to an explicit failure. **No real TS↔SQL mismatch exists today**: all nine mapped tables agree exactly with the winning 00205 map. Tests in this file 125 → 141 (+16); suite-wide 3064 → 3080, 232 files all passing; `make check` green. Tests only — no production code touched. Two adjacent hazards noted but not fixed here: 00167 is a *partial* redefinition that drops `brew_logs`/`allocations`/`pick_lists`/`recipes` (restored by 00205), and `supabase/migrations/` now holds two files numbered `00298` (`constraint_tightening`, `sync_status_preserving_upserts`), which makes apply order between them ambiguous.
