- **2026-07-25 (API route hygiene: 204 responses, swallowed sync-log errors, bare status flips).** Fixed #609 by short-circuiting null-body statuses (204/205/304) in `successResponse`, so `DELETE /api/batches/[id]` and `DELETE /api/recipes/[id]` stop returning 500 with a leaked `TypeError` after successfully deleting the row; fixed #611 by making the QuickBooks sync-log route return a logged `500 DB_ERROR` like its siblings instead of `200 {"logs":[],"total":0}`, with `res.ok` guards and distinct error states in both consumers so a failed read no longer renders as "Not Synced" / "No sync activity yet"; fixed #601 by stripping `status` from `PATCH /api/batches/[id]` (400 pointing at `POST /api/batches/[id]/transfer`) and widening the `transition-call-sites` CI detector to flag the `.update(<validated partial>)` form it previously missed, now chain-scoped so a route that only reads another table is not misattributed. The red tests also exposed a second defect the issues did not name: Zod `.default()` survives `.partial()`, so every PATCH was silently writing `batches.status = "planned"`, `recipes.status = "draft"`, and `recipes.is_active = true` — both routes now use explicit `batchUpdateSchema` / `recipeUpdateSchema`.
