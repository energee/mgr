- **2026-07-11 (Portal email-case lockout — branch `fix/portal-email-case`, backlog P2 #17, audit DL-6; app-code only, NO migration).** Portal auto-link (`src/app/portal/(main)/layout.tsx`) matched `customers.email` with exact `.eq()` while migration 00201 assigns the `customer` role via `lower()=lower()` — a customer stored with a mixed-case email (auth emails are lowercased) got role-locked out of the staff app AND an empty portal. Now `.ilike()` with new `escapeIlikePattern()` (`src/lib/supabase/query-helpers.ts`; escapes `\ % _` plus PostgREST's `*`→`%` alias so wildcards fail closed) + JS-side exact lower() re-verification before linking. Same-class sweep fixed: `api/users/invite` duplicate pre-check (case-variant duplicate fell through to inviteUserByEmail's opaque error) and dev `confirm-user`'s `===` find (+ non-string body guard); `api/auth/dev-login` compares a lowercase constant — fine. Tests +18 (4 files: escape-helper unit; portal-layout auto-link with an eq/ilike-semantics-honoring customers fake — mixed-case link, exact-case, no-wildcard-link, existing-link skip, login redirect; invite-route conflict-before-email pins; confirm-user route). Shared `src/test/supabase-admin-mock.ts` gained `ilike` + exported `ilikePatternToRegex`. Red-check verified (pre-fix `.eq` fails exactly the lockout test). — branch `fix/catalog-map-upsert`, backlog P0 #4, audit SF-4/IN-8/PERF-1 + SF-6/IN-11; migration `00236` NOT applied live).** Commit `b40c3f66`. `pushCatalog` persisted `square_catalog_map` rows via per-row INSERT-then-blind-UPDATE — the fallback UPDATE's error was never checked, so a failed write silently dropped the mapping and the next sync pushed temp `#brand-…` ids (full duplicate Square catalog); the 2N sequential writes were also the push's 6-12s latency tail. Now: ONE batched, error-checked `.upsert()` with `onConflict: "brand_id,object_type,selling_format_id"`; on failure every row in the batch is reported failed and none count in `itemsSynced`. **`00236_square_catalog_map_unique.sql`** de-duplicates existing rows (keep newest by `updated_at, created_at, id`) then adds `UNIQUE (brand_id, object_type, selling_format_id) NULLS NOT DISTINCT` — plain-column (not partial/COALESCE-expression) because PostgREST `on_conflict` can only infer an arbiter from a bare column list, and NULLS NOT DISTINCT (PG15+; CI replays on postgres:15) makes ITEM rows' NULL `selling_format_id` collide; self-rolling-back DO block proves the PostgREST-shaped upsert infers the index (00091's legacy partial indexes left untouched — they never block the new upsert). Also: `deleteStaleItems` surfaces the initial SELECT's error as a `chunk: -1` entry in `DeleteStaleResult.errors` (was folded into the silent empty-result return; deletion stays fail-safely skipped) and the catalog route's `success` flag now accounts for it. Tests +3 (pushCatalog batch success/failure accounting, stale-SELECT error) in `catalog.test.ts`. Gates: lint + typecheck + **test 2081** green. **Deploy = `scripts/db-push.sh` after merge.**
