- **2026-07-15 (loading pattern: detail template + relation-column list).** Generalized the sitewide server-prefetch/hydrate pattern past the batches-list reference so the mechanical Phases 1–4 can proceed. Detail pages: added a server-safe `detailQueryOptions` factory (`src/components/universal/detail-query-options.ts`) mirroring `useEntityRecord`'s key + `*` select; refactored `useEntityRecord` to consume it so client + server keys stay identical. Relation-column lists: added a server-safe `listRelations` field to `EntityCore` (mirrors the FK relation columns that live in the client `presentation.listColumns`) and made `runListQuery` fall back to it, plus a `select` override on `defaultListParams` so a page whose client list renders custom cells (buildSelectList → `*`) can match its first-render key. Converted two copyable templates end-to-end: **batch detail** (`/production/batches/[id]` → server `page.tsx` prefetches the detail query + hydrates a new `batch-detail-client.tsx`, `loading.tsx` renders `<DetailSkeleton>`) and **brands list** (`/settings/brands`, first relation-column list — `style_id`→`beer_style.name` resolved server-side). Browser-verified: brands STYLE cells render on first paint (no reflash); batch detail data present on first paint with the `useEntityRecord` query served from the hydrated cache (no client refetch); only the pre-existing app-wide RedirectErrorBoundary hydration mismatch. `make check` green.
