- **2026-07-16 (Sentry harness dedup + worktree-artifact pre-filter, #515).** The harness re-triaged the same ~6 dead Sentry issues 16 times because dedup only recognized merged PRs. Added `filterTriagedIssues` (suppresses issues with an existing `[sentry] <shortId>:` GitHub issue — open, or closed with no events since), `isWorktreeArtifact` (drops dev HMR noise from `_agents_worktrees_`/`_claude_worktrees_` chunk paths, pre-scoring and post-enrichment), and a step-0 prompt rule to comment on existing issues instead of filing duplicates when triage is (B)/(C) or the culprit is deleted on main.
