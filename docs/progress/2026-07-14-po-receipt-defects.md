- **2026-07-14 (fix four of the five PO-receipt defects from #412).** The Tier-2.1 extraction (#412) pinned five known defects with characterization tests rather than change behavior mid-refactor. Fixed four, flipping each quirk test to assert correct behavior first: an empty line list no longer flips a purchase order to `fulfilled` (`[].every()` is `true`, so a PO with no lines read as fully received); a null or zero ordered quantity no longer counts as received (`0 >= null` is `true`); the received-vs-ordered comparison carries `RECEIPT_EPSILON` (1e-6), so a line ordered 0.8 and received 0.7 + 0.1 — which sums to 0.7999999999999999 — no longer stays `partial` forever; and over-receipt is rejected server-side by a new `findOverReceipts`, checked in `receivePurchaseOrderItems` **before** any row is written (previously the only guard was a UI input clamp, so any non-UI caller could record 999 against 10 ordered and silently close the order). The governing principle: `fulfilled` is financially meaningful and hard to undo, so the rule degrades to `partial` on absent or nonsensical data rather than closing an order on it. Defect #5 (the write path is not atomic — receives are inserted before the status is read and validated, with no rollback) is **still open** and needs a Postgres function; the front-loaded validation narrows the window but does not close it. Suite 2382 passing / 158 files.
