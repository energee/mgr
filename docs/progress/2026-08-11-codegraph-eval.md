- **2026-08-11 (codegraph: AST eval coverage, CI gate, hub profiles).** Added the first AST gold case (`src/domain/yeast-lineage.ts` — 4 edge kinds, provenance-scored) plus the missing `ast` branch in `eval.ts`; all 4 cases 100% P/R. Wired the deterministic evals (`--ast`, `--sql`) into `test.yml`'s unit-tests job, contract-pinned. Profiled all 46 domain hubs via `summarize.ts`. Fixed `query.ts` dropping the first query word when `--hops` was absent. The handoff's "stale `brews` reference" claim did **not** reproduce — the only `.from("brews")` in repo history is a synthetic template-literal fixture in `transition-call-sites.test.ts`; README Known gaps corrected instead of filing an issue.
