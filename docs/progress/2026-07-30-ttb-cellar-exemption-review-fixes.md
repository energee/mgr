- **2026-07-30 (TTB cellar-exemption review fixes).** Cleaned up the five rough edges an adversarial review logged against PR #652: dropped the "(issue #618)" tracker reference out of the exemption reason rendered on the filing-prep screen (it now lives in the `IDENTITY_EXEMPT_TAX_CLASSES` docblock), fixed the stale "every row is run through the identities" docblock on `reports/ttb/page.tsx`, extracted the snapshot caveat + "not accounting-identity checked" disclosure into `src/components/reports/ttb-report-caveats.tsx` so the legacy `get_ttb_report`-unavailable fallback card carries the explanation and not just the honest label, added both notes as trailing NOTE rows to the CSV export (via a now-unit-testable `buildTTBReportCSV`) so the artifact most likely to be filed is as honest as the screen, and inverted the identity scoping to an enumerated exemption list (`cellar`) so an unknown future tax class defaults to being checked and shouting rather than quietly excused; filed #670 for the pre-existing Totals column that mixes cellar brewed volume with keg/bottled packaged volume.
