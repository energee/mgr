- **2026-07-24 (fix #559: drift-capture migration 00269 for legacy packaging xor constraints).** Live dropped 00080's `chk_sli_format_xor` / `chk_fg_format_xor` (and `chk_sli_keg_owner`, which rejects modern selling_format keg lines carrying `keg_owner_id`) out-of-band, but fresh replays still enforced them. Migration `00269_drop_legacy_format_xor_constraints.sql` captures the drop (no-op live); validated by a full local `supabase db reset` replay. Removed the pg_temp stamping shim from `packaging-completion-trigger.test.ts` (suite green de-shimmed) and updated the packaging-session E2E skip reason — the flow is now scaffold-blocked only (#437).
