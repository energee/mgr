- **2026-07-30 (data-integrity findings now reach someone).** Migration 00281 wires `data_integrity_findings` (written nightly by 00272/00273's `check_data_integrity()` and read by nothing) into the existing notification path via a new `notify_data_integrity_findings()` + pg_cron job at 05:45 UTC, using a new `notified_at` column so an open finding alerts active staff exactly once per occurrence instead of every night. The migration's self-check probes cap+1 findings so both the per-finding and the over-cap summary statement plan, and masks real open findings first so applying it alerts nobody. 00281 is **not applied** — an operator must run `scripts/db-push.sh`. Issue #586.
