- **2026-07-30 (Claude Action can run repo tooling).** The `@claude` workflow shipped with no `--allowed-tools`, so the action's Bash tool was capped at its tiny default allowlist — on PR #648 Claude diagnosed the frozen-lockfile failure correctly but could not run `bun install` to fix it and handed the work back to a human. Added a scoped `claude_args` allowlist (bun install/run, bunx, make, read-only git/gh) to `.github/workflows/claude.yml`; commits and pushes still go through claude-code-action's own git plumbing. PR #648's `bun.lock` was regenerated and pushed separately.
