- **2026-07-11 (UI silent-failure sweep — branch `fix/ui-silent-failures`, backlog P2 #18: UI-6, UI-7=SF-8, UI-8, UI-9, UI-10=SF-10, IN-12).** App-only, no migrations. **UI-6:** PO line-items editor now parses qty/price through the shared line-item parser — new `parsePoItemFieldEdit` in `order-item-edit-utils.ts` (price semantics identical to order items: $0 is a real price, the old `parseFloat(...) || null` stored it as NULL ≡ unpriced; quantities are positive decimals, POs order 55.5 lb); existing-row edits adopt the buffered blur/Enter commit pattern (was an unvalidated write per keystroke), add row holds raw strings parsed on Add (blank price = unpriced NULL, invalid input toasts instead of silently coercing). The ORDER editor's own add-path `|| null` (M10) is deliberately NOT one-line-fixed here — its `unit_price: 0` state is ambiguous between "untouched" and "explicit $0" until the auto-price effect gets a touched flag; stays tracked by 2026-07-06 backlog #17 (+ its DB CHECK, entity-architect lane). **UI-7=SF-8 + IN-12:** onError toasts on the Square enable toggle (settings/integrations page) and the channel-format toggle (`format-management.tsx`); `query-client.ts` now installs a **global `MutationCache.onError` fallback toast** (message via `parseUnknownError`) for any mutation without its own `onError` — this class of silent mutation dies by default; opt-out for mutateAsync-try/catch call sites via `meta.suppressGlobalErrorToast` (used once: brew-log-timeline, whose call sites rollback+toast). IN-12: the toggle POST now uses new `updateSquareSettingsOrThrow` (log-only `updateSquareSettings` stays for post-sync timestamp bookkeeping) so a failed enable/disable write is a 5xx, not a silent-success 200. **UI-8:** pick-list brand/format/bin lookups throw on error (were `{data:[]}`-masked → picker walked the warehouse with no bin guidance) + error state with Try Again. **UI-9:** FG detail Location Breakdown/Commitments render failed reads as destructive error + retry, not "Not assigned to any bins"/"No commitments". **UI-10=SF-10:** Square status read failure now renders a new `error`/"Status Unavailable" IntegrationBadge state + retry banner instead of "Not Connected" (no more credential re-entry prompts on a 500); `getSquareSettings` logs both read errors before falling through to null/disabled. Tests +12 (parser twin, global fallback incl. Postgres-shaped + opt-out paths, format-toggle onError wiring + switch→variables). backlog P0 #4, audit SF-4/IN-8/PERF-1 + SF-6/IN-11; migration `00236` NOT applied live).** Commit `b40c3f66`. `pushCatalog` persisted `square_catalog_map` rows via per-row INSERT-then-blind-UPDATE — the fallback UPDATE's error was never checked, so a failed write silently dropped the mapping and the next sync pushed temp `#brand-…` ids (full duplicate Square catalog); the 2N sequential writes were also the push's 6-12s latency tail. Now: ONE batched, error-checked `.upsert()` with `onConflict: "brand_id,object_type,selling_format_id"`; on failure every row in the batch is reported failed and none count in `itemsSynced`. **`00236_square_catalog_map_unique.sql`** de-duplicates existing rows (keep newest by `updated_at, created_at, id`) then adds `UNIQUE (brand_id, object_type, selling_format_id) NULLS NOT DISTINCT` — plain-column (not partial/COALESCE-expression) because PostgREST `on_conflict` can only infer an arbiter from a bare column list, and NULLS NOT DISTINCT (PG15+; CI replays on postgres:15) makes ITEM rows' NULL `selling_format_id` collide; self-rolling-back DO block proves the PostgREST-shaped upsert infers the index (00091's legacy partial indexes left untouched — they never block the new upsert). Also: `deleteStaleItems` surfaces the initial SELECT's error as a `chunk: -1` entry in `DeleteStaleResult.errors` (was folded into the silent empty-result return; deletion stays fail-safely skipped) and the catalog route's `success` flag now accounts for it. Tests +3 (pushCatalog batch success/failure accounting, stale-SELECT error) in `catalog.test.ts`. Gates: lint + typecheck + **test 2081** green. **Deploy = `scripts/db-push.sh` after merge.**
