- **2026-07-15 (nightly local-Supabase E2E, reworked 2026-07-16).** The hosted-credential E2E secrets were never configured, so the browser suite silently skipped and never actually ran. The E2E job now boots an isolated local Supabase stack (start → status env export → run → always tear down), needs no production credentials, and fails visibly when setup is broken. Originally written as a per-PR gate (#506); reworked onto the weekday-nightly schedule after #522's CI-minutes diet removed E2E from the PR lane. The Playwright readiness probe uses `/api/health` with a CI cold-start allowance so it confirms both Next.js and the local database without compiling the full root page first.
