- **2026-08-21 (Square draft-sale reconciliation exactly-once, #834).** Migration 00293 adds `reconcile_square_draft_sale_atomic`: key-check + allocation insert + reconciled_at stamp in one transaction, serialized per sale by an advisory xact lock (00257 pattern) — two concurrent reconcile runs can no longer double-allocate a sale into duplicate TTB removals. FIFO planning stays app-side; guard_allocation_availability still rejects stale plans atomically, and the function re-checks voided_at in-transaction. Route rewritten to a single RPC call per sale; 8 route tests updated to the RPC contract; 3 real-Postgres integration tests prove dedupe, void, and guard-rollback. 00293 applied LIVE via `scripts/db-push.sh` same day (snapshot regenerated, one FUNC line added; the #812 exit-4 gate fix let the push proceed without `--rebaseline-drift`).
