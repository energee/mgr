- **2026-08-11 (scope gh api grants, #736).** Narrowed the unscoped `Bash(gh api:*)` allowlist entry in feedback-distill.yml to `Bash(gh api -X GET:*)` (its prompt only reads review comments) and removed it from quality-regrade.yml (its prompt never uses `gh api`), so the write-capable App token can no longer reach merge/Contents endpoints; added a ci-workflows.test.ts contract asserting every workflow's `gh api` grant pins the method to GET.
