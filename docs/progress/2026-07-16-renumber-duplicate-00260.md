- **2026-07-16 (renumber duplicate migration 00260).** Two parallel PRs both claimed version 00260 (`atomic_yeast_pitch` and `durable_ai_chat_rate_limit`); the psql replay gate tolerated it but the new `supabase-reset` CI job (real Supabase CLI, unique version tracking) fails every migration-touching PR with a `schema_migrations_pkey` duplicate-key error. Renamed `00260_durable_ai_chat_rate_limit.sql` → `00265_...` (next free slot; 00261–00264 neither redefine nor depend on its objects). Live DB note: verify `supabase migration list` — if live recorded the rate-limit migration as 00260, a `supabase migration repair` may be needed to re-key it as 00265.
