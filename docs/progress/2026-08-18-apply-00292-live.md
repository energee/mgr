- **2026-08-18 (00292 applied live — portal product picker unblocked, #818).** Applied `00292_portal_available_products.sql` to production via `scripts/db-push.sh --rebaseline-drift` (the flag needed only because the pre-push gate counts the migration being applied as drift, #812; the catalog itself matched the snapshot). Verified by reading live directly, not the push output: `supabase_migrations.schema_migrations` carries `00292`; `get_portal_available_products` is `SECURITY DEFINER` with `search_path=public`; EXECUTE is held by `authenticated`/`service_role`/`postgres` only — no `anon`, no `PUBLIC`. Snapshot diff is exactly one added line (the new function's hash). F114 `deployment` flipped `pending` → `live` and its stale LIVE-BLOCKED note rewritten; `make check-deploy-state` reads 24 migration-backed / 24 live / 0 pending, matching the AGENTS.md sentence unchanged. This should also clear the 2026-08-18 live-drift alarm (#827), whose only failing assertion was the unapplied 00292.
