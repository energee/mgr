- **2026-07-31 (keg_owner_deposits.keg_type_id retired).** Migration 00284 mirrors 00283 for `keg_owner_deposits` — the last NOT NULL survivor of the retired `keg_types` contract on a chain-built database (#711): backfill `selling_format_id` from the legacy id (00112 UUID reuse), abort on unmappable rows, drop the column, re-key uniqueness as `uq_keg_owner_deposits_owner_format (keg_owner_id, selling_format_id)`. Integration coverage in `src/__tests__/integration/keg-owner-deposits-schema.test.ts`; `docs/data-model/kegs.md` updated. Not done here: live apply, and the #711 side-question of dropping `keg_types` from the chain entirely.
