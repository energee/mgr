- **2026-07-30 (health-audit's read-only audit job now holds a read-only token).** `health-audit.yml`'s `audit` job was documented, reviewed and prompted as read-only, but passed no `github_token`, so `claude-code-action` exchanged its `id-token: write` OIDC assertion for a Claude App installation token hardcoded to `contents`/`pull_requests`/`issues: write` and assigned it to `GITHUB_TOKEN`/`GH_TOKEN` before starting the agent — a write-capable credential in every Bash tool call, unrestrained by the job's `permissions:` block. Verified against the pinned SHA `be7b93b` rather than the issue text (`src/github/token.ts` early-returns on `OVERRIDE_GITHUB_TOKEN`; `src/entrypoints/run.ts:185-186` does the env assignment; `configureGitAuth()` writes the resolved token back into the origin remote URL, so `persist-credentials: false` never left the workspace token-free). Fixed by binding `github_token: ${{ secrets.GITHUB_TOKEN }}` and dropping `id-token: write`, mirroring #668/#690's treatment of `sentry-harness.yml`'s `fix-error`; `additional_permissions: actions: read` was removed with it because `parseAdditionalPermissions()` is unreachable past the early return and the `github_ci` MCP server it appears to enable is gated on `isEntityContext(context) && context.isPR`, never true for a scheduled run. The loop still works: its allowlist is `Task`, five local `Bash(git …)` reads against a `fetch-depth: 0` checkout, and `gh issue list`/`gh issue view`/`gh label list`, all covered by `issues: read`, and the action's `checkWritePermissions` preflight is skipped for automation contexts. `.github/scripts/workflow-egress.ts` gained `readOnlyMintContradictions` — the invariant the existing enumeration could not express, since a listed exception reads as an accepted one: a job whose `permissions:` grant no repository write has declared itself read-only and must pass `github_token`, `id-token: write` explicitly not exempting it. `ci-workflows.test.ts` 55 → 62 tests; the audit job drops out of the push-capable inventory (#689's acceptance check), and two mutation checks plus four synthetic shapes pin both directions. `bug-patrol`, `feedback-distill` and `quality-regrade` are deliberately out of scope: they push from inside the agent, and `secrets.GITHUB_TOKEN` would also stop their PRs triggering `test.yml`. Not verified: no scheduled or dispatched run of the changed workflow.
