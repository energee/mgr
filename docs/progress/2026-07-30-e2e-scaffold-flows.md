- **2026-07-30 (E2E scaffolds: three real flows in, two render scaffolds out).** Closed acceptance criterion 4 of #437. Implemented the packaging-session flow (planned → in_progress → line item via PackagingDayView → completion review, asserting the finished_goods rows, `YYYYMMDD-NNN` lot numbering and the batch→finished_good allocation the DB trigger writes — unblocked by 00269's capture of the dropped 00080 xor constraints), the customer-order chain (confirm → auto-allocate FIFO → schedule → pick → pack → fulfil, asserting `planned`→`completed` allocations and the `volume_bbl` `transition_entity_atomic` stamps), and the production chain (Start Brew Day → `brew_logs` + link with a server-generated `BRW-YYYY-DDD`, batch advance, Start Packaging → session + line item). Deleted both `dashboard.spec.ts` scaffolds on evidence rather than assertion: the heatmap skip's stated reason ("F200 still in_progress") was stale (F200 shipped 2026-05-06) and its "12 weeks × 7 days" assertion described a product that was never built (what shipped is a year view), while `heatmap-utils.test.ts` already covers the count→bucket mapping; the trends skip's own reason — whole-database aggregates under `fullyParallel` — is itself the argument against implementing it, and the failure that reached users (MGR-K) is covered by `production-trends.test.tsx`. The portal leg of the order scaffold turned out to be unbuildable, not unautomated — the portal has no order-placement UI at all — so it stays skipped with #706 filed. Added `e2e/combobox.ts` after a trace showed a 2-in-6 flake: typing into a Dice UI combobox before its async options resolve leaves it `aria-expanded` over unmounted content forever; the helper polls the interaction and selects by dispatching on the matched option, because `force: true` silently selects nothing and ArrowDown+Enter committed the wrong row. Also made `createSeedClient()` refuse a non-loopback target — the repo-root `.env` points at the HOSTED project, so `bun e2e` without exported local values would have run delete-and-recreate cleanup against production.
