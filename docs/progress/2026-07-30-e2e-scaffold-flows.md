- **2026-07-30 (E2E scaffolds: three real flows in, two render scaffolds out).** Closed acceptance criterion 4 of #437. Implemented the packaging-session flow (planned → in_progress → line item via PackagingDayView → completion review, asserting the finished_goods rows, `YYYYMMDD-NNN` lot numbering and the batch→finished_good allocation the DB trigger writes — unblocked by 00269's capture of the dropped 00080 xor constraints, re-confirmed absent on the replayed local stack by a `pg_constraint` query returning 0 rows), the customer-order chain (confirm → auto-allocate FIFO → schedule → pick → pack → fulfil, asserting `planned`→`completed` allocations and the `volume_bbl` `transition_entity_atomic` stamps), and the production chain (Start Brew Day → `brew_logs` + link with a server-generated `BRW-YYYY-DDD`, batch advance, Start Packaging → session + line item). Deleted both `dashboard.spec.ts` scaffolds on evidence rather than assertion: the heatmap skip's stated reason ("F200 still in_progress") was stale (F200 is `passing`, shipped 2026-05-06) and its "12 weeks × 7 days" assertion described a product that was never built (`batch-activity-heatmap.tsx` is a `DAYS = 365` year view), while `heatmap-utils.test.ts` covers `bucketForCount` 0/1/2/3/≥4 and `bucketWeekly`'s zero-filling, and `batch-activity-heatmap.test.tsx` covers RPC-error surfacing; the trends skip's own reason — whole-database aggregates under `fullyParallel` — is itself the argument against implementing it, and the MGR-K failure that reached users is covered by `production-trends.test.tsx`. The one step those unit tests could not cover — that the heatmap is mounted on `/dashboard` at all — was added to the dashboard smoke test as a data-independent "Batches Scheduled" heading assertion rather than left deleted. The portal leg of the order scaffold turned out to be unbuildable, not unautomated — the portal has no order-placement UI at all — so it stays skipped with #706 filed, and is now the suite's only `test.skip`. Added `e2e/ui.ts` with a polled combobox picker and a toast-proof click; the picker's first version polled only up to "option visible" and clicked outside the loop, which lost a full-suite run, so the click and its `toBeHidden` confirmation moved inside the retry with per-step timeouts. Also made `createSeedClient()` refuse a non-loopback target — verified by running it with the local values unexported, where it correctly refused the hosted project the repo-root `.env` resolves to. Measured after the fix: full suite 3/3 green serial (`--workers=1`) and 3/3 green at `--workers=4`, both `--retries=0` against `bun start` on a production build compiled for the local stack; assertions shown non-vacuous by a deliberate red-check (expected 25, received 24) on the packaging trigger's output. Still open on #437: criteria 1, 2, 5 and 6 — E2E is nightly-only, not a required PR check.
