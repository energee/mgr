- **2026-07-29 (TTB cellar row exempted from the accounting-identity checks).** Scoped Form 5130.9's `ending = available − removals` / `available = beginning + produced + received` checks to the tax classes they can actually verify (keg, bottled) via `checkRowIdentities` in `src/domain/ttb-utils.ts`, so the cellar row is reported as explicitly exempt instead of failing every month — its balance lives in the in-process columns, which no identity references — and relabelled the in-process figures as a current snapshot in the report page, CSV, and print view; the snapshot-vs-history defect underneath (issue #618 options 1/2) is documented in `docs/knowledge/brewing-domain.md` and still open.
