- **2026-08-12 (packaging hook → domain + service, T3.2).** Split `src/hooks/use-packaging.ts` (242 lines) per Tier 3.2 of `docs/plans/backend-extraction.md`: pure planned-quantity/fill-volume/readiness-sort/row-mapping math moved to the React-free `src/domain/packaging-planning.ts`, the four Supabase reads to `src/services/packaging-service.ts` (throwing readers, documented, because their only callers are React-Query `queryFn`s). The hook stays as a thin React-Query wrapper and re-exports the pure helpers so existing consumers and their tests are untouched. Behavior-preserving: both pre-existing packaging test files are byte-identical and green; added `src/domain/__tests__/packaging-planning.test.ts` covering `buildFillVolumeMap` and `comparePackagingReadiness`, which had no direct coverage before the move.
