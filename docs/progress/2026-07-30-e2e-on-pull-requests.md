- **2026-07-30 (Playwright now runs on pull requests — for the first time).** Half of acceptance criterion 1 of #437, and the half a workflow file can own; the other half is a repository-settings change and is **not** done. The criterion has never held on `main`: #506 was written as a per-PR E2E gate but merged 14 minutes after #522's CI-minutes diet (2026-07-17T04:04:28Z vs 03:50:19Z, both read back from `gh pr view`) and merged in nightly form, so the job has carried `if: schedule || workflow_dispatch` since the day it first existed and had never run on a `pull_request` event — a documented cost decision whose rationale expired when the repo went public on 2026-07-24 and Actions minutes became free. Dropped that gate, and dropped the job's `needs:` **entirely** rather than repointing it: a dependency that is skipped, red *or* cancelled skips its dependents, and a skipped check reads as green, so any edge is a live route back into the bug (measured across nine runs — `Static Checks` failed on the five 2026-07-20..24 nightlies and `Production Build` on the four 2026-07-27..30 ones, and `E2E Tests` reported "skipped" on every one). The job consumes nothing from any other — it boots its own Supabase and runs its own `bun run build` — so removing the edge also took the wait behind `static -> unit-tests` off the PR's critical path: measured on two real `pull_request` events, run 30559728366 (edge present) started e2e 2m54s late for a 7m55s PR, and run 30569164797 (edge removed) started e2e and `static` in the same second for a **5m14s** PR — PR latency went down ~2m40s even though a five-minute browser suite was added. Criterion 2 alongside it: the job exits 1 with an `::error::` when `supabase status -o env` exports no credentials (`bun run build` sets `SKIP_ENV_VALIDATION=1`, so an empty-but-set URL compiles and only shows up as opaque timeouts), when the local API URL is not loopback (`/api/auth/dev-login` stays 404 without it, #656), and when fewer than `E2E_MIN_PASSING` (16) tests actually passed — an all-skipped Playwright run exits 0, re-verified here against the version this repo actually resolves, 1.61.1, not the `^1.58.2` range `package.json` asks for: a two-test all-skipped suite exits 0 reporting `{"expected":0,"skipped":2}`. `ci-workflows.test.ts` gained two contracts, both written so prose cannot satisfy them: every assertion reads the job with comments stripped, each guard is pinned as `::error::` **plus** its `exit 1`, the `needs:` graph is walked transitively, and the floor is ratcheted against the count of enabled `test(` declarations under `e2e/`. Thirteen mutations were run against the workflow to confirm the contracts catch what they claim — eleven go red (including deleting the loopback `case` but leaving its `::error::` prose behind, and event-gating a job two edges upstream of `e2e`), two behaviour-neutral edits stay green. Measured on run 30569862548, a real `pull_request` event on this exact commit: `E2E Tests` success, `Running 23 tests using 1 worker` / `5 skipped` / `18 passed (22.8s)`, 4m53s. Two stale copies of the same overclaim were corrected alongside, because a fact is only fixed when every copy of it is: `progress.yml` said "static + unit are required status checks on main" and `bug-patrol.yml` said "the required checks on main + a human are the gate" — neither is true, and the first directly contradicted the note this change added to the `--auto` contract in `ci-workflows.test.ts`. A third: rule 8 claimed `db-lint.yml` **and** `shell-lint.yml` are contract-pinned to their trigger paths; only db-lint's were, so `ci-workflows.test.ts` now pins `shell-lint`'s `scripts/**` as well (green with it, `1 failed | 56 passed` with the `paths:` list deleted) rather than the sentence being softened. And a **fourth route to a silently-green check** was found by walking straight into it: a skip-CI token anywhere in the HEAD commit message suppresses every workflow — no red X, no skipped context, the contexts simply never exist, which is greener than green. The first draft of the follow-up commit quoted the token inside backticks while explaining `progress.yml`'s comment and produced **zero** workflow runs (`gh api ".../actions/runs?head_sha=117725d1…" -q .total_count` → `0`, against `1` for its parent `3eb41ff4`); it is now documented in `ci.md` beside the `if:`/path-filter/`needs:` routes, because unlike those three it lives in no workflow file at all. **Not done, and an owner call:** ruleset `main` (11725742) declares `deletion`, `non_fast_forward`, `pull_request` and `required_linear_history` and **no `required_status_checks` rule at all**, and `main` has no legacy protection (404), so these contexts report but do not block — criterion 1's "required" half stays open, and nothing in the change claims otherwise.
