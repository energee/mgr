- **2026-08-11 (removed the brace-expansion audit exception).** GHSA-mh99-v99m-4gvg replaced its flat `<= 5.0.7` vulnerable range with per-major-line ranges (1.x patched at `>= 1.1.17`), so the pinned `brace-expansion@1.1.18` backport is no longer flagged and `bun audit` reports nothing at any severity — dropped `--ignore GHSA-mh99-v99m-4gvg` from `.github/workflows/test.yml` and moved the policy record to a "Resolved exceptions" section, leaving the audit gate with zero suppressions ("There are no active exceptions." is now contract-asserted). Cleared the 4 newer high advisories that would otherwise still redden the gate: `undici` override 7.28.0 → 7.29.0 (GHSA-4cwx-7wf7-3272), `fast-uri` override 3.1.4 → 3.1.5 (GHSA-7p8r-x3mc-p8w7), and in-range lockfile refreshes for `nanoid` 3.3.16 → 3.3.18 (GHSA-2v37-7h3g-55p8) and `js-yaml` 4.3.0 → 4.3.1 (GHSA-5p4m-2wfm-xmqj); recorded as the 2026-08-11 triage in `docs/security/dependency-policy.md`. Note for future lockfile surgery: `bun update <transitive>` adds the package as a *direct* dependency at latest major and leaves parents resolving out-of-range — edit the `bun.lock` entry (version + integrity) instead (refs #653).
