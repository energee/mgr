- **2026-07-14 (Beer Orders spreadsheet sync — F201).** Added an admin-only Settings → Integrations workflow that previews, maps, atomically applies, and audits `Beer orders.xlsx` reconciliation without MongoDB; it skips In House rows, reports stale orders without deleting them, preserves the approved customer/brand mappings and distributor pricing, and matches the source workbook at 216 external orders and 1,205 lines. Migration `00250` still needs deployment through `scripts/db-push.sh` once `SUPABASE_DB_URL` is available.
