- **2026-07-16 (migration chain repair: duplicate 00260).** Two migrations shared version 00260 (`atomic_yeast_pitch` from #497 and `durable_ai_chat_rate_limit` from #501), breaking the Fresh Supabase Migration Replay CI job on every PR (`schema_migrations` PK collision) — #504/#505 merged over that red check after verifying it pre-existed. Renumbered the rate-limit migration to `00266_durable_ai_chat_rate_limit.sql` (its objects have no 00261–00265 dependents; live never recorded or applied it, so no live rename hazard).
