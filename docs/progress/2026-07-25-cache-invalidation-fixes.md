- **2026-07-25 (React Query invalidation defects #613/#614/#615 + class-wide guard).** Fixed three stale-cache bugs: packaging-session line-item mutations now invalidate `materialPlanningKeys.sessionMaterials` so "Materials Required" stops showing pre-edit shortfalls (#613); change-request approval invalidates `materialPlanningKeys.orderMaterials`, and `MaterialRow` is keyed on its server values so the new refetch cannot persist a stale `actual_qty` override (#614); PO "Accept into Inventory" invalidates the `inventory_lots_with_quantities` view key the lots pages actually use instead of the inert base-table key, plus the on-hand aggregates, and the dead key in the pure-read `calculate_landed_cost` handler was removed (#615). Swept every `entityKeys.*` call site: fixed 5 dead `"user_profile"` keys (entity name, not the `user_profiles` table), a dead `"vessels_with_current_batch"` key (view renamed in 00014), and 6 base-table-without-view invalidations across brew-log, brew-day, batch-cancellation and packaging dialogs. Added `src/lib/__tests__/entity-key-table-names.test.ts`, a source scan that fails CI on both shapes so this class (#560's recurrence) stops coming back.
