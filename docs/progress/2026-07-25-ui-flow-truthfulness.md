- **2026-07-25 (UI flows tell the truth about what the database did).** Fixed #600 and #602: the Complete Brew wizard now branches on `batch.status` (not just vessel presence), skips batches already `fermenting` instead of sending a rejected `fermenting → fermenting` transition, collects per-batch failures so one bad batch cannot skip the ones after it, and only completes the brew log once every batch landed — making a re-run after a partial failure converge; steps 2 and 3 label an already-fermenting batch as such instead of promising a transition that will not happen. `BulkStatusActionBar` now coalesces the transitioned count with `??` and suppresses the success toast when zero records moved, so an all-fail bulk apply no longer toasts "Updated N". New coverage: `src/components/domain/brew/__tests__/brew-log-completion-dialog.test.tsx` (7 cases) and `src/components/universal/__tests__/bulk-status-action-bar.test.tsx` (5 cases), both on the repo's `createRoot` + `act` harness.
