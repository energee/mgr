- **2026-07-30 (tighten F201 deployment evidence).** Fixed forward the `evidence` string PR #649 landed on F201: "applied live 2026-07-15" overstated what the source supports (the audit establishes applied *at or before* that date) and the embedded `(live history 00250–00259)` was already stale against a chain at `00280` — a drifting parenthetical in a durable field, which is the confusion #440 was filed about. The field now says "confirmed applied live as of", discloses that the deployment fact is inferred from `supabase/live-catalog.snapshot.txt` rather than a direct live query, and points at #654 for the structured deployed-state field that would replace free-text prose (refs #440, #654).
