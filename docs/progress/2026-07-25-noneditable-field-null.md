- **2026-07-25 (Non-editable fields were being null-normalized into schema failures — branch `fix/noneditable-field-null-normalization`, app-code only, NO migration).** Found while reviewing PR #590; pre-existing on `main`. `getEditableFieldsFromSections` (`entity-detail-unified.tsx`) filtered section fields on `type` alone, so `editable: false` fields landed in the list handed to `makeFormResolver` — whose issue-#558 `""`→`null` pass then nulled them, because a non-editable field carries no `required` flag. `buildDefaultValues` seeds a `text` field with `""`, so an untouched create form sent `null` for the field, and a schema entry that is `.optional()` without `.nullable()` rejected it: batch create died on `batch_code: Invalid input: expected string, received null`, with no way for the user to clear the error since the field is not editable (it is auto-generated from planned start date + recipe). Same shape latent for `brew_logs.brew_number` and `yeast_pitch_events.pitched_at`. Fix is the one-predicate narrowing `f.type && f.editable !== false`, which also correctly covers the second call site — the save-time `""`→`null` pass at :935, the other half of the bug (it would have written `batch_code: null`). `nullHiddenDirtyFieldValues` reads the same list but keys on `dirtyFields`, which cannot contain a non-editable field, so field-level `showWhen` behavior is unchanged. Red-check verified: pre-fix the new spec fails on exactly the reported message. Tests +4 in `src/components/universal/__tests__/noneditable-field-null.test.ts` (helper excludes `editable: false` and display-only renderers; untouched batch create validates clean; fields outside the section list stay untouched; required fields still enforced) — `getEditableFieldsFromSections` is now exported as the test seam, and the spec mocks `@/lib/supabase/client` because the module's top-level import runs env validation. `make check` green.
