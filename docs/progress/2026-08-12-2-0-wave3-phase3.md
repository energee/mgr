- **2026-08-12 (2.0 streamlining — Wave 3 + Phase 3).** Closed out the streamlining plan's remaining waves across eight merged PRs (#788 D3 nav, #789 Tier 2 pricing, #790 orphan RPCs + reports layering, #791 live schema drift, #792 packaging hooks, #793 material planning, #797 required CI checks, plus #787 carrying the Waves 1–2 receipt). **Live database:** applied 00287 and 00288, closing #769 — the drift check now exits 0 on both assertions after failing every scheduled run since 2026-08-10; the snapshot had simply gone stale, all 17 removed lines trace to committed migrations, and no out-of-band change was accepted. F124 flipped `pending` → `live`, so `check-deploy-state` reads 24 migration-backed / 24 live / 0 pending. **Required status checks (#713) are now active on `main`** — `Static Checks`, `Unit Tests`, `E2E Tests`, `Database Lint and Integration Tests` on ruleset 11725742, closing the "every CI gate is advisory" gap that both the Tests and Harness rows of `quality.md` named as fundamental. **Backend extraction:** Tier 2 and Tier 3 of `docs/plans/backend-extraction.md` are done — customer-tier pricing, packaging, and material-planning BOM math now live in `src/domain/` and `src/services/` and are callable without a renderer; characterization tests were written against the pre-extraction code first in every case, and all three diffs passed an independent `refactor-reviewer` gate. **Worktrees:** 26 → 2. Honest accounting: this wave was **LOC-positive**. The extractions net roughly flat-to-up on production code (+202 on packaging, ~flat on material planning, −28/+88 on pricing) with ~460 lines of new tests where there were none, and the RPC cleanup added ~65. The win is structural — business rules that would have died with the frontend now have a home, and 199 lines of dead SQL left the chain — not a line count. **Ten audit claims turned out to be wrong**, which is the more useful finding: three high-value nodes described code that no longer existed (D3's five nav targets were never in `nav-items.ts`; T2.1's PO-receiving rule had already moved into the `receive_purchase_order_items` Postgres function; T3.1's `useCalculateOrderMaterials` was deleted by #505 into migration 00265's atomic path), every worktree flagged as "old unmerged work" was already merged, #783 missed a test file and was not coverage-neutral, #697 understated the orphaning, and #713's "db-lint is the slowest job" premise was false — measured, db-lint is ~3m13s against E2E's ~5m50s, which is why the required-checks fix became a nine-line filter deletion instead of a 157-line shim. Details in the per-node entries alongside this one.
