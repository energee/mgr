- **2026-07-30 (Repair three wrong sentences and two broken contracts left by #690).** An adversarial review of #690 (the #668 credential split, already merged) found nine problems and eight overclaims; this is the remediation. **Restored `docs/agents/ci.md`'s `## Live apply and rollback` section** — #690's commit message called it "a duplicated section" and deleted 33 lines, but it appeared exactly once on `origin/main`, was added deliberately on 2026-07-24 (`docs/progress/2026-07-24-harness-gaps-lopopolo-review.md`), and #657 had already removed the real duplicate, so the deletion took the only copy of the no-down-migrations rule, the `db-push.sh` rollback order of operations, and the "watchdog is only as live as its secret" `SUPABASE_DB_URL` warning; the restore is byte-identical to the pre-merge text. **Corrected the security disclosure**, which said `persist-credentials: false` leaves the workspace token-free: at the pinned SHA `be7b93b`, `claude-code-action`'s `configureGitAuth()` unsets the `http.<server>/.extraheader` `actions/checkout` writes and then runs `git remote set-url origin https://x-access-token:<token>@github.com/…`, putting the resolved token back into `.git/config` in a *more* durable form — verified against the pinned source, not from memory. Fixed in all six copies (`sentry-harness.yml`, `workflow-egress.ts`, `workflow.test.ts`, `docs/agents/ci.md` ×3, `docs/sentry-harness-setup.md`) plus `claude.yml`, whose exemption rationale rested on the same false claim while the job holds three write scopes. **`parsePlan` now accepts `A` + `issue`**: the prompt's Investigation-Issue Fallback documents filing an issue when three attempts fail to fix an (A), and the old rule threw `classification A must open a PR` on exactly that plan — a documented give-up path that failed red and published nothing, where the pre-split design filed the issue and stayed green; the rule is now "A must produce a PR **or** an issue", with a prompt test that feeds the documented plan through the real validator so the two cannot drift again. **Widened `FORBIDDEN_PATCH_PREFIXES`** from `.github/{workflows,actions,scripts}/` to every executor one hop from CI (`.github/`, `Makefile`, `scripts/`, `package.json`, `bun.lock`, `bunfig.toml`, `.claude/`, `.agents/`) — `Makefile`, `scripts/db-push.sh`, `package.json` and `.claude/settings.json` were all accepted while the docstring claimed the list removed "the one path" — and rewrote the docstring to say what it actually buys; the genuinely inherent residual (prompt step 5's test file runs in CI on the fix PR) is now #699. **Added `land-sentry-fix.test.ts`**, the first coverage of the only credentialed path in the design: 17 tests driving real `git` against a scratch repo with a real bare origin and a stub `gh` on PATH that records every argv, so "agent text never reaches a shell" is checked as data. **`pushFix` no longer fails every re-run** — a plain push to the stable `sentry-fix/SENTRY-<id>` branch was non-fast-forward the second time a scheduled run picked the same error; it now leases against the tip it just read and reports the PR already open instead of erroring in `gh pr create`. **A `comment` may no longer target an arbitrary issue**: the lander re-reads the target's labels and requires `sentry-fix`. Also: `fix-error` installs with `bun install --frozen-lockfile` before the agent runs (an agent-run `bun install` put `bun.lock` in the packed patch, which fails a (B)/(C) triage that must pack an empty one), the prompt's tree-clean recipe is now `git checkout -- . && git clean -fd -e outbox` (`git checkout` alone leaves untracked scratch files that `git add -A` picks up), and the outbox's agent-authored bodies are disclosed as a permanent publishing channel rather than omitted from the residual list. Five red-checks: reverting the A-rule, narrowing the denylist, dropping the label check, reverting to a plain push, and removing the frozen-lockfile step each fail the suite.
