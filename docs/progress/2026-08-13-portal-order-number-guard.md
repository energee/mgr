- **2026-08-13 (portal order-number sequence guard).** Review of the live-first `00290` rollout found that its customer INSERT policy relied on an `order_number` DEFAULT but did not reject an explicitly supplied value. Because the shared generator parses the maximum trailing suffix as an `INTEGER`, a portal customer could insert the current year's `2147483647` suffix and make every later generated staff or customer order overflow. Migration `00291_portal_order_number_guard.sql` requires portal inserts to match the advisory-locked generator's next shared-series value while leaving staff's separate `orders_write` path unchanged; the RLS integration suite now reproduces and guards the sequence-poisoning case. Applied live on 2026-08-13 through `scripts/db-push.sh`; direct catalog verification confirmed migration `00291` and the generator predicate, and the refreshed snapshot matches live with no remaining drift.
