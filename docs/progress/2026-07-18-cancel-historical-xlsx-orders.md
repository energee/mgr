- **2026-07-18 (Cancel historical XLSX orders tooling, issue #425).** Added `scripts/migration/cancel_historical_beer_orders.py`, a dry-run-by-default PostgREST script implementing the issue #425 decision: transition the ~200 pre-cutover `Beer orders.xlsx` keg orders from `packed` to `cancelled` while preserving all order/item rows and metadata. Belt-and-suspenders candidate selection (XLSX order-number regex + importer notes prefix + pre-cutover date), preimage JSON backup before mutation, planned-allocation release mirroring the client-side cancel side effect, cleanup of the per-user "Order Cancelled" broadcast notifications, and post-apply verification. Execution against hosted data remains a human action; documented in `scripts/migration/README.md`.
