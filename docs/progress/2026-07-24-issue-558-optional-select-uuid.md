- **2026-07-24 (fix #558: empty optional selects blocked form submit).** The unified create form initializes untouched optional selects to `""`, which optional `.uuid()` schema fields reject during resolver validation — an empty-catalog recipe create could never submit. Added `makeFormResolver` (`src/lib/form-resolver.ts`) nulling `""` on non-required fields before validation, wired into `EntityDetailUnified`; covered by `src/components/universal/__tests__/empty-optional-select-resolver.test.ts`.
