- **2026-07-29 (stale local service-role key gotcha).** Documented the `Unregistered API key` failure mode in `docs/agents/gotchas.md` — a local Supabase key in `.env.local` that no longer matches the running stack, which fails every `createAdminClient()` route at once with no Postgres error code and cost five duplicate Sentry investigations (#636, #637, #638, #641, #642); `scripts/db-local.sh` now closes by telling you to resync the keys from `supabase status`, since a reset is the moment they rotate. No `src/` change: the routes already catch and report the upstream error correctly.
