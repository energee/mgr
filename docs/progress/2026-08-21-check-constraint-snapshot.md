- **2026-08-21 (live-drift watchdog now covers CHECK constraints, #865).** `scripts/live-catalog.sql` emits `CHECK|<table>|<constraint>|<md5 of pg_get_constraintdef>` for every public-table CHECK constraint, so an out-of-band DROP or edit (the class that silently removed `orders.status`'s CHECK until a manual audit found it) turns the daily `live-drift.yml` cron red instead of staying invisible. Snapshot rebaselined from live (+82 CHECK lines, purely additive; `orders_status_check` confirmed present, i.e. 00295 healed live). The deferred retention-index item in #865 stays deferred until row counts justify it.
