- **2026-07-11 (Catalog keep-set safety — branch `feat/brands-is-active-keepset`, backlog P2 #16, audit IN-9; migration `00244` NOT applied live).** The Square catalog stale-cleanup keep set was DERIVED from POS-bin/stock reads (in-stock brands ∪ `bin_inventory` rows at POS bins ∪ any keg-format FG brand), so re-pointing a bin's POS target before moving stock shrank the read and `deleteStaleItems` bulk-deleted live Square items (images + Item-Sales history). Now: **`00244_brands_is_active.sql`** adds `brands.is_active boolean NOT NULL DEFAULT true` (self-verifying DO block), and the catalog route keys the keep set off the ACTIVE brand set — pure config state; an inactive brand is excluded from the push AND the keep set (discontinuing = the one intentional Square-removal path; the recorded upgrade path from the route's old ponytail comment, now realized). Interim guard kept as defense in depth: `deleteStaleItems` refuses stale sets > `STALE_DELETE_MAX_COUNT` (10) rows or > `STALE_DELETE_MAX_FRACTION` (50%, floor 3 rows) of mapped objects, returning `aborted` (nothing deleted, `success:false`, surfaced in response + `square_sync_log.details.staleAborted`); override threads `{ forceStaleDelete: true }` from the request body (the combined `/api/square/sync` passes the caller's request straight through under #382's direct invocation — the catalog handler is the single body reader). Brand entity gains the `is_active` switch/column/filter (`Brand` type hand-extended pending regen — 00244 unapplied; route reads via `dynamicFrom`). Tests: +4 guard tests (`catalog.test.ts`), catalog-route keep-set suite reworked to is_active semantics + force-threading + abort-surfacing (`sync-routes.test.ts`). **Deploy = `scripts/db-push.sh` after merge.** Branch rebased onto `fix/square-sync-origin` (#382) to resolve the combined-route overlap. Expected merge overlap with PR #377: PROGRESS.md + adjacent hunks in `sync-routes.test.ts` (their edits are inventory-block only).
- **2026-07-11 (Square combined sync — no Host-derived self-fetch; branch `fix/square-sync-origin`, audit SEC-1 / backlog P2 #20).** `/api/square/sync` (POST) previously fetched `${new URL(request.url).origin}/api/square/sync/{catalog,inventory}` with the caller's cookie forwarded — `request.url` derives from the incoming Host header, so on a deployment that doesn't strictly validate Host a spoofed header could send the admin's session cookie to an attacker-chosen origin. Now the route imports the sibling route handlers and invokes them DIRECTLY as functions with the same request (chosen over the `SITE_URL` fallback: it removes the attacker-influenceable origin AND the loopback HTTP hop entirely; each inner handler still runs its own `withPermission` gate). Response shape, error semantics (CATALOG_SYNC_FAILED passthrough status, partial-success on inventory failure), and sync-log writes unchanged — the inner handlers are untouched. Same-pattern grep across `src/app/api`: the only other `request.url`-origin uses are the QuickBooks OAuth redirect-URI fallbacks (`auth/route.ts:29`, `callback/route.ts:60`) — browser-redirect URIs bounded by Intuit's registered-URI matching (SEC-1's own assessment), not self-fetch targets; left as-is. New `combined-sync-route.test.ts` (4 tests) asserts direct invocation with the identical request object and ZERO outbound fetch even under a hostile Host. Gates: lint + typecheck + full test suite green.
