- **2026-07-15 (Staff-authorized, durably rate-limited chat).** Restricted `/api/chat` to the new `ai:use` staff permission before service-role or Anthropic access, replaced the per-instance IP counter with an atomic service-role-only Postgres bucket per user, preserved caller-scoped RLS for chat tools, and added route, permission, privilege, reset, and concurrency regressions for issue #448.
