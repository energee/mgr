- **2026-07-31 (capture the last live-only tables; #696 backlog reaches 0).** Migration `00285_capture_channel_formats.sql` brings `channel_formats` and `pricing_channel_formats` into the migration chain (#724). Both had been live-only since the #217 renumbering deleted `00117`/`00125`, and `channel_formats` is load-bearing — `format-management.tsx` and `pricing-matrix-view.tsx` read and write it — so a from-scratch replay produced a database the shipped pricing UI could not run against. Shapes were cross-checked against three sources that agree column-for-column (generated types from live, `docs/data-model/packaging.md`, and the recovered `00117` body via `git show 2909140d^:…`), and the policy bodies were verified byte-exact by replaying the migration into a transaction on a chain-built database and rolling it back: the emitted `POLICY|…` and `RLS|…` lines for `channel_formats` match `supabase/live-catalog.snapshot.txt` hash-for-hash. Along the way the audit turned up a real live gap: `00125` tightened `pricing_channel_formats` RLS but never reached live, so live still carries `00117`'s three `USING (true)` policies letting any authenticated user read, insert and delete there. Section 4 of the migration restores the intended `settings:manage` pattern (the table is dead code, so nothing breaks). F124 is now anchored on 00285 with `deployment: pending` — the tables are already live, but 00285 has not been pushed, so the RLS fix is not applied. `deployment_conventions.unaudited_backlog.count` drops 1 → 0, closing #696's last entry.
