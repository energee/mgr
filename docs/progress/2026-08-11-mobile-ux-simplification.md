- **2026-08-11 (Mobile-first UX simplification shipped).** Branch `feat/mobile-ux-simplification` (plan `docs/superpowers/plans/2026-07-12-mobile-ux-simplification.md`, spec `docs/superpowers/specs/2026-07-12-mobile-ux-simplification-design.md`): nav restructured to 2 direct links + 4 always-open sections (Planning link dropped — feature removed upstream in #453), mobile bottom tab bar (Batches/Inventory/Picks/More) replaces the hamburger, dialogs render as bottom sheets on phones with 44px inputs, entity detail pages stack their header / collapse actions / scroll tabs on mobile, and a measured 390×844 floor-screen pass fixed the PO receive dialog (DialogContent stretched to 1068px by its 8-column table; qty/lot inputs unreachable) and the packaging quick-add row (680px mini-table → stacked cells, 44px controls below md). Pick list screen passed as-is. Residue: the in-progress packaging day-view table still pans horizontally on phones (controls are 44px; a stacked layout there is a redesign, not a class tweak). Suite 2820 green; no migrations.
