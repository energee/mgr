- **2026-07-30 (supplier_catalog + enum_values revision trigger).** Migration 00282 puts `supplier_catalog` and `enum_values` under the `entity_revisions` ledger (triggers mirroring 00213, plus the CASE branches `docs/data-model/system.md` requires in the same migration: `purchasing:read` and `settings:manage`), so a preference flag or an admin-set status option destroyed by a data migration or hand-edit stays reconstructible. `enum_values` was added after re-running the sibling sweep properly — of 35 `DELETE FROM` statements in the chain, 10 are top-level one-shot deletes, not 2 as the first draft of the migration header claimed, and 4 of them delete `enum_values` rows wholesale by `enum_type`. `entity-revisions-rls.test.ts` now derives the tracked-table list from `pg_trigger` so the policy can no longer drift from the triggers. Residuals filed: #693 (00282 unapplied live, and nothing detects a committed-but-unapplied migration), #694 (`suppliers` deletions still untracked), #695 (brew-log Revision History tab is permanently empty). The live re-marking owed by #549 is still open and needs a human with credentials.
