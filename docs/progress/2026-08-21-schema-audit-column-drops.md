- **2026-08-21 (Schema audit steps 5–6: dead-column drops + constraint tightening).** Authored `00297_drop_dead_columns.sql` (audit M1: drops 22 dead columns across yeast_pitches, recipes, order_items, and six catalog tables, with 00294-style loud guards — the recipes.ingredients/instructions guard intentionally fires on live until the operator archives the legacy JSONB; rebuilds recipes_with_estimates, batches_with_blend_info, recipes_with_cogs, batch_additions_with_costs, order_items_with_details, yeast_pitches_with_remaining; permanently drops the chain-only yeast_pitches_with_details; corrects _schema_registry) and `00298_constraint_tightening.sql` (audit low-severity bundle: 13 positivity CHECKs via the 00192 NOT VALID + VALIDATE idiom, locations UNIQUE(name), batch_logs.log_type CHECK; bin_inventory/_items skipped — already covered by 00028/00073/00239). Updated supabase/seed.sql (recipes/malts/hops/sugars/fruits inserts no longer reference dropped columns) and docs/data-model/. Live-apply is a separate operator step.
