- **2026-08-21 (docs-dream loop).** Added the weekly Docs Dream Sweep (`docs-dream.yml`, Sat 04:00 UTC): a deterministic broken-link report (`.github/scripts/check-doc-links.ts` + unit tests) feeds a bounded Sonnet agent that fixes broken links and provable doc contradictions as ONE docs-only `docs-dream` PR, with ambiguous conflicts surfaced in the PR body for a human call. Registered in `scheduled-jobs.md`, `improvement-loop.md`, `ci.md`, and the CI contract suite (credential inventory + structural pins).
