- **2026-08-21 (characterization for known-unfixed edges — backlog item 24, audit TC-3/TC-12).** Tests only; no production code touched. Three edges the 2026-07-10 audit flagged as unfixed now have tripwires so a refactor cannot change them silently. (1) `src/lib/__tests__/bin-placement-sql.test.ts` (new) pins 00219 `place_finished_good_in_bin`: keg skip via `selling_formats -> containers.type`, a NULL `selling_format_id` treated as NON-keg and placed, session-`default_bin_id`-then-`is_default_fg` resolution, the LOCATION-BLIND fallback (no FG-derived location predicate — a satellite-location FG lands in the primary location's bin), the never-block contract (no `RAISE EXCEPTION` anywhere, so an unplaceable FG fails silently), `ON CONFLICT DO NOTHING`, no backfill, and the WHEN-less `AFTER INSERT` trigger. Self-invalidates if a later migration re-creates the trigger or the function. (2) `src/lib/__tests__/sellable-inventory-sql.test.ts` (new) pins 00221/00226 `sellable_inventory`: the packaged branch's INNER JOIN on `selling_formats` that DROPS NULL-format FGs (the mirror image of what 00219 places — such stock is in the bin UI and invisible to the POS), the `<> 'keg'` double-count guard, the 00226 `LEAST(bin, ledger availability)` read-path clamp, and the keg branch's asymmetry (neither the format join nor the clamp applies). (3) `src/domain/__tests__/report-utils.test.ts` gains a "planned + completed cost semantics" block pinning the `status IN ('completed','planned')` filter, the batch-as-destination scoping, that a planned and a completed allocation on the same lot BOTH count (the flagged COGS double-count), that `status` is neither selected nor returned so no caller can separate the two, and that an unpriced planned allocation contributes $0 (the opposite-direction error). `sql-def-helpers.ts` gained `migrationsMatching()` for contracts that live outside a function/view body (CREATE TRIGGER, view options) and for self-invalidating "only one migration defines this" assertions. Every knowingly-imperfect assertion is labelled `KNOWN-IMPERFECT` with a comment saying why it is recorded rather than blessed.
- **Ceiling.** These are SQL-TEXT assertions (the `sql-def-helpers` idiom), not behavioural DB tests — the vitest tier has no Postgres and this work ran under a no-local-DB constraint. They catch an edit to the migration chain; they cannot catch a runtime regression that keeps the matched text, and they cannot prove the runtime consequences described in the comments (an FG actually vanishing from the POS, a real planned+completed double count in live data). Those belong to the live-Postgres tier (`vitest.integration.config.ts`, backlog item 21) — TC-2's seeded receive→fill→ship round-trip is the real test for the view. Backlog item 23 (seeded TTB summary invocation; two-customer JWT RLS round-trip) was NOT attempted: both need a seeded database and belong to the integration harness, not here.
- Suite: 3058→3090 tests, 232→234 files. `make check` green.
