- **2026-07-29 (production health probe).** Added `.github/workflows/prod-health.yml`, a 15-minute cron watchdog that probes `${vars.PRODUCTION_URL}/api/health` (HTTP 200 *and* body `status: "ok"`) plus `/login`, retries 3× with backoff, and maintains ONE deduped `prod-down` issue — closing issue #587's gap where every merge auto-deployed to Vercel production with nothing verifying the deployed artifact; a missing `PRODUCTION_URL` repository variable fails the run loudly instead of skipping silently.
