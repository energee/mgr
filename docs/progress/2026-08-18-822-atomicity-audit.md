- **2026-08-18 (write-atomicity audit, #822).** Audited all 9 dynamicFrom multi-write files the gate was blind to before PR #821. Verdict for every file: tx-ok — no path chains dependent writes. entity-service and entity-detail-unified count mutually exclusive branches; the delete dialog's per-row bulk fallback is independent by design; the invite route compensates (auth-API calls can't join a SQL transaction, #548/#605); Square draft-sale reconciliation and mongodb sync are idempotent-by-key with re-run repair, and mongo aggregates already use reconcile_mongodb_* RPCs; the three child-row editors fire one write per user gesture. No RPC conversions or migrations needed. All 9 lines removed from the allowlist; its second (#822) section retired.
