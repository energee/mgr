- **2026-07-11 (`relatedInvalidations` design — branch `feat/entity-related-invalidations`, backlog P2 #12, audit UI-1..UI-5, docs-only).** Design doc `docs/plans/2026-07-11-related-invalidations-design.md` for the structural fix to TanStack Query invalidation drift. Decisions: `relatedInvalidations?: { always, onTransition }` on `EntityCore` (server-safe; static keys from `src/lib/query-keys.ts` factories only, per-id keys stay in `transition-side-effects.ts`); ONE composition function `invalidateEntityWrite()` in new `src/lib/entity-invalidation.ts` (baseline own table/viewTable/detail/revision/dynamic-options keys + config keys); ALL transition invalidation moves inside `runTransitionSideEffects` (call sites just pass `queryClient` — the UI-3 fix; new `coresByTable` export from `src/entities/cores.ts` for cross-entity table writes, also fixing a newly-found `order_list_details` miss in the pick-list→orders sync); CRUD surfaces + the six UI-1/UI-2 domain dialogs call the helper directly (QuickCreateDialog gets viewTable = UI-5). Per-entity matrix: 19 of 39 entities declare extras (two patterns: domain-root alias ≠ table name; cross-entity derived data); dashboards/planning/reports deliberately excluded. 8-task implementation plan with parallelism, tsc/vitest acceptance criteria, and guard tests (config-shape walk + `runTransitionSideEffects` call-site source-walk). Implementation = follow-up pass.
