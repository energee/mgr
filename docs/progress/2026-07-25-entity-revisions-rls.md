- **2026-07-25 (entity_revisions read authorization).** Closed #604: `entity_revisions` stored full `to_jsonb()` row images behind an `auth.uid() IS NOT NULL` SELECT policy, so any authenticated principal — including portal customers, whose staff permission set is empty — could read every customer's orders plus recipe formulations, supplier costs and pricing-tier rows in one PostgREST call. Migration 00275 replaces the policy with a `CASE entity_type` gate mapping each tracked table to the permission its own SELECT policy requires (mapping read off the live policy set, correcting the issue's proposed `allocations`→`orders:read` and `packaging_sessions`→`inventory:read`), with an `ELSE settings:manage` branch so an unenumerated entity_type is admin-only; also replaced the false 00198 RLS-EXCEPTION comment. New behavioral integration spec `src/__tests__/integration/entity-revisions-rls.test.ts` pins the per-role matrix (customer reads zero rows, `sales` denied purchase-order revisions, unknown type admin-only) — 8 assertions red before the migration, 11/11 green after; full integration suite 144/144.
