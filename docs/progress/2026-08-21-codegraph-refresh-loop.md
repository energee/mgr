- **2026-08-21 (codegraph refresh merge-loop fix).** `codegraph-refresh.yml` merge-looped against itself (~29 consecutive metadata-only `chore(codegraph): refresh graph.json` commits on main): every rebuild stamps the new HEAD sha + timestamp into `graph.json`, the whole-file `git diff --quiet` counted that as drift, and `tools/codegraph/**` in the trigger paths re-fired the workflow on each bot merge. Fixed by comparing bodies with `jq 'del(.commit, .built_at)'` and excluding `graph.json` from the trigger paths; verified locally that a no-input-change rebuild is now detected as fresh.
