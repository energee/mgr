- **2026-07-31 (feature-tracker migrations audit, #696).** Audited 31 of the 32 `migrations: "unaudited"` entries and cross-checked every conclusion with a second, independent pass whose reviewers were instructed to refute rather than agree; the two passes agreed on 25 of 32, and three entries changed as a result. F100 gained the durable-rate-limit anchor — the chat route calls `consume_ai_rate_limit` unconditionally and throws on error, so it is a hard runtime dependency, and adding it also moved the entry from the gate's not-verifiable bucket into the snapshot-verified one. F122 is no longer `[]`: the shipped additions UI reads `recipes.target_water_profile_id`, so it anchors to the drift-capture shim where that column enters the chain. F126 dropped the catalog/junction migration it had also listed — 18 tables this feature only reads, which made the entry read as fully snapshot-confirmed on objects that are not its schema. Final split: 23 migration-backed (22 snapshot-verified, 1 not verifiable), 38 `[]`, 1 `"unaudited"`. F124 stays `"unaudited"` structurally: its `channel_formats` table exists only on live — no chain migration creates it (a #217 renumbering casualty) — and needs a capture migration before it can be anchored (#724). Ratchet 32→1.
