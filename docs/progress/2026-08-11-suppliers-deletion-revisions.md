- **2026-08-11 (suppliers deletion revision trail, #694).** Migration `00286` attaches a DELETE-only `log_entity_revision()` trigger to `suppliers` (INSERT/UPDATE deliberately untracked — MongoDB sync upserts every supplier per run) and re-states `entity_revisions_select` with the `suppliers → purchasing:read` CASE branch, closing the 00282 residual. Integration coverage: new `suppliers-revisions.test.ts` (DELETE writes the ledger row with full `old_data`; INSERT/UPDATE stay silent; set-based DELETE à la `00252:86` is captured) plus `suppliers` added to the `entity-revisions-rls` fixtures. Committed only — live apply is an operator step sequenced behind 00282 (#693). Docs: `system.md` trigger table + access-control mapping, `purchasing.md` suppliers section.
