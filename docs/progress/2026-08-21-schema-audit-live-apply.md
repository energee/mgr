- **2026-08-21 (schema audit steps 1–4 live apply).** Fixed PR #866's red CI (RLS-EXCEPTION comments on the captured `email_notification_log` policies; removed the `packages`/`package_types` demo seed that 00294 orphaned) and merged it. Applied 00294–00296 to live via `scripts/db-push.sh`: exported `pricing_channel_formats`' 14 rows to `docs/audits/exports/` and then deleted them (00294's guard aborts on any row — export-then-delete is the destructive step the guard forces; every other guarded table was already empty), verified the drops and the `prune-log-tables` pg_cron job (05:10 daily) on live (00296 also schedules `cleanup-old-notifications`, not separately verified — the snapshot has no CRON section), and committed the refreshed snapshot + regenerated `src/types/supabase.ts` (the types diff also backfills drift from earlier PRs that never regenerated). Note: `orders_status_check` already appears in main's snapshot because #874 rebaselined it from live after 00295 had been applied there — 00295's "no CHECK on live" rationale describes the pre-apply state, not a contradiction. Tracker: F112/F124 re-audited (their anchors created objects 00294 retired); `check-feature-deployment.ts` now supports `deployment_conventions.retired_objects` so a deliberate later drop is recorded instead of failing the gate forever. Live pre-checks for audit step 5: `order_items.package_id` is 0/1205 non-NULL; `yeast_pitches.{batch_id,pitched_at,cell_count_billion}` and `recipes.boil_time_minutes` are chain-only (already absent on live — and live's `boil_time_min` is alive, not a drop target); `recipes.ingredients/instructions` are data-bearing (153/153) and need export before drop; `recipe_yeasts.pitch_rate` is 141/141 but every value is the 0.75 column default; MongoDB sync writes none of the M1 catalog fields.
