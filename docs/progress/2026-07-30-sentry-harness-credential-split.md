- **2026-07-30 (Take the push-capable token out of the Sentry agent's reach).** Split `sentry-harness.yml`'s `fix-error` into an uncredentialed agent job that declares its outcome in `outbox/plan.json` plus body files, and a deterministic `land-fix` job that validates the artifact, `git apply`s the patch and publishes — and found while doing it that a read-only `permissions:` block is not sufficient on its own: with no `github_token` input, `claude-code-action` exchanges an OIDC assertion for a Claude GitHub App token hardcoded to `contents`/`pull_requests`/`issues: write` and assigns it to the agent's `GITHUB_TOKEN`/`GH_TOKEN`, which is why bug-patrol's PRs are authored by `app/claude`; `fix-error` now also binds `github_token` and drops `id-token: write`, and the contract test enumerates every agent job holding either credential (including `health-audit`'s nominally read-only audit job).
