- **2026-07-13 (Share agent instructions across harnesses — branch `chore/share-agent-instructions-across-harnesses`).** Promoted user-level Claude process rules into repo `docs/agents/process.md` (bug RGR, feature two-phase, worktrees, docs gate, guardrails) and linked them from `AGENTS.md` so Grok/Codex get the same workflows. Thinned `CLAUDE.md` to Claude-only mechanics; progress-log convention is now an `AGENTS.md` hard constraint. Normalized skills to `*/SKILL.md` packages (`bug-patrol`, `bug-triage`, `sweep-report`, refreshed `mgr-patterns` for entity triad + `make check` / `bun run test`). De-Claude'd `docs/agents/dispatching-agents.md` and entity path note in `patterns.md`; expert agent frontmatter documents Claude-only `tools` + portable `capability`.
