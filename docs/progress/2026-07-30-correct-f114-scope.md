- **2026-07-30 (F114's description no longer overstates the customer portal).** #706: the tracker entry was titled "Customer order portal" with `user_visible_behavior: "External-facing portal for customers to place orders."`, but `src/app/portal/` ships exactly four routes — login, orders list, order detail, change-request/new — and no order-creation path anywhere; the orders list's own empty state says "Once your brewery places an order on your behalf, it will appear here." That matches the documented design (invite-only portal, staff confirm everything), so the defect was the description, not the product. Retitled to "Customer order portal (view + change requests)" and rewrote the behavior line to say what shipped, that customers deliberately do not place orders, and which routes exist. The one remaining `test.skip` in `e2e/customer-order.spec.ts` keeps its tracked reason rather than being deleted — #437's fourth acceptance criterion asks for tracked skips, and the skip is the honest record that portal order placement does not exist.
