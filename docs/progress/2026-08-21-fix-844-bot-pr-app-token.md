- **2026-08-21 (bot PRs run their required checks via an app token, #844).** `progress.yml` and `codegraph-refresh.yml` now mint an app installation token (`MGR_AUTOMATION_APP_ID` / `MGR_AUTOMATION_APP_PRIVATE_KEY`, `actions/create-github-app-token` pinned) and use it for both the branch push and `gh pr create`/`gh pr merge`, so their bot PRs actually trigger the four required checks instead of stranding BLOCKED (#836/#837). Missing secrets fall back to `GITHUB_TOKEN` with a loud workflow warning. Shape pinned in `ci-workflows.test.ts`; operator still needs to create the GitHub App and add the two secrets.
