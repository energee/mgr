- **2026-08-11 (TTB cellar reconciliation, issues #618 + #698).** Migration 00287 makes `get_ttb_inventory_summary`'s in-process terms period-keyed: batch status is reconstructed at each period boundary from `entity_revisions` (issue #618 option 1), so closed months are reproducible and a month's in-process ending equals the next month's beginning by construction — new integration suite `ttb-in-process-period-keyed.test.ts` plus structural pins in `ttb-sql.test.ts`. The Total column gains its own cross-class check, `checkTotalColumnCrossFoot` in `src/domain/ttb-utils.ts` (issue #698's second acceptance path): `Total ending = Total available − (Total removals − exempt removals)`, wired into the report page's "review before filing" alert, with `getTotalScopeCaveat` reworded from "does not cross-foot" to the checked relationship. Report-backed surfaces (screen/CSV/print) switch to an "In Process (End of Period)" label + audit-trail balance note; the legacy fallback card keeps the snapshot wording because its figure is still a live client-side batch sum. Cellar stays identity-exempt — its remaining structural gap is that packaging outflow is not a removal line — with the exemption reason updated to say so.
