- **2026-07-13 (shared agent worktrees — PR #402).** Added harness-neutral worktree management under `~/.agents/worktrees/<repo>/`: `scripts/agent-worktree` creates, locates, diagnoses, and safely removes registered worktrees; Claude routes native creation through a `WorktreeCreate` hook; Codex, Claude, and Grok share the `worktree-manager` skill from canonical `.agents/skills/` with Claude compatibility symlinks. Added isolated Git lifecycle tests, static agent-config validation, Make targets, `.worktreeinclude`, F010 tracking, and shared process rules. `make verify-feature ID=F010` passes. Full `make check` passed lint, typecheck, and 2,282 tests, then stopped on four pre-existing SECURITY DEFINER justification findings in untouched migrations.
