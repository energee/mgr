- **2026-08-21 (P0 auth items 1–3 — branch `fix/customer-role-enforcement`; audit C1/M16/H9, `docs/plans/2026-07-06-audit-fix-backlog.md`).** Assignment was to land backlog P0 #1 (assign and enforce the `customer` role) and #2 (close self-registration). **Both had already landed** — PR #341, migration `00201_customer_role_and_notification_scoping.sql`, merged 2026-07-07 — along with #3 (scope order notifications), which was never checked off either. Every sub-bullet was re-verified against current code rather than taken from the plan: 00201 §1 restores the customer-email→role linking `create_user_profile()` lost in 00097 (case-insensitive, `COALESCE(is_active, true)`, customer-match taking precedence over first-user-admin so a replay cannot hand the admin bootstrap to a customer); §2 backfills exactly `roles = ARRAY['viewer']` rows whose email matches a customer, leaving multi-role staff untouched; §3 redefines `notify_all_users()` to iterate `user_profiles WHERE status = 'active' AND NOT ('customer' = ANY(roles))` instead of all `auth.users`. On the app side `src/app/api/customers/[id]/invite/route.ts:179` sets `roles: ["customer"]`, `src/app/(app)/layout.tsx` gates on `isPortalUser(roles)` rather than the exact-array match the audit flagged, `shouldCreateUser: false` is set on both the portal OTP path (`portal-login-form.tsx:48`) and the invite route (`invite/route.ts:288`), and no `/signup` route exists anywhere under `src/app` (the staff OTP path was closed separately by #368). **Exactly one bullet was genuinely outstanding**: item 1's "multi-user RLS round-trip test (customer JWT vs another customer's orders)". The three existing suites each cover a different axis and none cover this one — `customer-role-scoping.test.ts` asserts `pg_get_functiondef` *text* and its own header records that behavioral trigger tests were "not possible" from an `authenticated` role client; `portal-order-insert-rls.test.ts` covers cross-customer **INSERT** denial (00290); `portal-revocation-rls.test.ts` covers a *revoked* link on the **same** customer (00276). New `src/__tests__/integration/portal-cross-customer-rls.test.ts` (9 tests) seeds two live customers each with their own portal user, order, and line item, and asserts isolation in **both** directions across `orders`, `order_items`, and `customers` — plus a behavioral assertion that `create_user_profile()` actually stamps `roles=['customer']` on an `auth.users` insert, which is the C1 regression itself and which the schema-text suite could not reach. Because RLS denies reads by returning zero rows rather than raising, every denial is paired with a same-query positive control, so a dropped fixture cannot read as a pass. **Each assertion was mutation-proven, not merely observed green**: replacing `customer_orders_select`, `customer_order_items_select`, and `customers_customer_select` with `USING (true)` and restoring the pre-00201 `create_user_profile()` turned 6 of the 9 red while the 3 positive controls held; the schema was then restored by re-applying 00201 and 00276 and the suite returned to 9/9. **No production code, migration, or RLS policy was changed** — this branch is tests plus documentation, so there is no portal-user lockout risk to assess. Integration suite 37 files / 254 tests green against a from-scratch replay of all 276 migrations (that replay also confirms #920's 00299/00300 renumber repaired the chain); `make check` green. Verification ran against a throwaway `authp0_test` database created *beside* the shared local stack rather than through `scripts/db-local.sh`, whose `supabase db reset` would have dropped the database a parallel session was using. **Residual, not closable by any PR — and now closed:** the hosted Supabase "allow new signups" toggle has no representation in `supabase/config.toml` and cannot be read or set from this repo; it had been flagged as a pending operator action since 2026-07-06 and again on 2026-07-10 (`2026-07-10-audit-fix-backlog.md` item 2, PR #368). Confirmed **off** in the dashboard on 2026-08-22 (Authentication → Sign In / Providers → User Signups), which fully closes item 2 — ten weeks after the code half shipped, because nothing in the repo could surface it. The same invisibility runs the other way: no test or CI gate will fail if the toggle is ever switched back on, so it is worth re-checking whenever portal auth is touched. Backlog items 1–3 are now checked off with per-bullet evidence, so the next reader does not re-derive a second time that the work was already done.
