- **2026-07-13 (SENTRY-7597067759 — Failed to fetch planned batches by day; branch `sentry-fix/SENTRY-7597067759`).** Root cause: `BatchActivityHeatmap`'s RPC error handler (`src/components/dashboard/batch-activity-heatmap.tsx:104`) logged a destructured plain object (`{message, code, details, hint}`) instead of the actual `PostgrestError` instance, so `client-logger.ts`'s `instanceof Error` check — the gate that routes to `Sentry.captureException` with the real message/stack — missed it and silently fell back to a generic `captureMessage`, which is exactly why the Sentry issue title was the bare literal string with no diagnostic detail and no stack trace. Pattern scan found the same anti-pattern in `NotificationsProvider`'s fetch-notifications error path (`src/contexts/notifications.tsx:126`, passing individual primitives instead of the error object). Both fixed to pass the real error instance, matching the convention already used by the other dashboard trend fetchers. Two regression tests added (`src/components/dashboard/__tests__/batch-activity-heatmap.test.tsx`, new describe block in `src/contexts/__tests__/notifications.test.tsx`) confirmed to fail pre-fix and pass post-fix. Full suite (2225+ tests), typecheck, and lint all clean.
