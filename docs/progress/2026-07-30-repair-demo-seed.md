- **2026-07-30 (repair the rotted demo seed and apply it strictly).** Rebuilt `supabase/seed.sql` against the migration chain after it had drifted across several renames and drops — `batches.batch_number`→`batch_code`, the `fermenter` text column (vessel occupancy sits on `vessels.current_batch_id`, so the seed now carries real `vessels` rows), the dropped `inventory_items.supplier` (now structured `suppliers` + `supplier_catalog` rows), and `package_types`→`containers` + `selling_formats` for `packages`/`order_items`. Audited the result statement-by-statement against the chain rather than against live or `src/types/supabase.ts` (generated from live, and genuinely divergent: the chain still creates `package_types`, `packages.package_type_id`, `order_items.package_type_id` and `batches.fermenter`, while `inventory_items.supplier` really is dropped, at 00161). The audit found six more defects: three wrong migration attributions in the new comments; an over-claim that the file is idempotent (the eight bare-`ON CONFLICT DO NOTHING` catalog inserts hit tables with no unique constraint, so a re-run duplicates every row, and `ON CONFLICT (id)` cannot absorb a natural-key collision on `vessels.name` / `containers.name` / `suppliers` `lower(name)` / `batches.batch_code` / `orders.order_number`); an undocumented collision hazard where migration 00112 already converts the 00029 `keg_types` seed into six keg `containers` under a `UNIQUE` name, so the seed's keg rows duplicate two of them in substance (now documented, with volumes/deposits aligned to `keg_types`); and a `completed_at` left NULL because its trigger is BEFORE UPDATE only, which hid the demo's one finished batch from the TTB views that bucket by it (00237). Kept the `scripts/db-local.sh` strictness flip so seed rot breaks the next bootstrap instead of rotting for months, and added `MGR_SKIP_DEMO_SEED=1` so an unverified strict seed can never block the zero-to-running path. Refs #581 — the repair is chain-derived and static only; `make db-local` (which resets the local database) remains the outstanding replay verification.
