- **2026-07-18 (Invite compensation ordering, #548).** `compensateExistingPortalUser` no longer reverts the profile while the invite-created `customer_portal_users` row may still exist: if link cleanup fails, the upgraded active-customer profile is intentionally kept and the raised INTERNAL_ERROR names the surviving link row (customer/user ids) for operator repair. Pinning test updated plus two new tests (upgrade-kept and recovered-profile variants); docs amended in `docs/data-model/sales.md` and `docs/knowledge/entity-model.md`.
