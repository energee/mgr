- **2026-07-24 (agent-effectiveness hardening, Batch A — branch `chore/harden-agent-effectiveness`).** Stop hook `check-progress-drift.sh` now asks for a `docs/progress/YYYY-MM-DD-*.md` entry (constraint 18) instead of nagging to edit generated PROGRESS.md, and counts committed-this-session work via merge-base so committed sessions can't bypass it. `verify-feature.sh` `manual` branch exits 4 (UNVERIFIED) unless the feature entry carries a dated receipt (`last_verified` + `verified_by` — documented in feature_list.json conventions and AGENTS.md WIP rule); existing manual/passing features intentionally left as-is and now fail verification until walked. Deleted dead `quality-regrade.yml` (10 green runs, zero output — missing `--allowed-tools`) + its ci-workflows.test.ts contracts; scheduled-agent replacement pending. Stale-context fixes: quality.md fact refresh (147→244 migrations; TZ-pin claim corrected), vitest.config.ts comment now states the `env.TZ` pin is inert under the threads pool since #538 (filed issue #568), archive.md's "Next steps"/"Known issues"/"In progress"/"Deferred" sections retitled as frozen-2026-07-12 historical so build-progress.sh output can't mislead fresh agents. New `docs/agents/ci.md` (workflow set + "prefer scheduled agents over Actions for generative jobs") + AGENTS.md routing rows for it and health-audit-and-issue-triage.md. Constraint 3 promoted to ESLint (`consistent-type-definitions: type`; 8 stray interfaces converted, declare-module merge site suppressed inline); constraint 14 promoted to a `bunfig.toml` [test] preload that fails `bun test` with a pointer to `bun run test`.
