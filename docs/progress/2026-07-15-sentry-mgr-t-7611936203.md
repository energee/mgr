- **2026-07-15 (Sentry MGR-T / SENTRY-7611936203 — already fixed, added regression guard).** Investigated a dev-only `/_error` crash on `/production/batches` ("Attempted to call createRevisionHistoryDisplay() from the server but createRevisionHistoryDisplay is on the client", 5 events 06:00–06:05Z). Root cause: an earlier revision of `production/batches/loading.tsx` imported the assembled `batchEntity` from `@/entities/batch` (a client module — `presentation.tsx` calls the client-only `createRevisionHistoryDisplay` factory at module-evaluation top level) instead of the server-safe `batchCore`, so evaluating that import in the server module graph threw. That exact fix already landed on `main` via commit `c6b5539` (PR #453, authored 07:11:53Z — after the last Sentry event), which rewrote `loading.tsx` to render `null`. Confirmed no other route repeats the mistake: every server file under `src/app` imports only `*Core` modules or `@/entities/cores`. Added `src/app/__tests__/server-entity-import-boundary.test.ts`, a static scan that fails if any non-`"use client"` file under `src/app` imports an assembled `@/entities/<name>` config; verified it fails against the pre-fix `loading.tsx` content and passes on current `main`. `make check` green.
