- **2026-07-25 (Ponytail over-engineering audit — verification + cuts, branch `chore/harness-gaps`).** Verified the 47 candidates from `docs/plans/2026-07-24-ponytail-audit-findings.md` (each ref-counted with `rg` against the entity registry, presentation configs, `z.infer` sites, Makefile, workflows, package scripts and docs) and landed the confirmed ones in 12 typed commits: **−6,147 lines, −4 dependencies**, `make check` green throughout. Biggest deletes: the deprecated `transition-side-effects.ts` + its 1,547-line test (superseded by `transition_entity_atomic`, 00256), the entire npm Resend stack (production email is a DB trigger → pg_net → the `send-email` edge function, which uses plain fetch), the unreachable water-chemistry surface, `queryExamples`/`description`/`sortable: true` across all 39 entity cores (nothing reads them; the AI reads `keyFields` and `_schema_registry.query_examples`), the unreachable numeric/range data-table filters, column-pinning style machinery, five never-thrown error classes, and two uncalled API routes (`/api/dev/confirm-user`, quickbooks `sync/batch`). Deps dropped: `resend`, `nanoid` (→ `crypto.getRandomValues` with rejection sampling), `@testing-library/jest-dom` (3 matchers → plain vitest), `dotenv-cli` (→ `set -a; . ./.env`). Two latent bugs fell out: the `isRelativeToToday` date operator was offered in the filter UI but no branch handled it, so it silently degraded to `eq`; and `token-manager.refreshAccessToken` was dead while `client.ts` used a private copy. **Rejected on verification:** the `src/lib/__tests__` "duplicate" suites are supersets, not copies (moves, not deletes); `getYearOptions`, `isTokenExpired`'s class, `ConcurrentModificationError` and `InventoryOverview` all had live callers the hunt missed; the radix umbrella swap is not behaviour-neutral (`react-slot` resolves to two versions); and the duplicate gravity converters use genuinely different formulas (259-approximation vs ASBC cubic, ~0.06 °P apart at 1.050) so the divergence is documented rather than silently unified. **Second pass (same day):** compacted 55 metadata-only `layout.tsx` wrappers to the 4-line form and folded `(app)/error.tsx` + `portal/(main)/error.tsx` onto the shared `RouteError` they were copies of; deleted three never-passed `EntityDetailUnified` props (`backUrl`, `showEdit`, `disabledFields` — threaded end-to-end, always falsy/default), `EntityColumnDef.filterable`, the whole `propFilters` path through the list-query stack (no page passes `filters`, so it widened the select projection, joined the query key and ran a `.eq()` loop for nothing), and `DataTableAdvancedToolbar`'s "reserved for future use" `table` prop. Running total **−7,000 lines**. Also rejected on verification: #5 (the `/api/recipes` + `/api/batches` REST routes are a documented surface in `docs/spec/api-layer.md` with permission gates, and `/api/batches/[id]/transfer` exists specifically to fix a side-effects bypass — deleting them is a product decision, not cleanup); #36 (the eight unit wrappers are only reachable from tests, but they are the tested seam for brewing unit conversion — un-exporting them would delete real coverage for zero runtime gain); #30/#31 (`showInDetail`, `editable: "create-only"` and `dynamicOptions` all have live consumers). **Third pass:** collapsed three LIKE-escape helpers (`escapeLike`, a private `escapeLikePattern` in reorder.ts, `escapeIlikePattern`) onto the only one that escapes `*` — a fix, not a dedup, since every `escapeLike` call site went through PostgREST `.ilike()`/`.or()` where `*` is translated to `%` with no escape syntax, so a search term containing `*` was silently a wildcard in the batches/recipes list routes, the data-table contains-filter, notifications search and three AI chat tools. Also stripped the filter list's never-supplied configurability (`debounceMs`/`throttleMs`/`shallow`/`disabled`, `TableMeta.queryKeys` + the `QueryKeys` type, the four unpopulated `ColumnMeta` fields, `Option.icon`/`count`) and removed drag-to-reorder of filter rows — rows combine under one `and`/`or` join operator, so their order never affected the query. **Rejected in this pass:** #22 and #23 are stale — the empty-state JSX exists once and the bulk-transition intersection is implemented once; #24's kanban `formatCardFieldValue` produces different output from `lib/format`'s `formatValue` ("MMM d, yyyy"/"-" vs "1/5/2026"/"—"), so the divergence is documented in place rather than merged.
