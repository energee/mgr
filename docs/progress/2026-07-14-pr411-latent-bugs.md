- **2026-07-14 (fix three latent bugs documented by PR #411).** The Tier-1 domain move (#411) deliberately locked five known bugs in place with characterization tests rather than fix them mid-refactor. Fixed the three real ones, flipping each quirk test to assert correct behavior first: `isDuplicateTransfer` no longer reports a future timestamp (clock skew) or an unparseable one as a duplicate — either used to block or silently pass a legitimate vessel transfer; `computeFifoFill` now sums duplicate brand+format demand lines instead of last-one-wins overwriting them; `buildBinPlacements` places each `po_receive_id`'s quantity at most once, so two lots sharing a receive can no longer double-count on-hand inventory. Left as-is: the un-clamped `allocated` on `computeDemandLines` (reporting the true allocated quantity on an over-allocated line is correct, not a defect) and `:`-escaping in `comboKey`/`catalogKey` (unreachable — ids are UUIDs). Suite 2339 passing; the 4 `use-persisted-page-size` failures are pre-existing on `main` and unrelated.
