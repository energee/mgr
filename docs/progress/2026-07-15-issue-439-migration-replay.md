- **2026-07-15 (repair fresh migration replay and Square draft-sale schema, issue #439).** Restored the lost `containers`/`selling_formats`/`email_settings` foundation in reserved migration 00112, removed application tables from the plain-Postgres shim, made migration 00097 tolerate its historically out-of-order QuickBooks policy drops, renumbered the colliding customer-portal migration to 00253, and added 00254 to losslessly backfill then retire `square_draft_sales.keg_type_id` in favor of the canonical `selling_format_id` reference and dedup index, with an explicit mismatch guard. Added a real local Supabase/PostgreSQL 17 reset configuration and blocking CI replay job, aligned the tracked local Auth runtime with the linked project's GHCR-published version, refreshed the generated Square draft-sale types, and shared the webhook's canonical payload builder with a real-database regression test that inserts a selling-format-only row, discovers it through the reconciliation projection, stamps it reconciled, and proves the legacy column is absent. A cold Supabase boot plus reset replayed every migration, and the plain-Postgres gate applied all 230 migrations before all 29 database integration tests passed. No migration was deployed, no Square webhook was triggered, and no hosted Supabase data was mutated.
