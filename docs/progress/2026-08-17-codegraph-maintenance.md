- **2026-08-17 (codegraph as a maintenance engine).** Three additions to `tools/codegraph/`: (1) `doctor.ts` — standing deterministic checks over the graph (stale-cache client writes, multi-write files with no atomic RPC — it found exactly the 9 files issue #822 names, dead live-DB objects, untested write paths), ratcheted against a baselined `doctor-allowlist.json` (142 grandfathered) and run `--strict` in the required Static Checks job after a fresh rebuild of the PR's own tree; (2) `codegraph-refresh.yml` — closes #796 by rebuilding the deterministic passes on every main push that touches a graph input and landing `graph.json` through an auto-merged bot PR (progress.yml shape, merge retried); the recommitted graph drops the stale `src/lib/reports/` refs the issue cites; (3) a `tracker` pass (`features.ts`) parsing `docs/feature_list.json` into exact FEATURE edges — `requires` migrations, `documented_in` docs, `verifies` from named test files — so "what backs F201" is a one-hop query. Extractor enum gains `tracker`, EXTRACTOR_VERSION bumped, 10 new vitest cases + 2 workflow contract pins.
