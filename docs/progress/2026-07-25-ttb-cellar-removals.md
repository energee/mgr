- **2026-07-25 (TTB Losses line was structurally 0.00).** Fixed #603: `get_ttb_removals_summary` narrowed its CTE to `source_type = 'finished_good'`, but every writer of a cellar loss (`recordBatchLoss`, `archive_batch`, and the automatic completion reconciliation in `transition_entity_atomic`) emits `source_type='batch'`, so no recorded loss could reach `losses_bbl` on Form 5130.9. Migration 00274 admits batch-sourced removals, classifies them as the `cellar` tax class (an explicit arm is load-bearing — `get_ttb_tax_class(NULL)` returns `'bottled'`), and excludes internal batch movements (`finished_good`/`transfer`/`batch` destinations) so packaged volume is not deducted twice. Covered behaviorally against real Postgres in `src/__tests__/integration/ttb-removals-batch-losses.test.ts` (runs in the db-lint workflow) plus structural assertions in `src/lib/__tests__/ttb-sql.test.ts`; migration carries the self-rolling-back verification block. `get_ttb_inventory_summary`'s in-process terms were left alone deliberately: they are a status snapshot (`ip_ending` has no date filter), so the cellar identity holds neither before nor after — measured, not assumed.
