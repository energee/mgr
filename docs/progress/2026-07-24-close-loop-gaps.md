- **2026-07-24 (close the improvement loop's open loops).** Corpus-guided (Lopopolo harness-engineering) follow-up to PR #582: every scheduled agentic workflow now ends in a deterministic `require-durable-outcome` gate (PR, verifiable citation, or evidence-citing quiet-run file — else red) that also echoes `worker-epoch: model=<id>`; rebuilt `quality-regrade.yml` (v1 died silently for 10 runs without `--allowedTools` — the steering signal had no producer since 2026-07-24) with explicit allowlist + Sonnet + label bootstrap; feedback-distill now reads a deterministic 4-week `loop-scoreboard.ts` (PR acceptance per loop) and must propose retirements with falsifiable promotions; ci.md workflow table machine-checked against the workflows dir (was missing 4 rows + a stale docs-only-skip claim); autoharness venv patches captured as idempotent `scripts/autoharness-setup.sh`. Contract tests extended (all 13 workflows in the array, local composite actions exempt from SHA-pin rule, durable-outcome + regrade-shape + scoreboard + ci.md-coverage contracts).
